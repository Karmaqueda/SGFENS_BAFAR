/*
 * This source file was generated by FireStorm/DAO.
 * 
 * If you purchase a full license for FireStorm/DAO you can customize this header file.
 * 
 * For more information please visit http://www.codefutures.com/products/firestorm
 */

package com.tsp.sct.dao.jdbc;

import com.tsp.sct.dao.dao.*;
import com.tsp.sct.dao.factory.*;
import com.tsp.sct.dao.dto.*;
import com.tsp.sct.dao.exceptions.*;
import java.sql.Connection;
import java.util.Collection;
import java.sql.PreparedStatement;
import java.sql.Statement;
import java.sql.ResultSet;
import java.sql.SQLException;
import java.sql.Time;
import java.util.List;
import java.util.Iterator;
import java.util.ArrayList;

public class PretoModuloDaoImpl extends AbstractDAO implements PretoModuloDao
{
	/** 
	 * The factory class for this DAO has two versions of the create() method - one that
takes no arguments and one that takes a Connection argument. If the Connection version
is chosen then the connection will be stored in this attribute and will be used by all
calls to this DAO, otherwise a new Connection will be allocated for each operation.
	 */
	protected java.sql.Connection userConn;

	/** 
	 * All finder methods in this class use this SELECT constant to build their queries
	 */
	protected final String SQL_SELECT = "SELECT ID_PRETO_MODULO, ID_ESTATUS, NOMBRE_MODULO, IDENTIFICADOR FROM " + getTableName() + "";

	/** 
	 * Finder methods will pass this value to the JDBC setMaxRows method
	 */
	protected int maxRows;

	/** 
	 * SQL INSERT statement for this table
	 */
	protected final String SQL_INSERT = "INSERT INTO " + getTableName() + " ( ID_PRETO_MODULO, ID_ESTATUS, NOMBRE_MODULO, IDENTIFICADOR ) VALUES ( ?, ?, ?, ? )";

	/** 
	 * SQL UPDATE statement for this table
	 */
	protected final String SQL_UPDATE = "UPDATE " + getTableName() + " SET ID_PRETO_MODULO = ?, ID_ESTATUS = ?, NOMBRE_MODULO = ?, IDENTIFICADOR = ? WHERE ID_PRETO_MODULO = ?";

	/** 
	 * SQL DELETE statement for this table
	 */
	protected final String SQL_DELETE = "DELETE FROM " + getTableName() + " WHERE ID_PRETO_MODULO = ?";

	/** 
	 * Index of column ID_PRETO_MODULO
	 */
	protected static final int COLUMN_ID_PRETO_MODULO = 1;

	/** 
	 * Index of column ID_ESTATUS
	 */
	protected static final int COLUMN_ID_ESTATUS = 2;

	/** 
	 * Index of column NOMBRE_MODULO
	 */
	protected static final int COLUMN_NOMBRE_MODULO = 3;

	/** 
	 * Index of column IDENTIFICADOR
	 */
	protected static final int COLUMN_IDENTIFICADOR = 4;

	/** 
	 * Number of columns
	 */
	protected static final int NUMBER_OF_COLUMNS = 4;

	/** 
	 * Index of primary-key column ID_PRETO_MODULO
	 */
	protected static final int PK_COLUMN_ID_PRETO_MODULO = 1;

	/** 
	 * Inserts a new row in the preto_modulo table.
	 */
	public PretoModuloPk insert(PretoModulo dto) throws PretoModuloDaoException
	{
		long t1 = System.currentTimeMillis();
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		ResultSet rs = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			StringBuffer sql = new StringBuffer();
			StringBuffer values = new StringBuffer();
			sql.append( "INSERT INTO " + getTableName() + " (" );
			int modifiedCount = 0;
			if (dto.isIdPretoModuloModified()) {
				if (modifiedCount>0) {
					sql.append( ", " );
					values.append( ", " );
				}
		
				sql.append( "ID_PRETO_MODULO" );
				values.append( "?" );
				modifiedCount++;
			}
		
			if (dto.isIdEstatusModified()) {
				if (modifiedCount>0) {
					sql.append( ", " );
					values.append( ", " );
				}
		
				sql.append( "ID_ESTATUS" );
				values.append( "?" );
				modifiedCount++;
			}
		
			if (dto.isNombreModuloModified()) {
				if (modifiedCount>0) {
					sql.append( ", " );
					values.append( ", " );
				}
		
				sql.append( "NOMBRE_MODULO" );
				values.append( "?" );
				modifiedCount++;
			}
		
			if (dto.isIdentificadorModified()) {
				if (modifiedCount>0) {
					sql.append( ", " );
					values.append( ", " );
				}
		
				sql.append( "IDENTIFICADOR" );
				values.append( "?" );
				modifiedCount++;
			}
		
			if (modifiedCount==0) {
				// nothing to insert
				throw new IllegalStateException( "Nothing to insert" );
			}
		
			sql.append( ") VALUES (" );
			sql.append( values );
			sql.append( ")" );
			stmt = conn.prepareStatement( sql.toString(), Statement.RETURN_GENERATED_KEYS );
			int index = 1;
			if (dto.isIdPretoModuloModified()) {
				stmt.setInt( index++, dto.getIdPretoModulo() );
			}
		
			if (dto.isIdEstatusModified()) {
				if (dto.isIdEstatusNull()) {
					stmt.setNull( index++, java.sql.Types.INTEGER );
				} else {
					stmt.setInt( index++, dto.getIdEstatus() );
				}
		
			}
		
			if (dto.isNombreModuloModified()) {
				stmt.setString( index++, dto.getNombreModulo() );
			}
		
			if (dto.isIdentificadorModified()) {
				stmt.setString( index++, dto.getIdentificador() );
			}
		
			int rows = stmt.executeUpdate();
			long t2 = System.currentTimeMillis();
		
			// retrieve values from auto-increment columns
			rs = stmt.getGeneratedKeys();
			if (rs != null && rs.next()) {
				dto.setIdPretoModulo( rs.getInt( 1 ) );
			}
		
			reset(dto);
			return dto.createPk();
		}
		catch (Exception _e) {
			throw new PretoModuloDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Updates a single row in the preto_modulo table.
	 */
	public void update(PretoModuloPk pk, PretoModulo dto) throws PretoModuloDaoException
	{
		long t1 = System.currentTimeMillis();
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			StringBuffer sql = new StringBuffer();
			sql.append( "UPDATE " + getTableName() + " SET " );
			boolean modified = false;
			if (dto.isIdPretoModuloModified()) {
				if (modified) {
					sql.append( ", " );
				}
		
				sql.append( "ID_PRETO_MODULO=?" );
				modified=true;
			}
		
			if (dto.isIdEstatusModified()) {
				if (modified) {
					sql.append( ", " );
				}
		
				sql.append( "ID_ESTATUS=?" );
				modified=true;
			}
		
			if (dto.isNombreModuloModified()) {
				if (modified) {
					sql.append( ", " );
				}
		
				sql.append( "NOMBRE_MODULO=?" );
				modified=true;
			}
		
			if (dto.isIdentificadorModified()) {
				if (modified) {
					sql.append( ", " );
				}
		
				sql.append( "IDENTIFICADOR=?" );
				modified=true;
			}
		
			if (!modified) {
				// nothing to update
				return;
			}
		
			sql.append( " WHERE ID_PRETO_MODULO=?" );
			stmt = conn.prepareStatement( sql.toString() );
			int index = 1;
			if (dto.isIdPretoModuloModified()) {
				stmt.setInt( index++, dto.getIdPretoModulo() );
			}
		
			if (dto.isIdEstatusModified()) {
				if (dto.isIdEstatusNull()) {
					stmt.setNull( index++, java.sql.Types.INTEGER );
				} else {
					stmt.setInt( index++, dto.getIdEstatus() );
				}
		
			}
		
			if (dto.isNombreModuloModified()) {
				stmt.setString( index++, dto.getNombreModulo() );
			}
		
			if (dto.isIdentificadorModified()) {
				stmt.setString( index++, dto.getIdentificador() );
			}
		
			stmt.setInt( index++, pk.getIdPretoModulo() );
			int rows = stmt.executeUpdate();
			reset(dto);
			long t2 = System.currentTimeMillis();
		}
		catch (Exception _e) {
			throw new PretoModuloDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Deletes a single row in the preto_modulo table.
	 */
	public void delete(PretoModuloPk pk) throws PretoModuloDaoException
	{
		long t1 = System.currentTimeMillis();
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			stmt = conn.prepareStatement( SQL_DELETE );
			stmt.setInt( 1, pk.getIdPretoModulo() );
			int rows = stmt.executeUpdate();
			long t2 = System.currentTimeMillis();
		}
		catch (Exception _e) {
			throw new PretoModuloDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Returns the rows from the preto_modulo table that matches the specified primary-key value.
	 */
	public PretoModulo findByPrimaryKey(PretoModuloPk pk) throws PretoModuloDaoException
	{
		return findByPrimaryKey( pk.getIdPretoModulo() );
	}

	/** 
	 * Returns all rows from the preto_modulo table that match the criteria 'ID_PRETO_MODULO = :idPretoModulo'.
	 */
	public PretoModulo findByPrimaryKey(int idPretoModulo) throws PretoModuloDaoException
	{
		PretoModulo ret[] = findByDynamicSelect( SQL_SELECT + " WHERE ID_PRETO_MODULO = ?", new Object[] {  new Integer(idPretoModulo) } );
		return ret.length==0 ? null : ret[0];
	}

	/** 
	 * Returns all rows from the preto_modulo table that match the criteria ''.
	 */
	public PretoModulo[] findAll() throws PretoModuloDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " ORDER BY ID_PRETO_MODULO", null );
	}

	/** 
	 * Returns all rows from the preto_modulo table that match the criteria 'ID_PRETO_MODULO = :idPretoModulo'.
	 */
	public PretoModulo[] findWhereIdPretoModuloEquals(int idPretoModulo) throws PretoModuloDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE ID_PRETO_MODULO = ? ORDER BY ID_PRETO_MODULO", new Object[] {  new Integer(idPretoModulo) } );
	}

	/** 
	 * Returns all rows from the preto_modulo table that match the criteria 'ID_ESTATUS = :idEstatus'.
	 */
	public PretoModulo[] findWhereIdEstatusEquals(int idEstatus) throws PretoModuloDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE ID_ESTATUS = ? ORDER BY ID_ESTATUS", new Object[] {  new Integer(idEstatus) } );
	}

	/** 
	 * Returns all rows from the preto_modulo table that match the criteria 'NOMBRE_MODULO = :nombreModulo'.
	 */
	public PretoModulo[] findWhereNombreModuloEquals(String nombreModulo) throws PretoModuloDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE NOMBRE_MODULO = ? ORDER BY NOMBRE_MODULO", new Object[] { nombreModulo } );
	}

	/** 
	 * Returns all rows from the preto_modulo table that match the criteria 'IDENTIFICADOR = :identificador'.
	 */
	public PretoModulo[] findWhereIdentificadorEquals(String identificador) throws PretoModuloDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE IDENTIFICADOR = ? ORDER BY IDENTIFICADOR", new Object[] { identificador } );
	}

	/**
	 * Method 'PretoModuloDaoImpl'
	 * 
	 */
	public PretoModuloDaoImpl()
	{
	}

	/**
	 * Method 'PretoModuloDaoImpl'
	 * 
	 * @param userConn
	 */
	public PretoModuloDaoImpl(final java.sql.Connection userConn)
	{
		this.userConn = userConn;
	}

	/** 
	 * Sets the value of maxRows
	 */
	public void setMaxRows(int maxRows)
	{
		this.maxRows = maxRows;
	}

	/** 
	 * Gets the value of maxRows
	 */
	public int getMaxRows()
	{
		return maxRows;
	}

	/**
	 * Method 'getTableName'
	 * 
	 * @return String
	 */
	public String getTableName()
	{
		return "preto_modulo";
	}

	/** 
	 * Fetches a single row from the result set
	 */
	protected PretoModulo fetchSingleResult(ResultSet rs) throws SQLException
	{
		if (rs.next()) {
			PretoModulo dto = new PretoModulo();
			populateDto( dto, rs);
			return dto;
		} else {
			return null;
		}
		
	}

	/** 
	 * Fetches multiple rows from the result set
	 */
	protected PretoModulo[] fetchMultiResults(ResultSet rs) throws SQLException
	{
		Collection resultList = new ArrayList();
		while (rs.next()) {
			PretoModulo dto = new PretoModulo();
			populateDto( dto, rs);
			resultList.add( dto );
		}
		
		PretoModulo ret[] = new PretoModulo[ resultList.size() ];
		resultList.toArray( ret );
		return ret;
	}

	/** 
	 * Populates a DTO with data from a ResultSet
	 */
	protected void populateDto(PretoModulo dto, ResultSet rs) throws SQLException
	{
		dto.setIdPretoModulo( rs.getInt( COLUMN_ID_PRETO_MODULO ) );
		dto.setIdEstatus( rs.getInt( COLUMN_ID_ESTATUS ) );
		if (rs.wasNull()) {
			dto.setIdEstatusNull( true );
		}
		
		dto.setNombreModulo( rs.getString( COLUMN_NOMBRE_MODULO ) );
		dto.setIdentificador( rs.getString( COLUMN_IDENTIFICADOR ) );
		reset(dto);
	}

	/** 
	 * Resets the modified attributes in the DTO
	 */
	protected void reset(PretoModulo dto)
	{
		dto.setIdPretoModuloModified( false );
		dto.setIdEstatusModified( false );
		dto.setNombreModuloModified( false );
		dto.setIdentificadorModified( false );
	}

	/** 
	 * Returns all rows from the preto_modulo table that match the specified arbitrary SQL statement
	 */
	public PretoModulo[] findByDynamicSelect(String sql, Object[] sqlParams) throws PretoModuloDaoException
	{
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		ResultSet rs = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			// construct the SQL statement
			final String SQL = sql;
		
		
			// prepare statement
			stmt = conn.prepareStatement( SQL );
			stmt.setMaxRows( maxRows );
		
			// bind parameters
			for (int i=0; sqlParams!=null && i<sqlParams.length; i++ ) {
				stmt.setObject( i+1, sqlParams[i] );
			}
		
		
			rs = stmt.executeQuery();
		
			// fetch the results
			return fetchMultiResults(rs);
		}
		catch (Exception _e) {
			throw new PretoModuloDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(rs);
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Returns all rows from the preto_modulo table that match the specified arbitrary SQL statement
	 */
	public PretoModulo[] findByDynamicWhere(String sql, Object[] sqlParams) throws PretoModuloDaoException
	{
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		ResultSet rs = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			// construct the SQL statement
			final String SQL = SQL_SELECT + " WHERE " + sql;
		
		
			// prepare statement
			stmt = conn.prepareStatement( SQL );
			stmt.setMaxRows( maxRows );
		
			// bind parameters
			for (int i=0; sqlParams!=null && i<sqlParams.length; i++ ) {
				stmt.setObject( i+1, sqlParams[i] );
			}
		
		
			rs = stmt.executeQuery();
		
			// fetch the results
			return fetchMultiResults(rs);
		}
		catch (Exception _e) {
			throw new PretoModuloDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(rs);
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

}
