/*
 * This source file was generated by FireStorm/DAO.
 * 
 * If you purchase a full license for FireStorm/DAO you can customize this header file.
 * 
 * For more information please visit http://www.codefutures.com/products/firestorm
 */

package com.tsp.sct.dao.jdbc;

import com.tsp.sct.dao.dao.*;
import com.tsp.sct.dao.factory.*;
import com.tsp.sct.dao.dto.*;
import com.tsp.sct.dao.exceptions.*;
import java.sql.Connection;
import java.util.Collection;
import java.sql.PreparedStatement;
import java.sql.Statement;
import java.sql.ResultSet;
import java.sql.SQLException;
import java.sql.Time;
import java.util.List;
import java.util.Iterator;
import java.util.ArrayList;

public class SarCamposAdicionalesDaoImpl extends AbstractDAO implements SarCamposAdicionalesDao
{
	/** 
	 * The factory class for this DAO has two versions of the create() method - one that
takes no arguments and one that takes a Connection argument. If the Connection version
is chosen then the connection will be stored in this attribute and will be used by all
calls to this DAO, otherwise a new Connection will be allocated for each operation.
	 */
	protected java.sql.Connection userConn;

	/** 
	 * All finder methods in this class use this SELECT constant to build their queries
	 */
	protected final String SQL_SELECT = "SELECT ID_SAR_USUARIO, EXT_REQ_ORDEN_COMPRA FROM " + getTableName() + "";

	/** 
	 * Finder methods will pass this value to the JDBC setMaxRows method
	 */
	protected int maxRows;

	/** 
	 * SQL INSERT statement for this table
	 */
	protected final String SQL_INSERT = "INSERT INTO " + getTableName() + " ( ID_SAR_USUARIO, EXT_REQ_ORDEN_COMPRA ) VALUES ( ?, ? )";

	/** 
	 * SQL UPDATE statement for this table
	 */
	protected final String SQL_UPDATE = "UPDATE " + getTableName() + " SET ID_SAR_USUARIO = ?, EXT_REQ_ORDEN_COMPRA = ? WHERE ID_SAR_USUARIO = ?";

	/** 
	 * SQL DELETE statement for this table
	 */
	protected final String SQL_DELETE = "DELETE FROM " + getTableName() + " WHERE ID_SAR_USUARIO = ?";

	/** 
	 * Index of column ID_SAR_USUARIO
	 */
	protected static final int COLUMN_ID_SAR_USUARIO = 1;

	/** 
	 * Index of column EXT_REQ_ORDEN_COMPRA
	 */
	protected static final int COLUMN_EXT_REQ_ORDEN_COMPRA = 2;

	/** 
	 * Number of columns
	 */
	protected static final int NUMBER_OF_COLUMNS = 2;

	/** 
	 * Index of primary-key column ID_SAR_USUARIO
	 */
	protected static final int PK_COLUMN_ID_SAR_USUARIO = 1;

	/** 
	 * Inserts a new row in the sar_campos_adicionales table.
	 */
	public SarCamposAdicionalesPk insert(SarCamposAdicionales dto) throws SarCamposAdicionalesDaoException
	{
		long t1 = System.currentTimeMillis();
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		ResultSet rs = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			StringBuffer sql = new StringBuffer();
			StringBuffer values = new StringBuffer();
			sql.append( "INSERT INTO " + getTableName() + " (" );
			int modifiedCount = 0;
			if (dto.isIdSarUsuarioModified()) {
				if (modifiedCount>0) {
					sql.append( ", " );
					values.append( ", " );
				}
		
				sql.append( "ID_SAR_USUARIO" );
				values.append( "?" );
				modifiedCount++;
			}
		
			if (dto.isExtReqOrdenCompraModified()) {
				if (modifiedCount>0) {
					sql.append( ", " );
					values.append( ", " );
				}
		
				sql.append( "EXT_REQ_ORDEN_COMPRA" );
				values.append( "?" );
				modifiedCount++;
			}
		
			if (modifiedCount==0) {
				// nothing to insert
				throw new IllegalStateException( "Nothing to insert" );
			}
		
			sql.append( ") VALUES (" );
			sql.append( values );
			sql.append( ")" );
			stmt = conn.prepareStatement( sql.toString() );
			int index = 1;
			if (dto.isIdSarUsuarioModified()) {
				stmt.setInt( index++, dto.getIdSarUsuario() );
			}
		
			if (dto.isExtReqOrdenCompraModified()) {
				stmt.setInt( index++, dto.getExtReqOrdenCompra() );
			}
		
			System.out.println( "Executing " + sql.toString() + " with values: " + dto );
			int rows = stmt.executeUpdate();
			long t2 = System.currentTimeMillis();
			System.out.println( rows + " rows affected (" + (t2-t1) + " ms)" );
			reset(dto);
			return dto.createPk();
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new SarCamposAdicionalesDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Updates a single row in the sar_campos_adicionales table.
	 */
	public void update(SarCamposAdicionalesPk pk, SarCamposAdicionales dto) throws SarCamposAdicionalesDaoException
	{
		long t1 = System.currentTimeMillis();
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			StringBuffer sql = new StringBuffer();
			sql.append( "UPDATE " + getTableName() + " SET " );
			boolean modified = false;
			if (dto.isIdSarUsuarioModified()) {
				if (modified) {
					sql.append( ", " );
				}
		
				sql.append( "ID_SAR_USUARIO=?" );
				modified=true;
			}
		
			if (dto.isExtReqOrdenCompraModified()) {
				if (modified) {
					sql.append( ", " );
				}
		
				sql.append( "EXT_REQ_ORDEN_COMPRA=?" );
				modified=true;
			}
		
			if (!modified) {
				// nothing to update
				return;
			}
		
			sql.append( " WHERE ID_SAR_USUARIO=?" );
			System.out.println( "Executing " + sql.toString() + " with values: " + dto );
			stmt = conn.prepareStatement( sql.toString() );
			int index = 1;
			if (dto.isIdSarUsuarioModified()) {
				stmt.setInt( index++, dto.getIdSarUsuario() );
			}
		
			if (dto.isExtReqOrdenCompraModified()) {
				stmt.setInt( index++, dto.getExtReqOrdenCompra() );
			}
		
			stmt.setInt( index++, pk.getIdSarUsuario() );
			int rows = stmt.executeUpdate();
			reset(dto);
			long t2 = System.currentTimeMillis();
			System.out.println( rows + " rows affected (" + (t2-t1) + " ms)" );
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new SarCamposAdicionalesDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Deletes a single row in the sar_campos_adicionales table.
	 */
	public void delete(SarCamposAdicionalesPk pk) throws SarCamposAdicionalesDaoException
	{
		long t1 = System.currentTimeMillis();
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			System.out.println( "Executing " + SQL_DELETE + " with PK: " + pk );
			stmt = conn.prepareStatement( SQL_DELETE );
			stmt.setInt( 1, pk.getIdSarUsuario() );
			int rows = stmt.executeUpdate();
			long t2 = System.currentTimeMillis();
			System.out.println( rows + " rows affected (" + (t2-t1) + " ms)" );
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new SarCamposAdicionalesDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Returns the rows from the sar_campos_adicionales table that matches the specified primary-key value.
	 */
	public SarCamposAdicionales findByPrimaryKey(SarCamposAdicionalesPk pk) throws SarCamposAdicionalesDaoException
	{
		return findByPrimaryKey( pk.getIdSarUsuario() );
	}

	/** 
	 * Returns all rows from the sar_campos_adicionales table that match the criteria 'ID_SAR_USUARIO = :idSarUsuario'.
	 */
	public SarCamposAdicionales findByPrimaryKey(int idSarUsuario) throws SarCamposAdicionalesDaoException
	{
		SarCamposAdicionales ret[] = findByDynamicSelect( SQL_SELECT + " WHERE ID_SAR_USUARIO = ?", new Object[] {  new Integer(idSarUsuario) } );
		return ret.length==0 ? null : ret[0];
	}

	/** 
	 * Returns all rows from the sar_campos_adicionales table that match the criteria ''.
	 */
	public SarCamposAdicionales[] findAll() throws SarCamposAdicionalesDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " ORDER BY ID_SAR_USUARIO", null );
	}

	/** 
	 * Returns all rows from the sar_campos_adicionales table that match the criteria 'ID_SAR_USUARIO = :idSarUsuario'.
	 */
	public SarCamposAdicionales[] findWhereIdSarUsuarioEquals(int idSarUsuario) throws SarCamposAdicionalesDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE ID_SAR_USUARIO = ? ORDER BY ID_SAR_USUARIO", new Object[] {  new Integer(idSarUsuario) } );
	}

	/** 
	 * Returns all rows from the sar_campos_adicionales table that match the criteria 'EXT_REQ_ORDEN_COMPRA = :extReqOrdenCompra'.
	 */
	public SarCamposAdicionales[] findWhereExtReqOrdenCompraEquals(int extReqOrdenCompra) throws SarCamposAdicionalesDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE EXT_REQ_ORDEN_COMPRA = ? ORDER BY EXT_REQ_ORDEN_COMPRA", new Object[] {  new Integer(extReqOrdenCompra) } );
	}

	/**
	 * Method 'SarCamposAdicionalesDaoImpl'
	 * 
	 */
	public SarCamposAdicionalesDaoImpl()
	{
	}

	/**
	 * Method 'SarCamposAdicionalesDaoImpl'
	 * 
	 * @param userConn
	 */
	public SarCamposAdicionalesDaoImpl(final java.sql.Connection userConn)
	{
		this.userConn = userConn;
	}

	/** 
	 * Sets the value of maxRows
	 */
	public void setMaxRows(int maxRows)
	{
		this.maxRows = maxRows;
	}

	/** 
	 * Gets the value of maxRows
	 */
	public int getMaxRows()
	{
		return maxRows;
	}

	/**
	 * Method 'getTableName'
	 * 
	 * @return String
	 */
	public String getTableName()
	{
		return "sar_campos_adicionales";
	}

	/** 
	 * Fetches a single row from the result set
	 */
	protected SarCamposAdicionales fetchSingleResult(ResultSet rs) throws SQLException
	{
		if (rs.next()) {
			SarCamposAdicionales dto = new SarCamposAdicionales();
			populateDto( dto, rs);
			return dto;
		} else {
			return null;
		}
		
	}

	/** 
	 * Fetches multiple rows from the result set
	 */
	protected SarCamposAdicionales[] fetchMultiResults(ResultSet rs) throws SQLException
	{
		Collection resultList = new ArrayList();
		while (rs.next()) {
			SarCamposAdicionales dto = new SarCamposAdicionales();
			populateDto( dto, rs);
			resultList.add( dto );
		}
		
		SarCamposAdicionales ret[] = new SarCamposAdicionales[ resultList.size() ];
		resultList.toArray( ret );
		return ret;
	}

	/** 
	 * Populates a DTO with data from a ResultSet
	 */
	protected void populateDto(SarCamposAdicionales dto, ResultSet rs) throws SQLException
	{
		dto.setIdSarUsuario( rs.getInt( COLUMN_ID_SAR_USUARIO ) );
		dto.setExtReqOrdenCompra( rs.getInt( COLUMN_EXT_REQ_ORDEN_COMPRA ) );
		reset(dto);
	}

	/** 
	 * Resets the modified attributes in the DTO
	 */
	protected void reset(SarCamposAdicionales dto)
	{
		dto.setIdSarUsuarioModified( false );
		dto.setExtReqOrdenCompraModified( false );
	}

	/** 
	 * Returns all rows from the sar_campos_adicionales table that match the specified arbitrary SQL statement
	 */
	public SarCamposAdicionales[] findByDynamicSelect(String sql, Object[] sqlParams) throws SarCamposAdicionalesDaoException
	{
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		ResultSet rs = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			// construct the SQL statement
			final String SQL = sql;
		
		
			System.out.println( "Executing " + SQL );
			// prepare statement
			stmt = conn.prepareStatement( SQL );
			stmt.setMaxRows( maxRows );
		
			// bind parameters
			for (int i=0; sqlParams!=null && i<sqlParams.length; i++ ) {
				stmt.setObject( i+1, sqlParams[i] );
			}
		
		
			rs = stmt.executeQuery();
		
			// fetch the results
			return fetchMultiResults(rs);
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new SarCamposAdicionalesDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(rs);
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Returns all rows from the sar_campos_adicionales table that match the specified arbitrary SQL statement
	 */
	public SarCamposAdicionales[] findByDynamicWhere(String sql, Object[] sqlParams) throws SarCamposAdicionalesDaoException
	{
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		ResultSet rs = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			// construct the SQL statement
			final String SQL = SQL_SELECT + " WHERE " + sql;
		
		
			System.out.println( "Executing " + SQL );
			// prepare statement
			stmt = conn.prepareStatement( SQL );
			stmt.setMaxRows( maxRows );
		
			// bind parameters
			for (int i=0; sqlParams!=null && i<sqlParams.length; i++ ) {
				stmt.setObject( i+1, sqlParams[i] );
			}
		
		
			rs = stmt.executeQuery();
		
			// fetch the results
			return fetchMultiResults(rs);
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new SarCamposAdicionalesDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(rs);
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

}
