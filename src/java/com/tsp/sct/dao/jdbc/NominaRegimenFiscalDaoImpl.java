/*
 * This source file was generated by FireStorm/DAO.
 * 
 * If you purchase a full license for FireStorm/DAO you can customize this header file.
 * 
 * For more information please visit http://www.codefutures.com/products/firestorm
 */

package com.tsp.sct.dao.jdbc;

import com.tsp.sct.dao.dao.*;
import com.tsp.sct.dao.factory.*;
import com.tsp.sct.dao.dto.*;
import com.tsp.sct.dao.exceptions.*;
import java.sql.Connection;
import java.util.Collection;
import java.sql.PreparedStatement;
import java.sql.Statement;
import java.sql.ResultSet;
import java.sql.SQLException;
import java.sql.Time;
import java.util.List;
import java.util.Iterator;
import java.util.ArrayList;

public class NominaRegimenFiscalDaoImpl extends AbstractDAO implements NominaRegimenFiscalDao
{
	/** 
	 * The factory class for this DAO has two versions of the create() method - one that
takes no arguments and one that takes a Connection argument. If the Connection version
is chosen then the connection will be stored in this attribute and will be used by all
calls to this DAO, otherwise a new Connection will be allocated for each operation.
	 */
	protected java.sql.Connection userConn;

	/** 
	 * All finder methods in this class use this SELECT constant to build their queries
	 */
	protected final String SQL_SELECT = "SELECT ID_REGIMEN, REGIMEN_FISCAL, CLAVE FROM " + getTableName() + "";

	/** 
	 * Finder methods will pass this value to the JDBC setMaxRows method
	 */
	protected int maxRows;

	/** 
	 * SQL INSERT statement for this table
	 */
	protected final String SQL_INSERT = "INSERT INTO " + getTableName() + " ( ID_REGIMEN, REGIMEN_FISCAL, CLAVE ) VALUES ( ?, ?, ? )";

	/** 
	 * SQL UPDATE statement for this table
	 */
	protected final String SQL_UPDATE = "UPDATE " + getTableName() + " SET ID_REGIMEN = ?, REGIMEN_FISCAL = ?, CLAVE = ? WHERE ID_REGIMEN = ?";

	/** 
	 * SQL DELETE statement for this table
	 */
	protected final String SQL_DELETE = "DELETE FROM " + getTableName() + " WHERE ID_REGIMEN = ?";

	/** 
	 * Index of column ID_REGIMEN
	 */
	protected static final int COLUMN_ID_REGIMEN = 1;

	/** 
	 * Index of column REGIMEN_FISCAL
	 */
	protected static final int COLUMN_REGIMEN_FISCAL = 2;

	/** 
	 * Index of column CLAVE
	 */
	protected static final int COLUMN_CLAVE = 3;

	/** 
	 * Number of columns
	 */
	protected static final int NUMBER_OF_COLUMNS = 3;

	/** 
	 * Index of primary-key column ID_REGIMEN
	 */
	protected static final int PK_COLUMN_ID_REGIMEN = 1;

	/** 
	 * Inserts a new row in the nomina_regimen_fiscal table.
	 */
	public NominaRegimenFiscalPk insert(NominaRegimenFiscal dto) throws NominaRegimenFiscalDaoException
	{
		long t1 = System.currentTimeMillis();
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		ResultSet rs = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			StringBuffer sql = new StringBuffer();
			StringBuffer values = new StringBuffer();
			sql.append( "INSERT INTO " + getTableName() + " (" );
			int modifiedCount = 0;
			if (dto.isIdRegimenModified()) {
				if (modifiedCount>0) {
					sql.append( ", " );
					values.append( ", " );
				}
		
				sql.append( "ID_REGIMEN" );
				values.append( "?" );
				modifiedCount++;
			}
		
			if (dto.isRegimenFiscalModified()) {
				if (modifiedCount>0) {
					sql.append( ", " );
					values.append( ", " );
				}
		
				sql.append( "REGIMEN_FISCAL" );
				values.append( "?" );
				modifiedCount++;
			}
		
			if (dto.isClaveModified()) {
				if (modifiedCount>0) {
					sql.append( ", " );
					values.append( ", " );
				}
		
				sql.append( "CLAVE" );
				values.append( "?" );
				modifiedCount++;
			}
		
			if (modifiedCount==0) {
				// nothing to insert
				throw new IllegalStateException( "Nothing to insert" );
			}
		
			sql.append( ") VALUES (" );
			sql.append( values );
			sql.append( ")" );
			stmt = conn.prepareStatement( sql.toString(), Statement.RETURN_GENERATED_KEYS );
			int index = 1;
			if (dto.isIdRegimenModified()) {
				stmt.setInt( index++, dto.getIdRegimen() );
			}
		
			if (dto.isRegimenFiscalModified()) {
				stmt.setString( index++, dto.getRegimenFiscal() );
			}
		
			if (dto.isClaveModified()) {
				if (dto.isClaveNull()) {
					stmt.setNull( index++, java.sql.Types.INTEGER );
				} else {
					stmt.setInt( index++, dto.getClave() );
				}
		
			}
		
			System.out.println( "Executing " + sql.toString() + " with values: " + dto );
			int rows = stmt.executeUpdate();
			long t2 = System.currentTimeMillis();
			System.out.println( rows + " rows affected (" + (t2-t1) + " ms)" );
		
			// retrieve values from auto-increment columns
			rs = stmt.getGeneratedKeys();
			if (rs != null && rs.next()) {
				dto.setIdRegimen( rs.getInt( 1 ) );
			}
		
			reset(dto);
			return dto.createPk();
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new NominaRegimenFiscalDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Updates a single row in the nomina_regimen_fiscal table.
	 */
	public void update(NominaRegimenFiscalPk pk, NominaRegimenFiscal dto) throws NominaRegimenFiscalDaoException
	{
		long t1 = System.currentTimeMillis();
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			StringBuffer sql = new StringBuffer();
			sql.append( "UPDATE " + getTableName() + " SET " );
			boolean modified = false;
			if (dto.isIdRegimenModified()) {
				if (modified) {
					sql.append( ", " );
				}
		
				sql.append( "ID_REGIMEN=?" );
				modified=true;
			}
		
			if (dto.isRegimenFiscalModified()) {
				if (modified) {
					sql.append( ", " );
				}
		
				sql.append( "REGIMEN_FISCAL=?" );
				modified=true;
			}
		
			if (dto.isClaveModified()) {
				if (modified) {
					sql.append( ", " );
				}
		
				sql.append( "CLAVE=?" );
				modified=true;
			}
		
			if (!modified) {
				// nothing to update
				return;
			}
		
			sql.append( " WHERE ID_REGIMEN=?" );
			System.out.println( "Executing " + sql.toString() + " with values: " + dto );
			stmt = conn.prepareStatement( sql.toString() );
			int index = 1;
			if (dto.isIdRegimenModified()) {
				stmt.setInt( index++, dto.getIdRegimen() );
			}
		
			if (dto.isRegimenFiscalModified()) {
				stmt.setString( index++, dto.getRegimenFiscal() );
			}
		
			if (dto.isClaveModified()) {
				if (dto.isClaveNull()) {
					stmt.setNull( index++, java.sql.Types.INTEGER );
				} else {
					stmt.setInt( index++, dto.getClave() );
				}
		
			}
		
			stmt.setInt( index++, pk.getIdRegimen() );
			int rows = stmt.executeUpdate();
			reset(dto);
			long t2 = System.currentTimeMillis();
			System.out.println( rows + " rows affected (" + (t2-t1) + " ms)" );
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new NominaRegimenFiscalDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Deletes a single row in the nomina_regimen_fiscal table.
	 */
	public void delete(NominaRegimenFiscalPk pk) throws NominaRegimenFiscalDaoException
	{
		long t1 = System.currentTimeMillis();
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			System.out.println( "Executing " + SQL_DELETE + " with PK: " + pk );
			stmt = conn.prepareStatement( SQL_DELETE );
			stmt.setInt( 1, pk.getIdRegimen() );
			int rows = stmt.executeUpdate();
			long t2 = System.currentTimeMillis();
			System.out.println( rows + " rows affected (" + (t2-t1) + " ms)" );
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new NominaRegimenFiscalDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Returns the rows from the nomina_regimen_fiscal table that matches the specified primary-key value.
	 */
	public NominaRegimenFiscal findByPrimaryKey(NominaRegimenFiscalPk pk) throws NominaRegimenFiscalDaoException
	{
		return findByPrimaryKey( pk.getIdRegimen() );
	}

	/** 
	 * Returns all rows from the nomina_regimen_fiscal table that match the criteria 'ID_REGIMEN = :idRegimen'.
	 */
	public NominaRegimenFiscal findByPrimaryKey(int idRegimen) throws NominaRegimenFiscalDaoException
	{
		NominaRegimenFiscal ret[] = findByDynamicSelect( SQL_SELECT + " WHERE ID_REGIMEN = ?", new Object[] {  new Integer(idRegimen) } );
		return ret.length==0 ? null : ret[0];
	}

	/** 
	 * Returns all rows from the nomina_regimen_fiscal table that match the criteria ''.
	 */
	public NominaRegimenFiscal[] findAll() throws NominaRegimenFiscalDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " ORDER BY ID_REGIMEN", null );
	}

	/** 
	 * Returns all rows from the nomina_regimen_fiscal table that match the criteria 'ID_REGIMEN = :idRegimen'.
	 */
	public NominaRegimenFiscal[] findWhereIdRegimenEquals(int idRegimen) throws NominaRegimenFiscalDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE ID_REGIMEN = ? ORDER BY ID_REGIMEN", new Object[] {  new Integer(idRegimen) } );
	}

	/** 
	 * Returns all rows from the nomina_regimen_fiscal table that match the criteria 'REGIMEN_FISCAL = :regimenFiscal'.
	 */
	public NominaRegimenFiscal[] findWhereRegimenFiscalEquals(String regimenFiscal) throws NominaRegimenFiscalDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE REGIMEN_FISCAL = ? ORDER BY REGIMEN_FISCAL", new Object[] { regimenFiscal } );
	}

	/** 
	 * Returns all rows from the nomina_regimen_fiscal table that match the criteria 'CLAVE = :clave'.
	 */
	public NominaRegimenFiscal[] findWhereClaveEquals(int clave) throws NominaRegimenFiscalDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE CLAVE = ? ORDER BY CLAVE", new Object[] {  new Integer(clave) } );
	}

	/**
	 * Method 'NominaRegimenFiscalDaoImpl'
	 * 
	 */
	public NominaRegimenFiscalDaoImpl()
	{
	}

	/**
	 * Method 'NominaRegimenFiscalDaoImpl'
	 * 
	 * @param userConn
	 */
	public NominaRegimenFiscalDaoImpl(final java.sql.Connection userConn)
	{
		this.userConn = userConn;
	}

	/** 
	 * Sets the value of maxRows
	 */
	public void setMaxRows(int maxRows)
	{
		this.maxRows = maxRows;
	}

	/** 
	 * Gets the value of maxRows
	 */
	public int getMaxRows()
	{
		return maxRows;
	}

	/**
	 * Method 'getTableName'
	 * 
	 * @return String
	 */
	public String getTableName()
	{
		return "nomina_regimen_fiscal";
	}

	/** 
	 * Fetches a single row from the result set
	 */
	protected NominaRegimenFiscal fetchSingleResult(ResultSet rs) throws SQLException
	{
		if (rs.next()) {
			NominaRegimenFiscal dto = new NominaRegimenFiscal();
			populateDto( dto, rs);
			return dto;
		} else {
			return null;
		}
		
	}

	/** 
	 * Fetches multiple rows from the result set
	 */
	protected NominaRegimenFiscal[] fetchMultiResults(ResultSet rs) throws SQLException
	{
		Collection resultList = new ArrayList();
		while (rs.next()) {
			NominaRegimenFiscal dto = new NominaRegimenFiscal();
			populateDto( dto, rs);
			resultList.add( dto );
		}
		
		NominaRegimenFiscal ret[] = new NominaRegimenFiscal[ resultList.size() ];
		resultList.toArray( ret );
		return ret;
	}

	/** 
	 * Populates a DTO with data from a ResultSet
	 */
	protected void populateDto(NominaRegimenFiscal dto, ResultSet rs) throws SQLException
	{
		dto.setIdRegimen( rs.getInt( COLUMN_ID_REGIMEN ) );
		dto.setRegimenFiscal( rs.getString( COLUMN_REGIMEN_FISCAL ) );
		dto.setClave( rs.getInt( COLUMN_CLAVE ) );
		if (rs.wasNull()) {
			dto.setClaveNull( true );
		}
		
		reset(dto);
	}

	/** 
	 * Resets the modified attributes in the DTO
	 */
	protected void reset(NominaRegimenFiscal dto)
	{
		dto.setIdRegimenModified( false );
		dto.setRegimenFiscalModified( false );
		dto.setClaveModified( false );
	}

	/** 
	 * Returns all rows from the nomina_regimen_fiscal table that match the specified arbitrary SQL statement
	 */
	public NominaRegimenFiscal[] findByDynamicSelect(String sql, Object[] sqlParams) throws NominaRegimenFiscalDaoException
	{
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		ResultSet rs = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			// construct the SQL statement
			final String SQL = sql;
		
		
			System.out.println( "Executing " + SQL );
			// prepare statement
			stmt = conn.prepareStatement( SQL );
			stmt.setMaxRows( maxRows );
		
			// bind parameters
			for (int i=0; sqlParams!=null && i<sqlParams.length; i++ ) {
				stmt.setObject( i+1, sqlParams[i] );
			}
		
		
			rs = stmt.executeQuery();
		
			// fetch the results
			return fetchMultiResults(rs);
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new NominaRegimenFiscalDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(rs);
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Returns all rows from the nomina_regimen_fiscal table that match the specified arbitrary SQL statement
	 */
	public NominaRegimenFiscal[] findByDynamicWhere(String sql, Object[] sqlParams) throws NominaRegimenFiscalDaoException
	{
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		ResultSet rs = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			// construct the SQL statement
			final String SQL = SQL_SELECT + " WHERE " + sql;
		
		
			System.out.println( "Executing " + SQL );
			// prepare statement
			stmt = conn.prepareStatement( SQL );
			stmt.setMaxRows( maxRows );
		
			// bind parameters
			for (int i=0; sqlParams!=null && i<sqlParams.length; i++ ) {
				stmt.setObject( i+1, sqlParams[i] );
			}
		
		
			rs = stmt.executeQuery();
		
			// fetch the results
			return fetchMultiResults(rs);
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new NominaRegimenFiscalDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(rs);
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

}
