/*
 * This source file was generated by FireStorm/DAO.
 * 
 * If you purchase a full license for FireStorm/DAO you can customize this header file.
 * 
 * For more information please visit http://www.codefutures.com/products/firestorm
 */

package com.tsp.sct.dao.jdbc;

import com.tsp.sct.dao.dao.*;
import com.tsp.sct.dao.factory.*;
import com.tsp.sct.dao.dto.*;
import com.tsp.sct.dao.exceptions.*;
import java.sql.Connection;
import java.util.Collection;
import java.sql.PreparedStatement;
import java.sql.Statement;
import java.sql.ResultSet;
import java.sql.SQLException;
import java.sql.Time;
import java.util.List;
import java.util.Iterator;
import java.util.ArrayList;

public class SgfensBancoDaoImpl extends AbstractDAO implements SgfensBancoDao
{
	/** 
	 * The factory class for this DAO has two versions of the create() method - one that
takes no arguments and one that takes a Connection argument. If the Connection version
is chosen then the connection will be stored in this attribute and will be used by all
calls to this DAO, otherwise a new Connection will be allocated for each operation.
	 */
	protected java.sql.Connection userConn;

	/** 
	 * All finder methods in this class use this SELECT constant to build their queries
	 */
	protected final String SQL_SELECT = "SELECT ID_BANCO, ID_EMPRESA, ID_ESTATUS, NOMBRE_BANCO, NUMERO_SUCURSAL, NUMERO_CUENTA, CLABE, COMENTARIOS FROM " + getTableName() + "";

	/** 
	 * Finder methods will pass this value to the JDBC setMaxRows method
	 */
	protected int maxRows;

	/** 
	 * SQL INSERT statement for this table
	 */
	protected final String SQL_INSERT = "INSERT INTO " + getTableName() + " ( ID_BANCO, ID_EMPRESA, ID_ESTATUS, NOMBRE_BANCO, NUMERO_SUCURSAL, NUMERO_CUENTA, CLABE, COMENTARIOS ) VALUES ( ?, ?, ?, ?, ?, ?, ?, ? )";

	/** 
	 * SQL UPDATE statement for this table
	 */
	protected final String SQL_UPDATE = "UPDATE " + getTableName() + " SET ID_BANCO = ?, ID_EMPRESA = ?, ID_ESTATUS = ?, NOMBRE_BANCO = ?, NUMERO_SUCURSAL = ?, NUMERO_CUENTA = ?, CLABE = ?, COMENTARIOS = ? WHERE ID_BANCO = ?";

	/** 
	 * SQL DELETE statement for this table
	 */
	protected final String SQL_DELETE = "DELETE FROM " + getTableName() + " WHERE ID_BANCO = ?";

	/** 
	 * Index of column ID_BANCO
	 */
	protected static final int COLUMN_ID_BANCO = 1;

	/** 
	 * Index of column ID_EMPRESA
	 */
	protected static final int COLUMN_ID_EMPRESA = 2;

	/** 
	 * Index of column ID_ESTATUS
	 */
	protected static final int COLUMN_ID_ESTATUS = 3;

	/** 
	 * Index of column NOMBRE_BANCO
	 */
	protected static final int COLUMN_NOMBRE_BANCO = 4;

	/** 
	 * Index of column NUMERO_SUCURSAL
	 */
	protected static final int COLUMN_NUMERO_SUCURSAL = 5;

	/** 
	 * Index of column NUMERO_CUENTA
	 */
	protected static final int COLUMN_NUMERO_CUENTA = 6;

	/** 
	 * Index of column CLABE
	 */
	protected static final int COLUMN_CLABE = 7;

	/** 
	 * Index of column COMENTARIOS
	 */
	protected static final int COLUMN_COMENTARIOS = 8;

	/** 
	 * Number of columns
	 */
	protected static final int NUMBER_OF_COLUMNS = 8;

	/** 
	 * Index of primary-key column ID_BANCO
	 */
	protected static final int PK_COLUMN_ID_BANCO = 1;

	/** 
	 * Inserts a new row in the sgfens_banco table.
	 */
	public SgfensBancoPk insert(SgfensBanco dto) throws SgfensBancoDaoException
	{
		long t1 = System.currentTimeMillis();
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		ResultSet rs = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			StringBuffer sql = new StringBuffer();
			StringBuffer values = new StringBuffer();
			sql.append( "INSERT INTO " + getTableName() + " (" );
			int modifiedCount = 0;
			if (dto.isIdBancoModified()) {
				if (modifiedCount>0) {
					sql.append( ", " );
					values.append( ", " );
				}
		
				sql.append( "ID_BANCO" );
				values.append( "?" );
				modifiedCount++;
			}
		
			if (dto.isIdEmpresaModified()) {
				if (modifiedCount>0) {
					sql.append( ", " );
					values.append( ", " );
				}
		
				sql.append( "ID_EMPRESA" );
				values.append( "?" );
				modifiedCount++;
			}
		
			if (dto.isIdEstatusModified()) {
				if (modifiedCount>0) {
					sql.append( ", " );
					values.append( ", " );
				}
		
				sql.append( "ID_ESTATUS" );
				values.append( "?" );
				modifiedCount++;
			}
		
			if (dto.isNombreBancoModified()) {
				if (modifiedCount>0) {
					sql.append( ", " );
					values.append( ", " );
				}
		
				sql.append( "NOMBRE_BANCO" );
				values.append( "?" );
				modifiedCount++;
			}
		
			if (dto.isNumeroSucursalModified()) {
				if (modifiedCount>0) {
					sql.append( ", " );
					values.append( ", " );
				}
		
				sql.append( "NUMERO_SUCURSAL" );
				values.append( "?" );
				modifiedCount++;
			}
		
			if (dto.isNumeroCuentaModified()) {
				if (modifiedCount>0) {
					sql.append( ", " );
					values.append( ", " );
				}
		
				sql.append( "NUMERO_CUENTA" );
				values.append( "?" );
				modifiedCount++;
			}
		
			if (dto.isClabeModified()) {
				if (modifiedCount>0) {
					sql.append( ", " );
					values.append( ", " );
				}
		
				sql.append( "CLABE" );
				values.append( "?" );
				modifiedCount++;
			}
		
			if (dto.isComentariosModified()) {
				if (modifiedCount>0) {
					sql.append( ", " );
					values.append( ", " );
				}
		
				sql.append( "COMENTARIOS" );
				values.append( "?" );
				modifiedCount++;
			}
		
			if (modifiedCount==0) {
				// nothing to insert
				throw new IllegalStateException( "Nothing to insert" );
			}
		
			sql.append( ") VALUES (" );
			sql.append( values );
			sql.append( ")" );
			stmt = conn.prepareStatement( sql.toString(), Statement.RETURN_GENERATED_KEYS );
			int index = 1;
			if (dto.isIdBancoModified()) {
				stmt.setInt( index++, dto.getIdBanco() );
			}
		
			if (dto.isIdEmpresaModified()) {
				stmt.setInt( index++, dto.getIdEmpresa() );
			}
		
			if (dto.isIdEstatusModified()) {
				if (dto.isIdEstatusNull()) {
					stmt.setNull( index++, java.sql.Types.INTEGER );
				} else {
					stmt.setInt( index++, dto.getIdEstatus() );
				}
		
			}
		
			if (dto.isNombreBancoModified()) {
				stmt.setString( index++, dto.getNombreBanco() );
			}
		
			if (dto.isNumeroSucursalModified()) {
				stmt.setString( index++, dto.getNumeroSucursal() );
			}
		
			if (dto.isNumeroCuentaModified()) {
				stmt.setString( index++, dto.getNumeroCuenta() );
			}
		
			if (dto.isClabeModified()) {
				stmt.setString( index++, dto.getClabe() );
			}
		
			if (dto.isComentariosModified()) {
				stmt.setString( index++, dto.getComentarios() );
			}
		
			System.out.println( "Executing " + sql.toString() + " with values: " + dto );
			int rows = stmt.executeUpdate();
			long t2 = System.currentTimeMillis();
			System.out.println( rows + " rows affected (" + (t2-t1) + " ms)" );
		
			// retrieve values from auto-increment columns
			rs = stmt.getGeneratedKeys();
			if (rs != null && rs.next()) {
				dto.setIdBanco( rs.getInt( 1 ) );
			}
		
			reset(dto);
			return dto.createPk();
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new SgfensBancoDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Updates a single row in the sgfens_banco table.
	 */
	public void update(SgfensBancoPk pk, SgfensBanco dto) throws SgfensBancoDaoException
	{
		long t1 = System.currentTimeMillis();
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			StringBuffer sql = new StringBuffer();
			sql.append( "UPDATE " + getTableName() + " SET " );
			boolean modified = false;
			if (dto.isIdBancoModified()) {
				if (modified) {
					sql.append( ", " );
				}
		
				sql.append( "ID_BANCO=?" );
				modified=true;
			}
		
			if (dto.isIdEmpresaModified()) {
				if (modified) {
					sql.append( ", " );
				}
		
				sql.append( "ID_EMPRESA=?" );
				modified=true;
			}
		
			if (dto.isIdEstatusModified()) {
				if (modified) {
					sql.append( ", " );
				}
		
				sql.append( "ID_ESTATUS=?" );
				modified=true;
			}
		
			if (dto.isNombreBancoModified()) {
				if (modified) {
					sql.append( ", " );
				}
		
				sql.append( "NOMBRE_BANCO=?" );
				modified=true;
			}
		
			if (dto.isNumeroSucursalModified()) {
				if (modified) {
					sql.append( ", " );
				}
		
				sql.append( "NUMERO_SUCURSAL=?" );
				modified=true;
			}
		
			if (dto.isNumeroCuentaModified()) {
				if (modified) {
					sql.append( ", " );
				}
		
				sql.append( "NUMERO_CUENTA=?" );
				modified=true;
			}
		
			if (dto.isClabeModified()) {
				if (modified) {
					sql.append( ", " );
				}
		
				sql.append( "CLABE=?" );
				modified=true;
			}
		
			if (dto.isComentariosModified()) {
				if (modified) {
					sql.append( ", " );
				}
		
				sql.append( "COMENTARIOS=?" );
				modified=true;
			}
		
			if (!modified) {
				// nothing to update
				return;
			}
		
			sql.append( " WHERE ID_BANCO=?" );
			System.out.println( "Executing " + sql.toString() + " with values: " + dto );
			stmt = conn.prepareStatement( sql.toString() );
			int index = 1;
			if (dto.isIdBancoModified()) {
				stmt.setInt( index++, dto.getIdBanco() );
			}
		
			if (dto.isIdEmpresaModified()) {
				stmt.setInt( index++, dto.getIdEmpresa() );
			}
		
			if (dto.isIdEstatusModified()) {
				if (dto.isIdEstatusNull()) {
					stmt.setNull( index++, java.sql.Types.INTEGER );
				} else {
					stmt.setInt( index++, dto.getIdEstatus() );
				}
		
			}
		
			if (dto.isNombreBancoModified()) {
				stmt.setString( index++, dto.getNombreBanco() );
			}
		
			if (dto.isNumeroSucursalModified()) {
				stmt.setString( index++, dto.getNumeroSucursal() );
			}
		
			if (dto.isNumeroCuentaModified()) {
				stmt.setString( index++, dto.getNumeroCuenta() );
			}
		
			if (dto.isClabeModified()) {
				stmt.setString( index++, dto.getClabe() );
			}
		
			if (dto.isComentariosModified()) {
				stmt.setString( index++, dto.getComentarios() );
			}
		
			stmt.setInt( index++, pk.getIdBanco() );
			int rows = stmt.executeUpdate();
			reset(dto);
			long t2 = System.currentTimeMillis();
			System.out.println( rows + " rows affected (" + (t2-t1) + " ms)" );
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new SgfensBancoDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Deletes a single row in the sgfens_banco table.
	 */
	public void delete(SgfensBancoPk pk) throws SgfensBancoDaoException
	{
		long t1 = System.currentTimeMillis();
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			System.out.println( "Executing " + SQL_DELETE + " with PK: " + pk );
			stmt = conn.prepareStatement( SQL_DELETE );
			stmt.setInt( 1, pk.getIdBanco() );
			int rows = stmt.executeUpdate();
			long t2 = System.currentTimeMillis();
			System.out.println( rows + " rows affected (" + (t2-t1) + " ms)" );
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new SgfensBancoDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Returns the rows from the sgfens_banco table that matches the specified primary-key value.
	 */
	public SgfensBanco findByPrimaryKey(SgfensBancoPk pk) throws SgfensBancoDaoException
	{
		return findByPrimaryKey( pk.getIdBanco() );
	}

	/** 
	 * Returns all rows from the sgfens_banco table that match the criteria 'ID_BANCO = :idBanco'.
	 */
	public SgfensBanco findByPrimaryKey(int idBanco) throws SgfensBancoDaoException
	{
		SgfensBanco ret[] = findByDynamicSelect( SQL_SELECT + " WHERE ID_BANCO = ?", new Object[] {  new Integer(idBanco) } );
		return ret.length==0 ? null : ret[0];
	}

	/** 
	 * Returns all rows from the sgfens_banco table that match the criteria ''.
	 */
	public SgfensBanco[] findAll() throws SgfensBancoDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " ORDER BY ID_BANCO", null );
	}

	/** 
	 * Returns all rows from the sgfens_banco table that match the criteria 'ID_BANCO = :idBanco'.
	 */
	public SgfensBanco[] findWhereIdBancoEquals(int idBanco) throws SgfensBancoDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE ID_BANCO = ? ORDER BY ID_BANCO", new Object[] {  new Integer(idBanco) } );
	}

	/** 
	 * Returns all rows from the sgfens_banco table that match the criteria 'ID_EMPRESA = :idEmpresa'.
	 */
	public SgfensBanco[] findWhereIdEmpresaEquals(int idEmpresa) throws SgfensBancoDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE ID_EMPRESA = ? ORDER BY ID_EMPRESA", new Object[] {  new Integer(idEmpresa) } );
	}

	/** 
	 * Returns all rows from the sgfens_banco table that match the criteria 'ID_ESTATUS = :idEstatus'.
	 */
	public SgfensBanco[] findWhereIdEstatusEquals(int idEstatus) throws SgfensBancoDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE ID_ESTATUS = ? ORDER BY ID_ESTATUS", new Object[] {  new Integer(idEstatus) } );
	}

	/** 
	 * Returns all rows from the sgfens_banco table that match the criteria 'NOMBRE_BANCO = :nombreBanco'.
	 */
	public SgfensBanco[] findWhereNombreBancoEquals(String nombreBanco) throws SgfensBancoDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE NOMBRE_BANCO = ? ORDER BY NOMBRE_BANCO", new Object[] { nombreBanco } );
	}

	/** 
	 * Returns all rows from the sgfens_banco table that match the criteria 'NUMERO_SUCURSAL = :numeroSucursal'.
	 */
	public SgfensBanco[] findWhereNumeroSucursalEquals(String numeroSucursal) throws SgfensBancoDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE NUMERO_SUCURSAL = ? ORDER BY NUMERO_SUCURSAL", new Object[] { numeroSucursal } );
	}

	/** 
	 * Returns all rows from the sgfens_banco table that match the criteria 'NUMERO_CUENTA = :numeroCuenta'.
	 */
	public SgfensBanco[] findWhereNumeroCuentaEquals(String numeroCuenta) throws SgfensBancoDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE NUMERO_CUENTA = ? ORDER BY NUMERO_CUENTA", new Object[] { numeroCuenta } );
	}

	/** 
	 * Returns all rows from the sgfens_banco table that match the criteria 'CLABE = :clabe'.
	 */
	public SgfensBanco[] findWhereClabeEquals(String clabe) throws SgfensBancoDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE CLABE = ? ORDER BY CLABE", new Object[] { clabe } );
	}

	/** 
	 * Returns all rows from the sgfens_banco table that match the criteria 'COMENTARIOS = :comentarios'.
	 */
	public SgfensBanco[] findWhereComentariosEquals(String comentarios) throws SgfensBancoDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE COMENTARIOS = ? ORDER BY COMENTARIOS", new Object[] { comentarios } );
	}

	/**
	 * Method 'SgfensBancoDaoImpl'
	 * 
	 */
	public SgfensBancoDaoImpl()
	{
	}

	/**
	 * Method 'SgfensBancoDaoImpl'
	 * 
	 * @param userConn
	 */
	public SgfensBancoDaoImpl(final java.sql.Connection userConn)
	{
		this.userConn = userConn;
	}

	/** 
	 * Sets the value of maxRows
	 */
	public void setMaxRows(int maxRows)
	{
		this.maxRows = maxRows;
	}

	/** 
	 * Gets the value of maxRows
	 */
	public int getMaxRows()
	{
		return maxRows;
	}

	/**
	 * Method 'getTableName'
	 * 
	 * @return String
	 */
	public String getTableName()
	{
		return "sgfens_banco";
	}

	/** 
	 * Fetches a single row from the result set
	 */
	protected SgfensBanco fetchSingleResult(ResultSet rs) throws SQLException
	{
		if (rs.next()) {
			SgfensBanco dto = new SgfensBanco();
			populateDto( dto, rs);
			return dto;
		} else {
			return null;
		}
		
	}

	/** 
	 * Fetches multiple rows from the result set
	 */
	protected SgfensBanco[] fetchMultiResults(ResultSet rs) throws SQLException
	{
		Collection resultList = new ArrayList();
		while (rs.next()) {
			SgfensBanco dto = new SgfensBanco();
			populateDto( dto, rs);
			resultList.add( dto );
		}
		
		SgfensBanco ret[] = new SgfensBanco[ resultList.size() ];
		resultList.toArray( ret );
		return ret;
	}

	/** 
	 * Populates a DTO with data from a ResultSet
	 */
	protected void populateDto(SgfensBanco dto, ResultSet rs) throws SQLException
	{
		dto.setIdBanco( rs.getInt( COLUMN_ID_BANCO ) );
		dto.setIdEmpresa( rs.getInt( COLUMN_ID_EMPRESA ) );
		dto.setIdEstatus( rs.getInt( COLUMN_ID_ESTATUS ) );
		if (rs.wasNull()) {
			dto.setIdEstatusNull( true );
		}
		
		dto.setNombreBanco( rs.getString( COLUMN_NOMBRE_BANCO ) );
		dto.setNumeroSucursal( rs.getString( COLUMN_NUMERO_SUCURSAL ) );
		dto.setNumeroCuenta( rs.getString( COLUMN_NUMERO_CUENTA ) );
		dto.setClabe( rs.getString( COLUMN_CLABE ) );
		dto.setComentarios( rs.getString( COLUMN_COMENTARIOS ) );
		reset(dto);
	}

	/** 
	 * Resets the modified attributes in the DTO
	 */
	protected void reset(SgfensBanco dto)
	{
		dto.setIdBancoModified( false );
		dto.setIdEmpresaModified( false );
		dto.setIdEstatusModified( false );
		dto.setNombreBancoModified( false );
		dto.setNumeroSucursalModified( false );
		dto.setNumeroCuentaModified( false );
		dto.setClabeModified( false );
		dto.setComentariosModified( false );
	}

	/** 
	 * Returns all rows from the sgfens_banco table that match the specified arbitrary SQL statement
	 */
	public SgfensBanco[] findByDynamicSelect(String sql, Object[] sqlParams) throws SgfensBancoDaoException
	{
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		ResultSet rs = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			// construct the SQL statement
			final String SQL = sql;
		
		
			System.out.println( "Executing " + SQL );
			// prepare statement
			stmt = conn.prepareStatement( SQL );
			stmt.setMaxRows( maxRows );
		
			// bind parameters
			for (int i=0; sqlParams!=null && i<sqlParams.length; i++ ) {
				stmt.setObject( i+1, sqlParams[i] );
			}
		
		
			rs = stmt.executeQuery();
		
			// fetch the results
			return fetchMultiResults(rs);
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new SgfensBancoDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(rs);
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Returns all rows from the sgfens_banco table that match the specified arbitrary SQL statement
	 */
	public SgfensBanco[] findByDynamicWhere(String sql, Object[] sqlParams) throws SgfensBancoDaoException
	{
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		ResultSet rs = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			// construct the SQL statement
			final String SQL = SQL_SELECT + " WHERE " + sql;
		
		
			System.out.println( "Executing " + SQL );
			// prepare statement
			stmt = conn.prepareStatement( SQL );
			stmt.setMaxRows( maxRows );
		
			// bind parameters
			for (int i=0; sqlParams!=null && i<sqlParams.length; i++ ) {
				stmt.setObject( i+1, sqlParams[i] );
			}
		
		
			rs = stmt.executeQuery();
		
			// fetch the results
			return fetchMultiResults(rs);
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new SgfensBancoDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(rs);
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

}
