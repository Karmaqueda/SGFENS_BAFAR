/*
 * This source file was generated by FireStorm/DAO.
 * 
 * If you purchase a full license for FireStorm/DAO you can customize this header file.
 * 
 * For more information please visit http://www.codefutures.com/products/firestorm
 */

package com.tsp.sct.dao.jdbc;

import com.tsp.sct.dao.dao.*;
import com.tsp.sct.dao.factory.*;
import java.util.Date;
import com.tsp.sct.dao.dto.*;
import com.tsp.sct.dao.exceptions.*;
import java.sql.Connection;
import java.util.Collection;
import java.sql.PreparedStatement;
import java.sql.Statement;
import java.sql.ResultSet;
import java.sql.SQLException;
import java.sql.Time;
import java.util.List;
import java.util.Iterator;
import java.util.ArrayList;

public class MetasVentaDaoImpl extends AbstractDAO implements MetasVentaDao
{
	/** 
	 * The factory class for this DAO has two versions of the create() method - one that
takes no arguments and one that takes a Connection argument. If the Connection version
is chosen then the connection will be stored in this attribute and will be used by all
calls to this DAO, otherwise a new Connection will be allocated for each operation.
	 */
	protected java.sql.Connection userConn;

	/** 
	 * All finder methods in this class use this SELECT constant to build their queries
	 */
	protected final String SQL_SELECT = "SELECT ID_META_VENTA, ID_EMPRESA, FECHA_CREACION, NOMBRE_META, ID_COMPONENTE_META, PERIODO, DURACION, ID_ESTATUS FROM " + getTableName() + "";

	/** 
	 * Finder methods will pass this value to the JDBC setMaxRows method
	 */
	protected int maxRows;

	/** 
	 * SQL INSERT statement for this table
	 */
	protected final String SQL_INSERT = "INSERT INTO " + getTableName() + " ( ID_META_VENTA, ID_EMPRESA, FECHA_CREACION, NOMBRE_META, ID_COMPONENTE_META, PERIODO, DURACION, ID_ESTATUS ) VALUES ( ?, ?, ?, ?, ?, ?, ?, ? )";

	/** 
	 * SQL UPDATE statement for this table
	 */
	protected final String SQL_UPDATE = "UPDATE " + getTableName() + " SET ID_META_VENTA = ?, ID_EMPRESA = ?, FECHA_CREACION = ?, NOMBRE_META = ?, ID_COMPONENTE_META = ?, PERIODO = ?, DURACION = ?, ID_ESTATUS = ? WHERE ID_META_VENTA = ?";

	/** 
	 * SQL DELETE statement for this table
	 */
	protected final String SQL_DELETE = "DELETE FROM " + getTableName() + " WHERE ID_META_VENTA = ?";

	/** 
	 * Index of column ID_META_VENTA
	 */
	protected static final int COLUMN_ID_META_VENTA = 1;

	/** 
	 * Index of column ID_EMPRESA
	 */
	protected static final int COLUMN_ID_EMPRESA = 2;

	/** 
	 * Index of column FECHA_CREACION
	 */
	protected static final int COLUMN_FECHA_CREACION = 3;

	/** 
	 * Index of column NOMBRE_META
	 */
	protected static final int COLUMN_NOMBRE_META = 4;

	/** 
	 * Index of column ID_COMPONENTE_META
	 */
	protected static final int COLUMN_ID_COMPONENTE_META = 5;

	/** 
	 * Index of column PERIODO
	 */
	protected static final int COLUMN_PERIODO = 6;

	/** 
	 * Index of column DURACION
	 */
	protected static final int COLUMN_DURACION = 7;

	/** 
	 * Index of column ID_ESTATUS
	 */
	protected static final int COLUMN_ID_ESTATUS = 8;

	/** 
	 * Number of columns
	 */
	protected static final int NUMBER_OF_COLUMNS = 8;

	/** 
	 * Index of primary-key column ID_META_VENTA
	 */
	protected static final int PK_COLUMN_ID_META_VENTA = 1;

	/** 
	 * Inserts a new row in the metas_venta table.
	 */
	public MetasVentaPk insert(MetasVenta dto) throws MetasVentaDaoException
	{
		long t1 = System.currentTimeMillis();
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		ResultSet rs = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			StringBuffer sql = new StringBuffer();
			StringBuffer values = new StringBuffer();
			sql.append( "INSERT INTO " + getTableName() + " (" );
			int modifiedCount = 0;
			if (dto.isIdMetaVentaModified()) {
				if (modifiedCount>0) {
					sql.append( ", " );
					values.append( ", " );
				}
		
				sql.append( "ID_META_VENTA" );
				values.append( "?" );
				modifiedCount++;
			}
		
			if (dto.isIdEmpresaModified()) {
				if (modifiedCount>0) {
					sql.append( ", " );
					values.append( ", " );
				}
		
				sql.append( "ID_EMPRESA" );
				values.append( "?" );
				modifiedCount++;
			}
		
			if (dto.isFechaCreacionModified()) {
				if (modifiedCount>0) {
					sql.append( ", " );
					values.append( ", " );
				}
		
				sql.append( "FECHA_CREACION" );
				values.append( "?" );
				modifiedCount++;
			}
		
			if (dto.isNombreMetaModified()) {
				if (modifiedCount>0) {
					sql.append( ", " );
					values.append( ", " );
				}
		
				sql.append( "NOMBRE_META" );
				values.append( "?" );
				modifiedCount++;
			}
		
			if (dto.isIdComponenteMetaModified()) {
				if (modifiedCount>0) {
					sql.append( ", " );
					values.append( ", " );
				}
		
				sql.append( "ID_COMPONENTE_META" );
				values.append( "?" );
				modifiedCount++;
			}
		
			if (dto.isPeriodoModified()) {
				if (modifiedCount>0) {
					sql.append( ", " );
					values.append( ", " );
				}
		
				sql.append( "PERIODO" );
				values.append( "?" );
				modifiedCount++;
			}
		
			if (dto.isDuracionModified()) {
				if (modifiedCount>0) {
					sql.append( ", " );
					values.append( ", " );
				}
		
				sql.append( "DURACION" );
				values.append( "?" );
				modifiedCount++;
			}
		
			if (dto.isIdEstatusModified()) {
				if (modifiedCount>0) {
					sql.append( ", " );
					values.append( ", " );
				}
		
				sql.append( "ID_ESTATUS" );
				values.append( "?" );
				modifiedCount++;
			}
		
			if (modifiedCount==0) {
				// nothing to insert
				throw new IllegalStateException( "Nothing to insert" );
			}
		
			sql.append( ") VALUES (" );
			sql.append( values );
			sql.append( ")" );
			stmt = conn.prepareStatement( sql.toString(), Statement.RETURN_GENERATED_KEYS );
			int index = 1;
			if (dto.isIdMetaVentaModified()) {
				stmt.setInt( index++, dto.getIdMetaVenta() );
			}
		
			if (dto.isIdEmpresaModified()) {
				stmt.setInt( index++, dto.getIdEmpresa() );
			}
		
			if (dto.isFechaCreacionModified()) {
				stmt.setTimestamp(index++, dto.getFechaCreacion()==null ? null : new java.sql.Timestamp( dto.getFechaCreacion().getTime() ) );
			}
		
			if (dto.isNombreMetaModified()) {
				stmt.setString( index++, dto.getNombreMeta() );
			}
		
			if (dto.isIdComponenteMetaModified()) {
				stmt.setInt( index++, dto.getIdComponenteMeta() );
			}
		
			if (dto.isPeriodoModified()) {
				if (dto.isPeriodoNull()) {
					stmt.setNull( index++, java.sql.Types.INTEGER );
				} else {
					stmt.setInt( index++, dto.getPeriodo() );
				}
		
			}
		
			if (dto.isDuracionModified()) {
				if (dto.isDuracionNull()) {
					stmt.setNull( index++, java.sql.Types.INTEGER );
				} else {
					stmt.setInt( index++, dto.getDuracion() );
				}
		
			}
		
			if (dto.isIdEstatusModified()) {
				if (dto.isIdEstatusNull()) {
					stmt.setNull( index++, java.sql.Types.INTEGER );
				} else {
					stmt.setInt( index++, dto.getIdEstatus() );
				}
		
			}
		
			System.out.println( "Executing " + sql.toString() + " with values: " + dto );
			int rows = stmt.executeUpdate();
			long t2 = System.currentTimeMillis();
			System.out.println( rows + " rows affected (" + (t2-t1) + " ms)" );
		
			// retrieve values from auto-increment columns
			rs = stmt.getGeneratedKeys();
			if (rs != null && rs.next()) {
				dto.setIdMetaVenta( rs.getInt( 1 ) );
			}
		
			reset(dto);
			return dto.createPk();
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new MetasVentaDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Updates a single row in the metas_venta table.
	 */
	public void update(MetasVentaPk pk, MetasVenta dto) throws MetasVentaDaoException
	{
		long t1 = System.currentTimeMillis();
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			StringBuffer sql = new StringBuffer();
			sql.append( "UPDATE " + getTableName() + " SET " );
			boolean modified = false;
			if (dto.isIdMetaVentaModified()) {
				if (modified) {
					sql.append( ", " );
				}
		
				sql.append( "ID_META_VENTA=?" );
				modified=true;
			}
		
			if (dto.isIdEmpresaModified()) {
				if (modified) {
					sql.append( ", " );
				}
		
				sql.append( "ID_EMPRESA=?" );
				modified=true;
			}
		
			if (dto.isFechaCreacionModified()) {
				if (modified) {
					sql.append( ", " );
				}
		
				sql.append( "FECHA_CREACION=?" );
				modified=true;
			}
		
			if (dto.isNombreMetaModified()) {
				if (modified) {
					sql.append( ", " );
				}
		
				sql.append( "NOMBRE_META=?" );
				modified=true;
			}
		
			if (dto.isIdComponenteMetaModified()) {
				if (modified) {
					sql.append( ", " );
				}
		
				sql.append( "ID_COMPONENTE_META=?" );
				modified=true;
			}
		
			if (dto.isPeriodoModified()) {
				if (modified) {
					sql.append( ", " );
				}
		
				sql.append( "PERIODO=?" );
				modified=true;
			}
		
			if (dto.isDuracionModified()) {
				if (modified) {
					sql.append( ", " );
				}
		
				sql.append( "DURACION=?" );
				modified=true;
			}
		
			if (dto.isIdEstatusModified()) {
				if (modified) {
					sql.append( ", " );
				}
		
				sql.append( "ID_ESTATUS=?" );
				modified=true;
			}
		
			if (!modified) {
				// nothing to update
				return;
			}
		
			sql.append( " WHERE ID_META_VENTA=?" );
			System.out.println( "Executing " + sql.toString() + " with values: " + dto );
			stmt = conn.prepareStatement( sql.toString() );
			int index = 1;
			if (dto.isIdMetaVentaModified()) {
				stmt.setInt( index++, dto.getIdMetaVenta() );
			}
		
			if (dto.isIdEmpresaModified()) {
				stmt.setInt( index++, dto.getIdEmpresa() );
			}
		
			if (dto.isFechaCreacionModified()) {
				stmt.setTimestamp(index++, dto.getFechaCreacion()==null ? null : new java.sql.Timestamp( dto.getFechaCreacion().getTime() ) );
			}
		
			if (dto.isNombreMetaModified()) {
				stmt.setString( index++, dto.getNombreMeta() );
			}
		
			if (dto.isIdComponenteMetaModified()) {
				stmt.setInt( index++, dto.getIdComponenteMeta() );
			}
		
			if (dto.isPeriodoModified()) {
				if (dto.isPeriodoNull()) {
					stmt.setNull( index++, java.sql.Types.INTEGER );
				} else {
					stmt.setInt( index++, dto.getPeriodo() );
				}
		
			}
		
			if (dto.isDuracionModified()) {
				if (dto.isDuracionNull()) {
					stmt.setNull( index++, java.sql.Types.INTEGER );
				} else {
					stmt.setInt( index++, dto.getDuracion() );
				}
		
			}
		
			if (dto.isIdEstatusModified()) {
				if (dto.isIdEstatusNull()) {
					stmt.setNull( index++, java.sql.Types.INTEGER );
				} else {
					stmt.setInt( index++, dto.getIdEstatus() );
				}
		
			}
		
			stmt.setInt( index++, pk.getIdMetaVenta() );
			int rows = stmt.executeUpdate();
			reset(dto);
			long t2 = System.currentTimeMillis();
			System.out.println( rows + " rows affected (" + (t2-t1) + " ms)" );
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new MetasVentaDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Deletes a single row in the metas_venta table.
	 */
	public void delete(MetasVentaPk pk) throws MetasVentaDaoException
	{
		long t1 = System.currentTimeMillis();
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			System.out.println( "Executing " + SQL_DELETE + " with PK: " + pk );
			stmt = conn.prepareStatement( SQL_DELETE );
			stmt.setInt( 1, pk.getIdMetaVenta() );
			int rows = stmt.executeUpdate();
			long t2 = System.currentTimeMillis();
			System.out.println( rows + " rows affected (" + (t2-t1) + " ms)" );
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new MetasVentaDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Returns the rows from the metas_venta table that matches the specified primary-key value.
	 */
	public MetasVenta findByPrimaryKey(MetasVentaPk pk) throws MetasVentaDaoException
	{
		return findByPrimaryKey( pk.getIdMetaVenta() );
	}

	/** 
	 * Returns all rows from the metas_venta table that match the criteria 'ID_META_VENTA = :idMetaVenta'.
	 */
	public MetasVenta findByPrimaryKey(int idMetaVenta) throws MetasVentaDaoException
	{
		MetasVenta ret[] = findByDynamicSelect( SQL_SELECT + " WHERE ID_META_VENTA = ?", new Object[] {  new Integer(idMetaVenta) } );
		return ret.length==0 ? null : ret[0];
	}

	/** 
	 * Returns all rows from the metas_venta table that match the criteria ''.
	 */
	public MetasVenta[] findAll() throws MetasVentaDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " ORDER BY ID_META_VENTA", null );
	}

	/** 
	 * Returns all rows from the metas_venta table that match the criteria 'ID_META_VENTA = :idMetaVenta'.
	 */
	public MetasVenta[] findWhereIdMetaVentaEquals(int idMetaVenta) throws MetasVentaDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE ID_META_VENTA = ? ORDER BY ID_META_VENTA", new Object[] {  new Integer(idMetaVenta) } );
	}

	/** 
	 * Returns all rows from the metas_venta table that match the criteria 'ID_EMPRESA = :idEmpresa'.
	 */
	public MetasVenta[] findWhereIdEmpresaEquals(int idEmpresa) throws MetasVentaDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE ID_EMPRESA = ? ORDER BY ID_EMPRESA", new Object[] {  new Integer(idEmpresa) } );
	}

	/** 
	 * Returns all rows from the metas_venta table that match the criteria 'FECHA_CREACION = :fechaCreacion'.
	 */
	public MetasVenta[] findWhereFechaCreacionEquals(Date fechaCreacion) throws MetasVentaDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE FECHA_CREACION = ? ORDER BY FECHA_CREACION", new Object[] { fechaCreacion==null ? null : new java.sql.Timestamp( fechaCreacion.getTime() ) } );
	}

	/** 
	 * Returns all rows from the metas_venta table that match the criteria 'NOMBRE_META = :nombreMeta'.
	 */
	public MetasVenta[] findWhereNombreMetaEquals(String nombreMeta) throws MetasVentaDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE NOMBRE_META = ? ORDER BY NOMBRE_META", new Object[] { nombreMeta } );
	}

	/** 
	 * Returns all rows from the metas_venta table that match the criteria 'ID_COMPONENTE_META = :idComponenteMeta'.
	 */
	public MetasVenta[] findWhereIdComponenteMetaEquals(int idComponenteMeta) throws MetasVentaDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE ID_COMPONENTE_META = ? ORDER BY ID_COMPONENTE_META", new Object[] {  new Integer(idComponenteMeta) } );
	}

	/** 
	 * Returns all rows from the metas_venta table that match the criteria 'PERIODO = :periodo'.
	 */
	public MetasVenta[] findWherePeriodoEquals(int periodo) throws MetasVentaDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE PERIODO = ? ORDER BY PERIODO", new Object[] {  new Integer(periodo) } );
	}

	/** 
	 * Returns all rows from the metas_venta table that match the criteria 'DURACION = :duracion'.
	 */
	public MetasVenta[] findWhereDuracionEquals(int duracion) throws MetasVentaDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE DURACION = ? ORDER BY DURACION", new Object[] {  new Integer(duracion) } );
	}

	/** 
	 * Returns all rows from the metas_venta table that match the criteria 'ID_ESTATUS = :idEstatus'.
	 */
	public MetasVenta[] findWhereIdEstatusEquals(int idEstatus) throws MetasVentaDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE ID_ESTATUS = ? ORDER BY ID_ESTATUS", new Object[] {  new Integer(idEstatus) } );
	}

	/**
	 * Method 'MetasVentaDaoImpl'
	 * 
	 */
	public MetasVentaDaoImpl()
	{
	}

	/**
	 * Method 'MetasVentaDaoImpl'
	 * 
	 * @param userConn
	 */
	public MetasVentaDaoImpl(final java.sql.Connection userConn)
	{
		this.userConn = userConn;
	}

	/** 
	 * Sets the value of maxRows
	 */
	public void setMaxRows(int maxRows)
	{
		this.maxRows = maxRows;
	}

	/** 
	 * Gets the value of maxRows
	 */
	public int getMaxRows()
	{
		return maxRows;
	}

	/**
	 * Method 'getTableName'
	 * 
	 * @return String
	 */
	public String getTableName()
	{
		return "metas_venta";
	}

	/** 
	 * Fetches a single row from the result set
	 */
	protected MetasVenta fetchSingleResult(ResultSet rs) throws SQLException
	{
		if (rs.next()) {
			MetasVenta dto = new MetasVenta();
			populateDto( dto, rs);
			return dto;
		} else {
			return null;
		}
		
	}

	/** 
	 * Fetches multiple rows from the result set
	 */
	protected MetasVenta[] fetchMultiResults(ResultSet rs) throws SQLException
	{
		Collection resultList = new ArrayList();
		while (rs.next()) {
			MetasVenta dto = new MetasVenta();
			populateDto( dto, rs);
			resultList.add( dto );
		}
		
		MetasVenta ret[] = new MetasVenta[ resultList.size() ];
		resultList.toArray( ret );
		return ret;
	}

	/** 
	 * Populates a DTO with data from a ResultSet
	 */
	protected void populateDto(MetasVenta dto, ResultSet rs) throws SQLException
	{
		dto.setIdMetaVenta( rs.getInt( COLUMN_ID_META_VENTA ) );
		dto.setIdEmpresa( rs.getInt( COLUMN_ID_EMPRESA ) );
		dto.setFechaCreacion( rs.getTimestamp(COLUMN_FECHA_CREACION ) );
		dto.setNombreMeta( rs.getString( COLUMN_NOMBRE_META ) );
		dto.setIdComponenteMeta( rs.getInt( COLUMN_ID_COMPONENTE_META ) );
		dto.setPeriodo( rs.getInt( COLUMN_PERIODO ) );
		if (rs.wasNull()) {
			dto.setPeriodoNull( true );
		}
		
		dto.setDuracion( rs.getInt( COLUMN_DURACION ) );
		if (rs.wasNull()) {
			dto.setDuracionNull( true );
		}
		
		dto.setIdEstatus( rs.getInt( COLUMN_ID_ESTATUS ) );
		if (rs.wasNull()) {
			dto.setIdEstatusNull( true );
		}
		
		reset(dto);
	}

	/** 
	 * Resets the modified attributes in the DTO
	 */
	protected void reset(MetasVenta dto)
	{
		dto.setIdMetaVentaModified( false );
		dto.setIdEmpresaModified( false );
		dto.setFechaCreacionModified( false );
		dto.setNombreMetaModified( false );
		dto.setIdComponenteMetaModified( false );
		dto.setPeriodoModified( false );
		dto.setDuracionModified( false );
		dto.setIdEstatusModified( false );
	}

	/** 
	 * Returns all rows from the metas_venta table that match the specified arbitrary SQL statement
	 */
	public MetasVenta[] findByDynamicSelect(String sql, Object[] sqlParams) throws MetasVentaDaoException
	{
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		ResultSet rs = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			// construct the SQL statement
			final String SQL = sql;
		
		
			System.out.println( "Executing " + SQL );
			// prepare statement
			stmt = conn.prepareStatement( SQL );
			stmt.setMaxRows( maxRows );
		
			// bind parameters
			for (int i=0; sqlParams!=null && i<sqlParams.length; i++ ) {
				stmt.setObject( i+1, sqlParams[i] );
			}
		
		
			rs = stmt.executeQuery();
		
			// fetch the results
			return fetchMultiResults(rs);
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new MetasVentaDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(rs);
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Returns all rows from the metas_venta table that match the specified arbitrary SQL statement
	 */
	public MetasVenta[] findByDynamicWhere(String sql, Object[] sqlParams) throws MetasVentaDaoException
	{
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		ResultSet rs = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			// construct the SQL statement
			final String SQL = SQL_SELECT + " WHERE " + sql;
		
		
			System.out.println( "Executing " + SQL );
			// prepare statement
			stmt = conn.prepareStatement( SQL );
			stmt.setMaxRows( maxRows );
		
			// bind parameters
			for (int i=0; sqlParams!=null && i<sqlParams.length; i++ ) {
				stmt.setObject( i+1, sqlParams[i] );
			}
		
		
			rs = stmt.executeQuery();
		
			// fetch the results
			return fetchMultiResults(rs);
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new MetasVentaDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(rs);
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

}
