/*
 * This source file was generated by FireStorm/DAO.
 * 
 * If you purchase a full license for FireStorm/DAO you can customize this header file.
 * 
 * For more information please visit http://www.codefutures.com/products/firestorm
 */

package com.tsp.sct.dao.jdbc;

import com.tsp.sct.dao.dao.*;
import com.tsp.sct.dao.factory.*;
import com.tsp.sct.dao.dto.*;
import com.tsp.sct.dao.exceptions.*;
import java.sql.Connection;
import java.util.Collection;
import java.sql.PreparedStatement;
import java.sql.Statement;
import java.sql.ResultSet;
import java.sql.SQLException;
import java.sql.Time;
import java.util.List;
import java.util.Iterator;
import java.util.ArrayList;

public class AddendaDaoImpl extends AbstractDAO implements AddendaDao
{
	/** 
	 * The factory class for this DAO has two versions of the create() method - one that
takes no arguments and one that takes a Connection argument. If the Connection version
is chosen then the connection will be stored in this attribute and will be used by all
calls to this DAO, otherwise a new Connection will be allocated for each operation.
	 */
	protected java.sql.Connection userConn;

	/** 
	 * All finder methods in this class use this SELECT constant to build their queries
	 */
	protected final String SQL_SELECT = "SELECT ID_ADDENDA, ID_FACTURA FROM " + getTableName() + "";

	/** 
	 * Finder methods will pass this value to the JDBC setMaxRows method
	 */
	protected int maxRows;

	/** 
	 * SQL INSERT statement for this table
	 */
	protected final String SQL_INSERT = "INSERT INTO " + getTableName() + " ( ID_ADDENDA, ID_FACTURA ) VALUES ( ?, ? )";

	/** 
	 * SQL UPDATE statement for this table
	 */
	protected final String SQL_UPDATE = "UPDATE " + getTableName() + " SET ID_ADDENDA = ?, ID_FACTURA = ? WHERE ID_ADDENDA = ?";

	/** 
	 * SQL DELETE statement for this table
	 */
	protected final String SQL_DELETE = "DELETE FROM " + getTableName() + " WHERE ID_ADDENDA = ?";

	/** 
	 * Index of column ID_ADDENDA
	 */
	protected static final int COLUMN_ID_ADDENDA = 1;

	/** 
	 * Index of column ID_FACTURA
	 */
	protected static final int COLUMN_ID_FACTURA = 2;

	/** 
	 * Number of columns
	 */
	protected static final int NUMBER_OF_COLUMNS = 2;

	/** 
	 * Index of primary-key column ID_ADDENDA
	 */
	protected static final int PK_COLUMN_ID_ADDENDA = 1;

	/** 
	 * Inserts a new row in the addenda table.
	 */
	public AddendaPk insert(Addenda dto) throws AddendaDaoException
	{
		long t1 = System.currentTimeMillis();
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		ResultSet rs = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			stmt = conn.prepareStatement( SQL_INSERT, Statement.RETURN_GENERATED_KEYS );
			int index = 1;
			stmt.setInt( index++, dto.getIdAddenda() );
			stmt.setInt( index++, dto.getIdFactura() );
			System.out.println( "Executing " + SQL_INSERT + " with DTO: " + dto );
			int rows = stmt.executeUpdate();
			long t2 = System.currentTimeMillis();
			System.out.println( rows + " rows affected (" + (t2-t1) + " ms)" );
		
			// retrieve values from auto-increment columns
			rs = stmt.getGeneratedKeys();
			if (rs != null && rs.next()) {
				dto.setIdAddenda( rs.getInt( 1 ) );
			}
		
			reset(dto);
			return dto.createPk();
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new AddendaDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Updates a single row in the addenda table.
	 */
	public void update(AddendaPk pk, Addenda dto) throws AddendaDaoException
	{
		long t1 = System.currentTimeMillis();
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			System.out.println( "Executing " + SQL_UPDATE + " with DTO: " + dto );
			stmt = conn.prepareStatement( SQL_UPDATE );
			int index=1;
			stmt.setInt( index++, dto.getIdAddenda() );
			stmt.setInt( index++, dto.getIdFactura() );
			stmt.setInt( 3, pk.getIdAddenda() );
			int rows = stmt.executeUpdate();
			reset(dto);
			long t2 = System.currentTimeMillis();
			System.out.println( rows + " rows affected (" + (t2-t1) + " ms)" );
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new AddendaDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Deletes a single row in the addenda table.
	 */
	public void delete(AddendaPk pk) throws AddendaDaoException
	{
		long t1 = System.currentTimeMillis();
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			System.out.println( "Executing " + SQL_DELETE + " with PK: " + pk );
			stmt = conn.prepareStatement( SQL_DELETE );
			stmt.setInt( 1, pk.getIdAddenda() );
			int rows = stmt.executeUpdate();
			long t2 = System.currentTimeMillis();
			System.out.println( rows + " rows affected (" + (t2-t1) + " ms)" );
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new AddendaDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Returns the rows from the addenda table that matches the specified primary-key value.
	 */
	public Addenda findByPrimaryKey(AddendaPk pk) throws AddendaDaoException
	{
		return findByPrimaryKey( pk.getIdAddenda() );
	}

	/** 
	 * Returns all rows from the addenda table that match the criteria 'ID_ADDENDA = :idAddenda'.
	 */
	public Addenda findByPrimaryKey(int idAddenda) throws AddendaDaoException
	{
		Addenda ret[] = findByDynamicSelect( SQL_SELECT + " WHERE ID_ADDENDA = ?", new Object[] {  new Integer(idAddenda) } );
		return ret.length==0 ? null : ret[0];
	}

	/** 
	 * Returns all rows from the addenda table that match the criteria ''.
	 */
	public Addenda[] findAll() throws AddendaDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " ORDER BY ID_ADDENDA", null );
	}

	/** 
	 * Returns all rows from the addenda table that match the criteria 'ID_ADDENDA = :idAddenda'.
	 */
	public Addenda[] findWhereIdAddendaEquals(int idAddenda) throws AddendaDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE ID_ADDENDA = ? ORDER BY ID_ADDENDA", new Object[] {  new Integer(idAddenda) } );
	}

	/** 
	 * Returns all rows from the addenda table that match the criteria 'ID_FACTURA = :idFactura'.
	 */
	public Addenda[] findWhereIdFacturaEquals(int idFactura) throws AddendaDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE ID_FACTURA = ? ORDER BY ID_FACTURA", new Object[] {  new Integer(idFactura) } );
	}

	/**
	 * Method 'AddendaDaoImpl'
	 * 
	 */
	public AddendaDaoImpl()
	{
	}

	/**
	 * Method 'AddendaDaoImpl'
	 * 
	 * @param userConn
	 */
	public AddendaDaoImpl(final java.sql.Connection userConn)
	{
		this.userConn = userConn;
	}

	/** 
	 * Sets the value of maxRows
	 */
	public void setMaxRows(int maxRows)
	{
		this.maxRows = maxRows;
	}

	/** 
	 * Gets the value of maxRows
	 */
	public int getMaxRows()
	{
		return maxRows;
	}

	/**
	 * Method 'getTableName'
	 * 
	 * @return String
	 */
	public String getTableName()
	{
		return "ADDENDA";
	}

	/** 
	 * Fetches a single row from the result set
	 */
	protected Addenda fetchSingleResult(ResultSet rs) throws SQLException
	{
		if (rs.next()) {
			Addenda dto = new Addenda();
			populateDto( dto, rs);
			return dto;
		} else {
			return null;
		}
		
	}

	/** 
	 * Fetches multiple rows from the result set
	 */
	protected Addenda[] fetchMultiResults(ResultSet rs) throws SQLException
	{
		Collection resultList = new ArrayList();
		while (rs.next()) {
			Addenda dto = new Addenda();
			populateDto( dto, rs);
			resultList.add( dto );
		}
		
		Addenda ret[] = new Addenda[ resultList.size() ];
		resultList.toArray( ret );
		return ret;
	}

	/** 
	 * Populates a DTO with data from a ResultSet
	 */
	protected void populateDto(Addenda dto, ResultSet rs) throws SQLException
	{
		dto.setIdAddenda( rs.getInt( COLUMN_ID_ADDENDA ) );
		dto.setIdFactura( rs.getInt( COLUMN_ID_FACTURA ) );
	}

	/** 
	 * Resets the modified attributes in the DTO
	 */
	protected void reset(Addenda dto)
	{
	}

	/** 
	 * Returns all rows from the addenda table that match the specified arbitrary SQL statement
	 */
	public Addenda[] findByDynamicSelect(String sql, Object[] sqlParams) throws AddendaDaoException
	{
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		ResultSet rs = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			// construct the SQL statement
			final String SQL = sql;
		
		
			System.out.println( "Executing " + SQL );
			// prepare statement
			stmt = conn.prepareStatement( SQL );
			stmt.setMaxRows( maxRows );
		
			// bind parameters
			for (int i=0; sqlParams!=null && i<sqlParams.length; i++ ) {
				stmt.setObject( i+1, sqlParams[i] );
			}
		
		
			rs = stmt.executeQuery();
		
			// fetch the results
			return fetchMultiResults(rs);
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new AddendaDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(rs);
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Returns all rows from the addenda table that match the specified arbitrary SQL statement
	 */
	public Addenda[] findByDynamicWhere(String sql, Object[] sqlParams) throws AddendaDaoException
	{
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		ResultSet rs = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			// construct the SQL statement
			final String SQL = SQL_SELECT + " WHERE " + sql;
		
		
			System.out.println( "Executing " + SQL );
			// prepare statement
			stmt = conn.prepareStatement( SQL );
			stmt.setMaxRows( maxRows );
		
			// bind parameters
			for (int i=0; sqlParams!=null && i<sqlParams.length; i++ ) {
				stmt.setObject( i+1, sqlParams[i] );
			}
		
		
			rs = stmt.executeQuery();
		
			// fetch the results
			return fetchMultiResults(rs);
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new AddendaDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(rs);
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

}
