/*
 * This source file was generated by FireStorm/DAO.
 * 
 * If you purchase a full license for FireStorm/DAO you can customize this header file.
 * 
 * For more information please visit http://www.codefutures.com/products/firestorm
 */

package com.tsp.sct.dao.jdbc;

import com.tsp.sct.dao.dao.*;
import com.tsp.sct.dao.factory.*;
import com.tsp.sct.dao.dto.*;
import com.tsp.sct.dao.exceptions.*;
import java.sql.Connection;
import java.util.Collection;
import java.sql.PreparedStatement;
import java.sql.Statement;
import java.sql.ResultSet;
import java.sql.SQLException;
import java.sql.Time;
import java.util.List;
import java.util.Iterator;
import java.util.ArrayList;

public class TipoReporteDaoImpl extends AbstractDAO implements TipoReporteDao
{
	/** 
	 * The factory class for this DAO has two versions of the create() method - one that
takes no arguments and one that takes a Connection argument. If the Connection version
is chosen then the connection will be stored in this attribute and will be used by all
calls to this DAO, otherwise a new Connection will be allocated for each operation.
	 */
	protected java.sql.Connection userConn;

	/** 
	 * All finder methods in this class use this SELECT constant to build their queries
	 */
	protected final String SQL_SELECT = "SELECT ID_REPORTE, ID_TIPO_REPORTE, ID_ESTATUS, VISIBLE, DESCRIPCION FROM " + getTableName() + "";

	/** 
	 * Finder methods will pass this value to the JDBC setMaxRows method
	 */
	protected int maxRows;

	/** 
	 * SQL INSERT statement for this table
	 */
	protected final String SQL_INSERT = "INSERT INTO " + getTableName() + " ( ID_REPORTE, ID_TIPO_REPORTE, ID_ESTATUS, VISIBLE, DESCRIPCION ) VALUES ( ?, ?, ?, ?, ? )";

	/** 
	 * SQL UPDATE statement for this table
	 */
	protected final String SQL_UPDATE = "UPDATE " + getTableName() + " SET ID_REPORTE = ?, ID_TIPO_REPORTE = ?, ID_ESTATUS = ?, VISIBLE = ?, DESCRIPCION = ? WHERE ID_REPORTE = ?";

	/** 
	 * SQL DELETE statement for this table
	 */
	protected final String SQL_DELETE = "DELETE FROM " + getTableName() + " WHERE ID_REPORTE = ?";

	/** 
	 * Index of column ID_REPORTE
	 */
	protected static final int COLUMN_ID_REPORTE = 1;

	/** 
	 * Index of column ID_TIPO_REPORTE
	 */
	protected static final int COLUMN_ID_TIPO_REPORTE = 2;

	/** 
	 * Index of column ID_ESTATUS
	 */
	protected static final int COLUMN_ID_ESTATUS = 3;

	/** 
	 * Index of column VISIBLE
	 */
	protected static final int COLUMN_VISIBLE = 4;

	/** 
	 * Index of column DESCRIPCION
	 */
	protected static final int COLUMN_DESCRIPCION = 5;

	/** 
	 * Number of columns
	 */
	protected static final int NUMBER_OF_COLUMNS = 5;

	/** 
	 * Index of primary-key column ID_REPORTE
	 */
	protected static final int PK_COLUMN_ID_REPORTE = 1;

	/** 
	 * Inserts a new row in the tipo_reporte table.
	 */
	public TipoReportePk insert(TipoReporte dto) throws TipoReporteDaoException
	{
		long t1 = System.currentTimeMillis();
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		ResultSet rs = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			StringBuffer sql = new StringBuffer();
			StringBuffer values = new StringBuffer();
			sql.append( "INSERT INTO " + getTableName() + " (" );
			int modifiedCount = 0;
			if (dto.isIdReporteModified()) {
				if (modifiedCount>0) {
					sql.append( ", " );
					values.append( ", " );
				}
		
				sql.append( "ID_REPORTE" );
				values.append( "?" );
				modifiedCount++;
			}
		
			if (dto.isIdTipoReporteModified()) {
				if (modifiedCount>0) {
					sql.append( ", " );
					values.append( ", " );
				}
		
				sql.append( "ID_TIPO_REPORTE" );
				values.append( "?" );
				modifiedCount++;
			}
		
			if (dto.isIdEstatusModified()) {
				if (modifiedCount>0) {
					sql.append( ", " );
					values.append( ", " );
				}
		
				sql.append( "ID_ESTATUS" );
				values.append( "?" );
				modifiedCount++;
			}
		
			if (dto.isVisibleModified()) {
				if (modifiedCount>0) {
					sql.append( ", " );
					values.append( ", " );
				}
		
				sql.append( "VISIBLE" );
				values.append( "?" );
				modifiedCount++;
			}
		
			if (dto.isDescripcionModified()) {
				if (modifiedCount>0) {
					sql.append( ", " );
					values.append( ", " );
				}
		
				sql.append( "DESCRIPCION" );
				values.append( "?" );
				modifiedCount++;
			}
		
			if (modifiedCount==0) {
				// nothing to insert
				throw new IllegalStateException( "Nothing to insert" );
			}
		
			sql.append( ") VALUES (" );
			sql.append( values );
			sql.append( ")" );
			stmt = conn.prepareStatement( sql.toString(), Statement.RETURN_GENERATED_KEYS );
			int index = 1;
			if (dto.isIdReporteModified()) {
				stmt.setInt( index++, dto.getIdReporte() );
			}
		
			if (dto.isIdTipoReporteModified()) {
				if (dto.isIdTipoReporteNull()) {
					stmt.setNull( index++, java.sql.Types.INTEGER );
				} else {
					stmt.setInt( index++, dto.getIdTipoReporte() );
				}
		
			}
		
			if (dto.isIdEstatusModified()) {
				if (dto.isIdEstatusNull()) {
					stmt.setNull( index++, java.sql.Types.INTEGER );
				} else {
					stmt.setInt( index++, dto.getIdEstatus() );
				}
		
			}
		
			if (dto.isVisibleModified()) {
				if (dto.isVisibleNull()) {
					stmt.setNull( index++, java.sql.Types.INTEGER );
				} else {
					stmt.setInt( index++, dto.getVisible() );
				}
		
			}
		
			if (dto.isDescripcionModified()) {
				stmt.setString( index++, dto.getDescripcion() );
			}
		
			System.out.println( "Executing " + sql.toString() + " with values: " + dto );
			int rows = stmt.executeUpdate();
			long t2 = System.currentTimeMillis();
			System.out.println( rows + " rows affected (" + (t2-t1) + " ms)" );
		
			// retrieve values from auto-increment columns
			rs = stmt.getGeneratedKeys();
			if (rs != null && rs.next()) {
				dto.setIdReporte( rs.getInt( 1 ) );
			}
		
			reset(dto);
			return dto.createPk();
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new TipoReporteDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Updates a single row in the tipo_reporte table.
	 */
	public void update(TipoReportePk pk, TipoReporte dto) throws TipoReporteDaoException
	{
		long t1 = System.currentTimeMillis();
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			StringBuffer sql = new StringBuffer();
			sql.append( "UPDATE " + getTableName() + " SET " );
			boolean modified = false;
			if (dto.isIdReporteModified()) {
				if (modified) {
					sql.append( ", " );
				}
		
				sql.append( "ID_REPORTE=?" );
				modified=true;
			}
		
			if (dto.isIdTipoReporteModified()) {
				if (modified) {
					sql.append( ", " );
				}
		
				sql.append( "ID_TIPO_REPORTE=?" );
				modified=true;
			}
		
			if (dto.isIdEstatusModified()) {
				if (modified) {
					sql.append( ", " );
				}
		
				sql.append( "ID_ESTATUS=?" );
				modified=true;
			}
		
			if (dto.isVisibleModified()) {
				if (modified) {
					sql.append( ", " );
				}
		
				sql.append( "VISIBLE=?" );
				modified=true;
			}
		
			if (dto.isDescripcionModified()) {
				if (modified) {
					sql.append( ", " );
				}
		
				sql.append( "DESCRIPCION=?" );
				modified=true;
			}
		
			if (!modified) {
				// nothing to update
				return;
			}
		
			sql.append( " WHERE ID_REPORTE=?" );
			System.out.println( "Executing " + sql.toString() + " with values: " + dto );
			stmt = conn.prepareStatement( sql.toString() );
			int index = 1;
			if (dto.isIdReporteModified()) {
				stmt.setInt( index++, dto.getIdReporte() );
			}
		
			if (dto.isIdTipoReporteModified()) {
				if (dto.isIdTipoReporteNull()) {
					stmt.setNull( index++, java.sql.Types.INTEGER );
				} else {
					stmt.setInt( index++, dto.getIdTipoReporte() );
				}
		
			}
		
			if (dto.isIdEstatusModified()) {
				if (dto.isIdEstatusNull()) {
					stmt.setNull( index++, java.sql.Types.INTEGER );
				} else {
					stmt.setInt( index++, dto.getIdEstatus() );
				}
		
			}
		
			if (dto.isVisibleModified()) {
				if (dto.isVisibleNull()) {
					stmt.setNull( index++, java.sql.Types.INTEGER );
				} else {
					stmt.setInt( index++, dto.getVisible() );
				}
		
			}
		
			if (dto.isDescripcionModified()) {
				stmt.setString( index++, dto.getDescripcion() );
			}
		
			stmt.setInt( index++, pk.getIdReporte() );
			int rows = stmt.executeUpdate();
			reset(dto);
			long t2 = System.currentTimeMillis();
			System.out.println( rows + " rows affected (" + (t2-t1) + " ms)" );
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new TipoReporteDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Deletes a single row in the tipo_reporte table.
	 */
	public void delete(TipoReportePk pk) throws TipoReporteDaoException
	{
		long t1 = System.currentTimeMillis();
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			System.out.println( "Executing " + SQL_DELETE + " with PK: " + pk );
			stmt = conn.prepareStatement( SQL_DELETE );
			stmt.setInt( 1, pk.getIdReporte() );
			int rows = stmt.executeUpdate();
			long t2 = System.currentTimeMillis();
			System.out.println( rows + " rows affected (" + (t2-t1) + " ms)" );
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new TipoReporteDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Returns the rows from the tipo_reporte table that matches the specified primary-key value.
	 */
	public TipoReporte findByPrimaryKey(TipoReportePk pk) throws TipoReporteDaoException
	{
		return findByPrimaryKey( pk.getIdReporte() );
	}

	/** 
	 * Returns all rows from the tipo_reporte table that match the criteria 'ID_REPORTE = :idReporte'.
	 */
	public TipoReporte findByPrimaryKey(int idReporte) throws TipoReporteDaoException
	{
		TipoReporte ret[] = findByDynamicSelect( SQL_SELECT + " WHERE ID_REPORTE = ?", new Object[] {  new Integer(idReporte) } );
		return ret.length==0 ? null : ret[0];
	}

	/** 
	 * Returns all rows from the tipo_reporte table that match the criteria ''.
	 */
	public TipoReporte[] findAll() throws TipoReporteDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " ORDER BY ID_REPORTE", null );
	}

	/** 
	 * Returns all rows from the tipo_reporte table that match the criteria 'ID_REPORTE = :idReporte'.
	 */
	public TipoReporte[] findWhereIdReporteEquals(int idReporte) throws TipoReporteDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE ID_REPORTE = ? ORDER BY ID_REPORTE", new Object[] {  new Integer(idReporte) } );
	}

	/** 
	 * Returns all rows from the tipo_reporte table that match the criteria 'ID_TIPO_REPORTE = :idTipoReporte'.
	 */
	public TipoReporte[] findWhereIdTipoReporteEquals(int idTipoReporte) throws TipoReporteDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE ID_TIPO_REPORTE = ? ORDER BY ID_TIPO_REPORTE", new Object[] {  new Integer(idTipoReporte) } );
	}

	/** 
	 * Returns all rows from the tipo_reporte table that match the criteria 'ID_ESTATUS = :idEstatus'.
	 */
	public TipoReporte[] findWhereIdEstatusEquals(int idEstatus) throws TipoReporteDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE ID_ESTATUS = ? ORDER BY ID_ESTATUS", new Object[] {  new Integer(idEstatus) } );
	}

	/** 
	 * Returns all rows from the tipo_reporte table that match the criteria 'VISIBLE = :visible'.
	 */
	public TipoReporte[] findWhereVisibleEquals(int visible) throws TipoReporteDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE VISIBLE = ? ORDER BY VISIBLE", new Object[] {  new Integer(visible) } );
	}

	/** 
	 * Returns all rows from the tipo_reporte table that match the criteria 'DESCRIPCION = :descripcion'.
	 */
	public TipoReporte[] findWhereDescripcionEquals(String descripcion) throws TipoReporteDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE DESCRIPCION = ? ORDER BY DESCRIPCION", new Object[] { descripcion } );
	}

	/**
	 * Method 'TipoReporteDaoImpl'
	 * 
	 */
	public TipoReporteDaoImpl()
	{
	}

	/**
	 * Method 'TipoReporteDaoImpl'
	 * 
	 * @param userConn
	 */
	public TipoReporteDaoImpl(final java.sql.Connection userConn)
	{
		this.userConn = userConn;
	}

	/** 
	 * Sets the value of maxRows
	 */
	public void setMaxRows(int maxRows)
	{
		this.maxRows = maxRows;
	}

	/** 
	 * Gets the value of maxRows
	 */
	public int getMaxRows()
	{
		return maxRows;
	}

	/**
	 * Method 'getTableName'
	 * 
	 * @return String
	 */
	public String getTableName()
	{
		return "tipo_reporte";
	}

	/** 
	 * Fetches a single row from the result set
	 */
	protected TipoReporte fetchSingleResult(ResultSet rs) throws SQLException
	{
		if (rs.next()) {
			TipoReporte dto = new TipoReporte();
			populateDto( dto, rs);
			return dto;
		} else {
			return null;
		}
		
	}

	/** 
	 * Fetches multiple rows from the result set
	 */
	protected TipoReporte[] fetchMultiResults(ResultSet rs) throws SQLException
	{
		Collection resultList = new ArrayList();
		while (rs.next()) {
			TipoReporte dto = new TipoReporte();
			populateDto( dto, rs);
			resultList.add( dto );
		}
		
		TipoReporte ret[] = new TipoReporte[ resultList.size() ];
		resultList.toArray( ret );
		return ret;
	}

	/** 
	 * Populates a DTO with data from a ResultSet
	 */
	protected void populateDto(TipoReporte dto, ResultSet rs) throws SQLException
	{
		dto.setIdReporte( rs.getInt( COLUMN_ID_REPORTE ) );
		dto.setIdTipoReporte( rs.getInt( COLUMN_ID_TIPO_REPORTE ) );
		if (rs.wasNull()) {
			dto.setIdTipoReporteNull( true );
		}
		
		dto.setIdEstatus( rs.getInt( COLUMN_ID_ESTATUS ) );
		if (rs.wasNull()) {
			dto.setIdEstatusNull( true );
		}
		
		dto.setVisible( rs.getInt( COLUMN_VISIBLE ) );
		if (rs.wasNull()) {
			dto.setVisibleNull( true );
		}
		
		dto.setDescripcion( rs.getString( COLUMN_DESCRIPCION ) );
		reset(dto);
	}

	/** 
	 * Resets the modified attributes in the DTO
	 */
	protected void reset(TipoReporte dto)
	{
		dto.setIdReporteModified( false );
		dto.setIdTipoReporteModified( false );
		dto.setIdEstatusModified( false );
		dto.setVisibleModified( false );
		dto.setDescripcionModified( false );
	}

	/** 
	 * Returns all rows from the tipo_reporte table that match the specified arbitrary SQL statement
	 */
	public TipoReporte[] findByDynamicSelect(String sql, Object[] sqlParams) throws TipoReporteDaoException
	{
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		ResultSet rs = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			// construct the SQL statement
			final String SQL = sql;
		
		
			System.out.println( "Executing " + SQL );
			// prepare statement
			stmt = conn.prepareStatement( SQL );
			stmt.setMaxRows( maxRows );
		
			// bind parameters
			for (int i=0; sqlParams!=null && i<sqlParams.length; i++ ) {
				stmt.setObject( i+1, sqlParams[i] );
			}
		
		
			rs = stmt.executeQuery();
		
			// fetch the results
			return fetchMultiResults(rs);
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new TipoReporteDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(rs);
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Returns all rows from the tipo_reporte table that match the specified arbitrary SQL statement
	 */
	public TipoReporte[] findByDynamicWhere(String sql, Object[] sqlParams) throws TipoReporteDaoException
	{
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		ResultSet rs = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			// construct the SQL statement
			final String SQL = SQL_SELECT + " WHERE " + sql;
		
		
			System.out.println( "Executing " + SQL );
			// prepare statement
			stmt = conn.prepareStatement( SQL );
			stmt.setMaxRows( maxRows );
		
			// bind parameters
			for (int i=0; sqlParams!=null && i<sqlParams.length; i++ ) {
				stmt.setObject( i+1, sqlParams[i] );
			}
		
		
			rs = stmt.executeQuery();
		
			// fetch the results
			return fetchMultiResults(rs);
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new TipoReporteDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(rs);
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

}
