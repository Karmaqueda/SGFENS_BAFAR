/*
 * This source file was generated by FireStorm/DAO.
 * 
 * If you purchase a full license for FireStorm/DAO you can customize this header file.
 * 
 * For more information please visit http://www.codefutures.com/products/firestorm
 */

package com.tsp.sct.dao.jdbc;

import com.tsp.sct.dao.dao.*;
import com.tsp.sct.dao.factory.*;
import com.tsp.sct.dao.dto.*;
import com.tsp.sct.dao.exceptions.*;
import java.sql.Connection;
import java.util.Collection;
import java.sql.PreparedStatement;
import java.sql.Statement;
import java.sql.ResultSet;
import java.sql.SQLException;
import java.sql.Time;
import java.util.List;
import java.util.Iterator;
import java.util.ArrayList;

public class ComprobanteImpuestoDaoImpl extends AbstractDAO implements ComprobanteImpuestoDao
{
	/** 
	 * The factory class for this DAO has two versions of the create() method - one that
takes no arguments and one that takes a Connection argument. If the Connection version
is chosen then the connection will be stored in this attribute and will be used by all
calls to this DAO, otherwise a new Connection will be allocated for each operation.
	 */
	protected java.sql.Connection userConn;

	/** 
	 * All finder methods in this class use this SELECT constant to build their queries
	 */
	protected final String SQL_SELECT = "SELECT ID_IMPUESTO, ID_COMPROBANTE_FISCAL, ID_COMPROBANTE_IMPUESTO, ORDEN FROM " + getTableName() + "";

	/** 
	 * Finder methods will pass this value to the JDBC setMaxRows method
	 */
	protected int maxRows;

	/** 
	 * SQL INSERT statement for this table
	 */
	protected final String SQL_INSERT = "INSERT INTO " + getTableName() + " ( ID_IMPUESTO, ID_COMPROBANTE_FISCAL, ID_COMPROBANTE_IMPUESTO, ORDEN ) VALUES ( ?, ?, ?, ? )";

	/** 
	 * SQL UPDATE statement for this table
	 */
	protected final String SQL_UPDATE = "UPDATE " + getTableName() + " SET ID_IMPUESTO = ?, ID_COMPROBANTE_FISCAL = ?, ID_COMPROBANTE_IMPUESTO = ?, ORDEN = ? WHERE ID_IMPUESTO = ? AND ID_COMPROBANTE_FISCAL = ?";

	/** 
	 * SQL DELETE statement for this table
	 */
	protected final String SQL_DELETE = "DELETE FROM " + getTableName() + " WHERE ID_IMPUESTO = ? AND ID_COMPROBANTE_FISCAL = ?";

	/** 
	 * Index of column ID_IMPUESTO
	 */
	protected static final int COLUMN_ID_IMPUESTO = 1;

	/** 
	 * Index of column ID_COMPROBANTE_FISCAL
	 */
	protected static final int COLUMN_ID_COMPROBANTE_FISCAL = 2;

	/** 
	 * Index of column ID_COMPROBANTE_IMPUESTO
	 */
	protected static final int COLUMN_ID_COMPROBANTE_IMPUESTO = 3;

	/** 
	 * Index of column ORDEN
	 */
	protected static final int COLUMN_ORDEN = 4;

	/** 
	 * Number of columns
	 */
	protected static final int NUMBER_OF_COLUMNS = 4;

	/** 
	 * Index of primary-key column ID_IMPUESTO
	 */
	protected static final int PK_COLUMN_ID_IMPUESTO = 1;

	/** 
	 * Index of primary-key column ID_COMPROBANTE_FISCAL
	 */
	protected static final int PK_COLUMN_ID_COMPROBANTE_FISCAL = 2;

	/** 
	 * Inserts a new row in the comprobante_impuesto table.
	 */
	public ComprobanteImpuestoPk insert(ComprobanteImpuesto dto) throws ComprobanteImpuestoDaoException
	{
		long t1 = System.currentTimeMillis();
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		ResultSet rs = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			StringBuffer sql = new StringBuffer();
			StringBuffer values = new StringBuffer();
			sql.append( "INSERT INTO " + getTableName() + " (" );
			int modifiedCount = 0;
			if (dto.isIdImpuestoModified()) {
				if (modifiedCount>0) {
					sql.append( ", " );
					values.append( ", " );
				}
		
				sql.append( "ID_IMPUESTO" );
				values.append( "?" );
				modifiedCount++;
			}
		
			if (dto.isIdComprobanteFiscalModified()) {
				if (modifiedCount>0) {
					sql.append( ", " );
					values.append( ", " );
				}
		
				sql.append( "ID_COMPROBANTE_FISCAL" );
				values.append( "?" );
				modifiedCount++;
			}
		
			if (dto.isIdComprobanteImpuestoModified()) {
				if (modifiedCount>0) {
					sql.append( ", " );
					values.append( ", " );
				}
		
				sql.append( "ID_COMPROBANTE_IMPUESTO" );
				values.append( "?" );
				modifiedCount++;
			}
		
			if (dto.isOrdenModified()) {
				if (modifiedCount>0) {
					sql.append( ", " );
					values.append( ", " );
				}
		
				sql.append( "ORDEN" );
				values.append( "?" );
				modifiedCount++;
			}
		
			if (modifiedCount==0) {
				// nothing to insert
				throw new IllegalStateException( "Nothing to insert" );
			}
		
			sql.append( ") VALUES (" );
			sql.append( values );
			sql.append( ")" );
			stmt = conn.prepareStatement( sql.toString() );
			int index = 1;
			if (dto.isIdImpuestoModified()) {
				stmt.setInt( index++, dto.getIdImpuesto() );
			}
		
			if (dto.isIdComprobanteFiscalModified()) {
				stmt.setInt( index++, dto.getIdComprobanteFiscal() );
			}
		
			if (dto.isIdComprobanteImpuestoModified()) {
				stmt.setInt( index++, dto.getIdComprobanteImpuesto() );
			}
		
			if (dto.isOrdenModified()) {
				stmt.setInt( index++, dto.getOrden() );
			}
		
			System.out.println( "Executing " + sql.toString() + " with values: " + dto );
			int rows = stmt.executeUpdate();
			long t2 = System.currentTimeMillis();
			System.out.println( rows + " rows affected (" + (t2-t1) + " ms)" );
			reset(dto);
			return dto.createPk();
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new ComprobanteImpuestoDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Updates a single row in the comprobante_impuesto table.
	 */
	public void update(ComprobanteImpuestoPk pk, ComprobanteImpuesto dto) throws ComprobanteImpuestoDaoException
	{
		long t1 = System.currentTimeMillis();
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			StringBuffer sql = new StringBuffer();
			sql.append( "UPDATE " + getTableName() + " SET " );
			boolean modified = false;
			if (dto.isIdImpuestoModified()) {
				if (modified) {
					sql.append( ", " );
				}
		
				sql.append( "ID_IMPUESTO=?" );
				modified=true;
			}
		
			if (dto.isIdComprobanteFiscalModified()) {
				if (modified) {
					sql.append( ", " );
				}
		
				sql.append( "ID_COMPROBANTE_FISCAL=?" );
				modified=true;
			}
		
			if (dto.isIdComprobanteImpuestoModified()) {
				if (modified) {
					sql.append( ", " );
				}
		
				sql.append( "ID_COMPROBANTE_IMPUESTO=?" );
				modified=true;
			}
		
			if (dto.isOrdenModified()) {
				if (modified) {
					sql.append( ", " );
				}
		
				sql.append( "ORDEN=?" );
				modified=true;
			}
		
			if (!modified) {
				// nothing to update
				return;
			}
		
			sql.append( " WHERE ID_IMPUESTO=? AND ID_COMPROBANTE_FISCAL=?" );
			System.out.println( "Executing " + sql.toString() + " with values: " + dto );
			stmt = conn.prepareStatement( sql.toString() );
			int index = 1;
			if (dto.isIdImpuestoModified()) {
				stmt.setInt( index++, dto.getIdImpuesto() );
			}
		
			if (dto.isIdComprobanteFiscalModified()) {
				stmt.setInt( index++, dto.getIdComprobanteFiscal() );
			}
		
			if (dto.isIdComprobanteImpuestoModified()) {
				stmt.setInt( index++, dto.getIdComprobanteImpuesto() );
			}
		
			if (dto.isOrdenModified()) {
				stmt.setInt( index++, dto.getOrden() );
			}
		
			stmt.setInt( index++, pk.getIdImpuesto() );
			stmt.setInt( index++, pk.getIdComprobanteFiscal() );
			int rows = stmt.executeUpdate();
			reset(dto);
			long t2 = System.currentTimeMillis();
			System.out.println( rows + " rows affected (" + (t2-t1) + " ms)" );
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new ComprobanteImpuestoDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Deletes a single row in the comprobante_impuesto table.
	 */
	public void delete(ComprobanteImpuestoPk pk) throws ComprobanteImpuestoDaoException
	{
		long t1 = System.currentTimeMillis();
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			System.out.println( "Executing " + SQL_DELETE + " with PK: " + pk );
			stmt = conn.prepareStatement( SQL_DELETE );
			stmt.setInt( 1, pk.getIdImpuesto() );
			stmt.setInt( 2, pk.getIdComprobanteFiscal() );
			int rows = stmt.executeUpdate();
			long t2 = System.currentTimeMillis();
			System.out.println( rows + " rows affected (" + (t2-t1) + " ms)" );
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new ComprobanteImpuestoDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Returns the rows from the comprobante_impuesto table that matches the specified primary-key value.
	 */
	public ComprobanteImpuesto findByPrimaryKey(ComprobanteImpuestoPk pk) throws ComprobanteImpuestoDaoException
	{
		return findByPrimaryKey( pk.getIdImpuesto(), pk.getIdComprobanteFiscal() );
	}

	/** 
	 * Returns all rows from the comprobante_impuesto table that match the criteria 'ID_IMPUESTO = :idImpuesto AND ID_COMPROBANTE_FISCAL = :idComprobanteFiscal'.
	 */
	public ComprobanteImpuesto findByPrimaryKey(int idImpuesto, int idComprobanteFiscal) throws ComprobanteImpuestoDaoException
	{
		ComprobanteImpuesto ret[] = findByDynamicSelect( SQL_SELECT + " WHERE ID_IMPUESTO = ? AND ID_COMPROBANTE_FISCAL = ?", new Object[] {  new Integer(idImpuesto),  new Integer(idComprobanteFiscal) } );
		return ret.length==0 ? null : ret[0];
	}

	/** 
	 * Returns all rows from the comprobante_impuesto table that match the criteria ''.
	 */
	public ComprobanteImpuesto[] findAll() throws ComprobanteImpuestoDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " ORDER BY ID_IMPUESTO, ID_COMPROBANTE_FISCAL", null );
	}

	/** 
	 * Returns all rows from the comprobante_impuesto table that match the criteria 'ID_IMPUESTO = :idImpuesto'.
	 */
	public ComprobanteImpuesto[] findWhereIdImpuestoEquals(int idImpuesto) throws ComprobanteImpuestoDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE ID_IMPUESTO = ? ORDER BY ID_IMPUESTO", new Object[] {  new Integer(idImpuesto) } );
	}

	/** 
	 * Returns all rows from the comprobante_impuesto table that match the criteria 'ID_COMPROBANTE_FISCAL = :idComprobanteFiscal'.
	 */
	public ComprobanteImpuesto[] findWhereIdComprobanteFiscalEquals(int idComprobanteFiscal) throws ComprobanteImpuestoDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE ID_COMPROBANTE_FISCAL = ? ORDER BY ID_COMPROBANTE_FISCAL", new Object[] {  new Integer(idComprobanteFiscal) } );
	}

	/** 
	 * Returns all rows from the comprobante_impuesto table that match the criteria 'ID_COMPROBANTE_IMPUESTO = :idComprobanteImpuesto'.
	 */
	public ComprobanteImpuesto[] findWhereIdComprobanteImpuestoEquals(int idComprobanteImpuesto) throws ComprobanteImpuestoDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE ID_COMPROBANTE_IMPUESTO = ? ORDER BY ID_COMPROBANTE_IMPUESTO", new Object[] {  new Integer(idComprobanteImpuesto) } );
	}

	/** 
	 * Returns all rows from the comprobante_impuesto table that match the criteria 'ORDEN = :orden'.
	 */
	public ComprobanteImpuesto[] findWhereOrdenEquals(int orden) throws ComprobanteImpuestoDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE ORDEN = ? ORDER BY ORDEN", new Object[] {  new Integer(orden) } );
	}

	/**
	 * Method 'ComprobanteImpuestoDaoImpl'
	 * 
	 */
	public ComprobanteImpuestoDaoImpl()
	{
	}

	/**
	 * Method 'ComprobanteImpuestoDaoImpl'
	 * 
	 * @param userConn
	 */
	public ComprobanteImpuestoDaoImpl(final java.sql.Connection userConn)
	{
		this.userConn = userConn;
	}

	/** 
	 * Sets the value of maxRows
	 */
	public void setMaxRows(int maxRows)
	{
		this.maxRows = maxRows;
	}

	/** 
	 * Gets the value of maxRows
	 */
	public int getMaxRows()
	{
		return maxRows;
	}

	/**
	 * Method 'getTableName'
	 * 
	 * @return String
	 */
	public String getTableName()
	{
		return "COMPROBANTE_IMPUESTO";
	}

	/** 
	 * Fetches a single row from the result set
	 */
	protected ComprobanteImpuesto fetchSingleResult(ResultSet rs) throws SQLException
	{
		if (rs.next()) {
			ComprobanteImpuesto dto = new ComprobanteImpuesto();
			populateDto( dto, rs);
			return dto;
		} else {
			return null;
		}
		
	}

	/** 
	 * Fetches multiple rows from the result set
	 */
	protected ComprobanteImpuesto[] fetchMultiResults(ResultSet rs) throws SQLException
	{
		Collection resultList = new ArrayList();
		while (rs.next()) {
			ComprobanteImpuesto dto = new ComprobanteImpuesto();
			populateDto( dto, rs);
			resultList.add( dto );
		}
		
		ComprobanteImpuesto ret[] = new ComprobanteImpuesto[ resultList.size() ];
		resultList.toArray( ret );
		return ret;
	}

	/** 
	 * Populates a DTO with data from a ResultSet
	 */
	protected void populateDto(ComprobanteImpuesto dto, ResultSet rs) throws SQLException
	{
		dto.setIdImpuesto( rs.getInt( COLUMN_ID_IMPUESTO ) );
		dto.setIdComprobanteFiscal( rs.getInt( COLUMN_ID_COMPROBANTE_FISCAL ) );
		dto.setIdComprobanteImpuesto( rs.getInt( COLUMN_ID_COMPROBANTE_IMPUESTO ) );
		dto.setOrden( rs.getInt( COLUMN_ORDEN ) );
		reset(dto);
	}

	/** 
	 * Resets the modified attributes in the DTO
	 */
	protected void reset(ComprobanteImpuesto dto)
	{
		dto.setIdImpuestoModified( false );
		dto.setIdComprobanteFiscalModified( false );
		dto.setIdComprobanteImpuestoModified( false );
		dto.setOrdenModified( false );
	}

	/** 
	 * Returns all rows from the comprobante_impuesto table that match the specified arbitrary SQL statement
	 */
	public ComprobanteImpuesto[] findByDynamicSelect(String sql, Object[] sqlParams) throws ComprobanteImpuestoDaoException
	{
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		ResultSet rs = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			// construct the SQL statement
			final String SQL = sql;
		
		
			System.out.println( "Executing " + SQL );
			// prepare statement
			stmt = conn.prepareStatement( SQL );
			stmt.setMaxRows( maxRows );
		
			// bind parameters
			for (int i=0; sqlParams!=null && i<sqlParams.length; i++ ) {
				stmt.setObject( i+1, sqlParams[i] );
			}
		
		
			rs = stmt.executeQuery();
		
			// fetch the results
			return fetchMultiResults(rs);
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new ComprobanteImpuestoDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(rs);
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Returns all rows from the comprobante_impuesto table that match the specified arbitrary SQL statement
	 */
	public ComprobanteImpuesto[] findByDynamicWhere(String sql, Object[] sqlParams) throws ComprobanteImpuestoDaoException
	{
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		ResultSet rs = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			// construct the SQL statement
			final String SQL = SQL_SELECT + " WHERE " + sql;
		
		
			System.out.println( "Executing " + SQL );
			// prepare statement
			stmt = conn.prepareStatement( SQL );
			stmt.setMaxRows( maxRows );
		
			// bind parameters
			for (int i=0; sqlParams!=null && i<sqlParams.length; i++ ) {
				stmt.setObject( i+1, sqlParams[i] );
			}
		
		
			rs = stmt.executeQuery();
		
			// fetch the results
			return fetchMultiResults(rs);
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new ComprobanteImpuestoDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(rs);
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}
        
        protected final String SQL_SELECT_MAX = "SELECT ID_IMPUESTO, ID_COMPROBANTE_FISCAL, ID_COMPROBANTE_IMPUESTO, ORDEN FROM " + getTableName() + " ORDER BY ID_COMPROBANTE_IMPUESTO DESC LIMIT 1 ";
        
                public ComprobanteImpuesto findLast() throws ComprobanteImpuestoDaoException
	{
		ComprobanteImpuesto ret[] = findByDynamicSelect( SQL_SELECT_MAX , new Object[0] );
		return ret.length==0 ? null : ret[0];
	}

}
