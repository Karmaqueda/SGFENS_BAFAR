/*
 * This source file was generated by FireStorm/DAO.
 * 
 * If you purchase a full license for FireStorm/DAO you can customize this header file.
 * 
 * For more information please visit http://www.codefutures.com/products/firestorm
 */

package com.tsp.sct.dao.jdbc;

import com.tsp.sct.dao.dao.*;
import com.tsp.sct.dao.factory.*;
import com.tsp.sct.dao.dto.*;
import com.tsp.sct.dao.exceptions.*;
import java.sql.Connection;
import java.util.Collection;
import java.sql.PreparedStatement;
import java.sql.Statement;
import java.sql.ResultSet;
import java.sql.SQLException;
import java.sql.Time;
import java.util.List;
import java.util.Iterator;
import java.util.ArrayList;

public class TipoUnidadDaoImpl extends AbstractDAO implements TipoUnidadDao
{
	/** 
	 * The factory class for this DAO has two versions of the create() method - one that
takes no arguments and one that takes a Connection argument. If the Connection version
is chosen then the connection will be stored in this attribute and will be used by all
calls to this DAO, otherwise a new Connection will be allocated for each operation.
	 */
	protected java.sql.Connection userConn;

	/** 
	 * All finder methods in this class use this SELECT constant to build their queries
	 */
	protected final String SQL_SELECT = "SELECT ID_TIPO_UNIDAD, DESC_TIPO_UNIDAD FROM " + getTableName() + "";

	/** 
	 * Finder methods will pass this value to the JDBC setMaxRows method
	 */
	protected int maxRows;

	/** 
	 * SQL INSERT statement for this table
	 */
	protected final String SQL_INSERT = "INSERT INTO " + getTableName() + " ( ID_TIPO_UNIDAD, DESC_TIPO_UNIDAD ) VALUES ( ?, ? )";

	/** 
	 * SQL UPDATE statement for this table
	 */
	protected final String SQL_UPDATE = "UPDATE " + getTableName() + " SET ID_TIPO_UNIDAD = ?, DESC_TIPO_UNIDAD = ? WHERE ID_TIPO_UNIDAD = ?";

	/** 
	 * SQL DELETE statement for this table
	 */
	protected final String SQL_DELETE = "DELETE FROM " + getTableName() + " WHERE ID_TIPO_UNIDAD = ?";

	/** 
	 * Index of column ID_TIPO_UNIDAD
	 */
	protected static final int COLUMN_ID_TIPO_UNIDAD = 1;

	/** 
	 * Index of column DESC_TIPO_UNIDAD
	 */
	protected static final int COLUMN_DESC_TIPO_UNIDAD = 2;

	/** 
	 * Number of columns
	 */
	protected static final int NUMBER_OF_COLUMNS = 2;

	/** 
	 * Index of primary-key column ID_TIPO_UNIDAD
	 */
	protected static final int PK_COLUMN_ID_TIPO_UNIDAD = 1;

	/** 
	 * Inserts a new row in the tipo_unidad table.
	 */
	public TipoUnidadPk insert(TipoUnidad dto) throws TipoUnidadDaoException
	{
		long t1 = System.currentTimeMillis();
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		ResultSet rs = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			stmt = conn.prepareStatement( SQL_INSERT );
			int index = 1;
			stmt.setInt( index++, dto.getIdTipoUnidad() );
			stmt.setString( index++, dto.getDescTipoUnidad() );
			System.out.println( "Executing " + SQL_INSERT + " with DTO: " + dto );
			int rows = stmt.executeUpdate();
			long t2 = System.currentTimeMillis();
			System.out.println( rows + " rows affected (" + (t2-t1) + " ms)" );
			reset(dto);
			return dto.createPk();
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new TipoUnidadDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Updates a single row in the tipo_unidad table.
	 */
	public void update(TipoUnidadPk pk, TipoUnidad dto) throws TipoUnidadDaoException
	{
		long t1 = System.currentTimeMillis();
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			System.out.println( "Executing " + SQL_UPDATE + " with DTO: " + dto );
			stmt = conn.prepareStatement( SQL_UPDATE );
			int index=1;
			stmt.setInt( index++, dto.getIdTipoUnidad() );
			stmt.setString( index++, dto.getDescTipoUnidad() );
			stmt.setInt( 3, pk.getIdTipoUnidad() );
			int rows = stmt.executeUpdate();
			reset(dto);
			long t2 = System.currentTimeMillis();
			System.out.println( rows + " rows affected (" + (t2-t1) + " ms)" );
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new TipoUnidadDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Deletes a single row in the tipo_unidad table.
	 */
	public void delete(TipoUnidadPk pk) throws TipoUnidadDaoException
	{
		long t1 = System.currentTimeMillis();
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			System.out.println( "Executing " + SQL_DELETE + " with PK: " + pk );
			stmt = conn.prepareStatement( SQL_DELETE );
			stmt.setInt( 1, pk.getIdTipoUnidad() );
			int rows = stmt.executeUpdate();
			long t2 = System.currentTimeMillis();
			System.out.println( rows + " rows affected (" + (t2-t1) + " ms)" );
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new TipoUnidadDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Returns the rows from the tipo_unidad table that matches the specified primary-key value.
	 */
	public TipoUnidad findByPrimaryKey(TipoUnidadPk pk) throws TipoUnidadDaoException
	{
		return findByPrimaryKey( pk.getIdTipoUnidad() );
	}

	/** 
	 * Returns all rows from the tipo_unidad table that match the criteria 'ID_TIPO_UNIDAD = :idTipoUnidad'.
	 */
	public TipoUnidad findByPrimaryKey(int idTipoUnidad) throws TipoUnidadDaoException
	{
		TipoUnidad ret[] = findByDynamicSelect( SQL_SELECT + " WHERE ID_TIPO_UNIDAD = ?", new Object[] {  new Integer(idTipoUnidad) } );
		return ret.length==0 ? null : ret[0];
	}

	/** 
	 * Returns all rows from the tipo_unidad table that match the criteria ''.
	 */
	public TipoUnidad[] findAll() throws TipoUnidadDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " ORDER BY ID_TIPO_UNIDAD", null );
	}

	/** 
	 * Returns all rows from the tipo_unidad table that match the criteria 'ID_TIPO_UNIDAD = :idTipoUnidad'.
	 */
	public TipoUnidad[] findWhereIdTipoUnidadEquals(int idTipoUnidad) throws TipoUnidadDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE ID_TIPO_UNIDAD = ? ORDER BY ID_TIPO_UNIDAD", new Object[] {  new Integer(idTipoUnidad) } );
	}

	/** 
	 * Returns all rows from the tipo_unidad table that match the criteria 'DESC_TIPO_UNIDAD = :descTipoUnidad'.
	 */
	public TipoUnidad[] findWhereDescTipoUnidadEquals(String descTipoUnidad) throws TipoUnidadDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE DESC_TIPO_UNIDAD = ? ORDER BY DESC_TIPO_UNIDAD", new Object[] { descTipoUnidad } );
	}

	/**
	 * Method 'TipoUnidadDaoImpl'
	 * 
	 */
	public TipoUnidadDaoImpl()
	{
	}

	/**
	 * Method 'TipoUnidadDaoImpl'
	 * 
	 * @param userConn
	 */
	public TipoUnidadDaoImpl(final java.sql.Connection userConn)
	{
		this.userConn = userConn;
	}

	/** 
	 * Sets the value of maxRows
	 */
	public void setMaxRows(int maxRows)
	{
		this.maxRows = maxRows;
	}

	/** 
	 * Gets the value of maxRows
	 */
	public int getMaxRows()
	{
		return maxRows;
	}

	/**
	 * Method 'getTableName'
	 * 
	 * @return String
	 */
	public String getTableName()
	{
		return "TIPO_UNIDAD";
	}

	/** 
	 * Fetches a single row from the result set
	 */
	protected TipoUnidad fetchSingleResult(ResultSet rs) throws SQLException
	{
		if (rs.next()) {
			TipoUnidad dto = new TipoUnidad();
			populateDto( dto, rs);
			return dto;
		} else {
			return null;
		}
		
	}

	/** 
	 * Fetches multiple rows from the result set
	 */
	protected TipoUnidad[] fetchMultiResults(ResultSet rs) throws SQLException
	{
		Collection resultList = new ArrayList();
		while (rs.next()) {
			TipoUnidad dto = new TipoUnidad();
			populateDto( dto, rs);
			resultList.add( dto );
		}
		
		TipoUnidad ret[] = new TipoUnidad[ resultList.size() ];
		resultList.toArray( ret );
		return ret;
	}

	/** 
	 * Populates a DTO with data from a ResultSet
	 */
	protected void populateDto(TipoUnidad dto, ResultSet rs) throws SQLException
	{
		dto.setIdTipoUnidad( rs.getInt( COLUMN_ID_TIPO_UNIDAD ) );
		dto.setDescTipoUnidad( rs.getString( COLUMN_DESC_TIPO_UNIDAD ) );
	}

	/** 
	 * Resets the modified attributes in the DTO
	 */
	protected void reset(TipoUnidad dto)
	{
	}

	/** 
	 * Returns all rows from the tipo_unidad table that match the specified arbitrary SQL statement
	 */
	public TipoUnidad[] findByDynamicSelect(String sql, Object[] sqlParams) throws TipoUnidadDaoException
	{
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		ResultSet rs = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			// construct the SQL statement
			final String SQL = sql;
		
		
			System.out.println( "Executing " + SQL );
			// prepare statement
			stmt = conn.prepareStatement( SQL );
			stmt.setMaxRows( maxRows );
		
			// bind parameters
			for (int i=0; sqlParams!=null && i<sqlParams.length; i++ ) {
				stmt.setObject( i+1, sqlParams[i] );
			}
		
		
			rs = stmt.executeQuery();
		
			// fetch the results
			return fetchMultiResults(rs);
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new TipoUnidadDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(rs);
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Returns all rows from the tipo_unidad table that match the specified arbitrary SQL statement
	 */
	public TipoUnidad[] findByDynamicWhere(String sql, Object[] sqlParams) throws TipoUnidadDaoException
	{
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		ResultSet rs = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			// construct the SQL statement
			final String SQL = SQL_SELECT + " WHERE " + sql;
		
		
			System.out.println( "Executing " + SQL );
			// prepare statement
			stmt = conn.prepareStatement( SQL );
			stmt.setMaxRows( maxRows );
		
			// bind parameters
			for (int i=0; sqlParams!=null && i<sqlParams.length; i++ ) {
				stmt.setObject( i+1, sqlParams[i] );
			}
		
		
			rs = stmt.executeQuery();
		
			// fetch the results
			return fetchMultiResults(rs);
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new TipoUnidadDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(rs);
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

}
