/*
 * This source file was generated by FireStorm/DAO.
 * 
 * If you purchase a full license for FireStorm/DAO you can customize this header file.
 * 
 * For more information please visit http://www.codefutures.com/products/firestorm
 */

package com.tsp.sct.dao.jdbc;

import com.tsp.sct.dao.dao.*;
import com.tsp.sct.dao.factory.*;
import java.util.Date;
import com.tsp.sct.dao.dto.*;
import com.tsp.sct.dao.exceptions.*;
import java.sql.Connection;
import java.util.Collection;
import java.sql.PreparedStatement;
import java.sql.Statement;
import java.sql.ResultSet;
import java.sql.SQLException;
import java.sql.Time;
import java.util.List;
import java.util.Iterator;
import java.util.ArrayList;

public class HistorialMovilDaoImpl extends AbstractDAO implements HistorialMovilDao
{
	/** 
	 * The factory class for this DAO has two versions of the create() method - one that
takes no arguments and one that takes a Connection argument. If the Connection version
is chosen then the connection will be stored in this attribute and will be used by all
calls to this DAO, otherwise a new Connection will be allocated for each operation.
	 */
	protected java.sql.Connection userConn;

	/** 
	 * All finder methods in this class use this SELECT constant to build their queries
	 */
	protected final String SQL_SELECT = "SELECT ID_HISTORIAL_MOVIL, ID_EMPRESA, IMEI, LOCALIZACION, METODO, DESCRIPCION, FECHA, TIPO FROM " + getTableName() + "";

	/** 
	 * Finder methods will pass this value to the JDBC setMaxRows method
	 */
	protected int maxRows;

	/** 
	 * SQL INSERT statement for this table
	 */
	protected final String SQL_INSERT = "INSERT INTO " + getTableName() + " ( ID_HISTORIAL_MOVIL, ID_EMPRESA, IMEI, LOCALIZACION, METODO, DESCRIPCION, FECHA, TIPO ) VALUES ( ?, ?, ?, ?, ?, ?, ?, ? )";

	/** 
	 * SQL UPDATE statement for this table
	 */
	protected final String SQL_UPDATE = "UPDATE " + getTableName() + " SET ID_HISTORIAL_MOVIL = ?, ID_EMPRESA = ?, IMEI = ?, LOCALIZACION = ?, METODO = ?, DESCRIPCION = ?, FECHA = ?, TIPO = ? WHERE ID_HISTORIAL_MOVIL = ?";

	/** 
	 * SQL DELETE statement for this table
	 */
	protected final String SQL_DELETE = "DELETE FROM " + getTableName() + " WHERE ID_HISTORIAL_MOVIL = ?";

	/** 
	 * Index of column ID_HISTORIAL_MOVIL
	 */
	protected static final int COLUMN_ID_HISTORIAL_MOVIL = 1;

	/** 
	 * Index of column ID_EMPRESA
	 */
	protected static final int COLUMN_ID_EMPRESA = 2;

	/** 
	 * Index of column IMEI
	 */
	protected static final int COLUMN_IMEI = 3;

	/** 
	 * Index of column LOCALIZACION
	 */
	protected static final int COLUMN_LOCALIZACION = 4;

	/** 
	 * Index of column METODO
	 */
	protected static final int COLUMN_METODO = 5;

	/** 
	 * Index of column DESCRIPCION
	 */
	protected static final int COLUMN_DESCRIPCION = 6;

	/** 
	 * Index of column FECHA
	 */
	protected static final int COLUMN_FECHA = 7;

	/** 
	 * Index of column TIPO
	 */
	protected static final int COLUMN_TIPO = 8;

	/** 
	 * Number of columns
	 */
	protected static final int NUMBER_OF_COLUMNS = 8;

	/** 
	 * Index of primary-key column ID_HISTORIAL_MOVIL
	 */
	protected static final int PK_COLUMN_ID_HISTORIAL_MOVIL = 1;

	/** 
	 * Inserts a new row in the historial_movil table.
	 */
	public HistorialMovilPk insert(HistorialMovil dto) throws HistorialMovilDaoException
	{
		long t1 = System.currentTimeMillis();
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		ResultSet rs = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			StringBuffer sql = new StringBuffer();
			StringBuffer values = new StringBuffer();
			sql.append( "INSERT INTO " + getTableName() + " (" );
			int modifiedCount = 0;
			if (dto.isIdHistorialMovilModified()) {
				if (modifiedCount>0) {
					sql.append( ", " );
					values.append( ", " );
				}
		
				sql.append( "ID_HISTORIAL_MOVIL" );
				values.append( "?" );
				modifiedCount++;
			}
		
			if (dto.isIdEmpresaModified()) {
				if (modifiedCount>0) {
					sql.append( ", " );
					values.append( ", " );
				}
		
				sql.append( "ID_EMPRESA" );
				values.append( "?" );
				modifiedCount++;
			}
		
			if (dto.isImeiModified()) {
				if (modifiedCount>0) {
					sql.append( ", " );
					values.append( ", " );
				}
		
				sql.append( "IMEI" );
				values.append( "?" );
				modifiedCount++;
			}
		
			if (dto.isLocalizacionModified()) {
				if (modifiedCount>0) {
					sql.append( ", " );
					values.append( ", " );
				}
		
				sql.append( "LOCALIZACION" );
				values.append( "?" );
				modifiedCount++;
			}
		
			if (dto.isMetodoModified()) {
				if (modifiedCount>0) {
					sql.append( ", " );
					values.append( ", " );
				}
		
				sql.append( "METODO" );
				values.append( "?" );
				modifiedCount++;
			}
		
			if (dto.isDescripcionModified()) {
				if (modifiedCount>0) {
					sql.append( ", " );
					values.append( ", " );
				}
		
				sql.append( "DESCRIPCION" );
				values.append( "?" );
				modifiedCount++;
			}
		
			if (dto.isFechaModified()) {
				if (modifiedCount>0) {
					sql.append( ", " );
					values.append( ", " );
				}
		
				sql.append( "FECHA" );
				values.append( "?" );
				modifiedCount++;
			}
		
			if (dto.isTipoModified()) {
				if (modifiedCount>0) {
					sql.append( ", " );
					values.append( ", " );
				}
		
				sql.append( "TIPO" );
				values.append( "?" );
				modifiedCount++;
			}
		
			if (modifiedCount==0) {
				// nothing to insert
				throw new IllegalStateException( "Nothing to insert" );
			}
		
			sql.append( ") VALUES (" );
			sql.append( values );
			sql.append( ")" );
			stmt = conn.prepareStatement( sql.toString(), Statement.RETURN_GENERATED_KEYS );
			int index = 1;
			if (dto.isIdHistorialMovilModified()) {
				stmt.setInt( index++, dto.getIdHistorialMovil() );
			}
		
			if (dto.isIdEmpresaModified()) {
				if (dto.isIdEmpresaNull()) {
					stmt.setNull( index++, java.sql.Types.INTEGER );
				} else {
					stmt.setInt( index++, dto.getIdEmpresa() );
				}
		
			}
		
			if (dto.isImeiModified()) {
				stmt.setString( index++, dto.getImei() );
			}
		
			if (dto.isLocalizacionModified()) {
				stmt.setString( index++, dto.getLocalizacion() );
			}
		
			if (dto.isMetodoModified()) {
				stmt.setString( index++, dto.getMetodo() );
			}
		
			if (dto.isDescripcionModified()) {
				stmt.setString( index++, dto.getDescripcion() );
			}
		
			if (dto.isFechaModified()) {
				stmt.setTimestamp(index++, dto.getFecha()==null ? null : new java.sql.Timestamp( dto.getFecha().getTime() ) );
			}
		
			if (dto.isTipoModified()) {
				if (dto.isTipoNull()) {
					stmt.setNull( index++, java.sql.Types.INTEGER );
				} else {
					stmt.setInt( index++, dto.getTipo() );
				}
		
			}
		
			System.out.println( "Executing " + sql.toString() + " with values: " + dto );
			int rows = stmt.executeUpdate();
			long t2 = System.currentTimeMillis();
			System.out.println( rows + " rows affected (" + (t2-t1) + " ms)" );
		
			// retrieve values from auto-increment columns
			rs = stmt.getGeneratedKeys();
			if (rs != null && rs.next()) {
				dto.setIdHistorialMovil( rs.getInt( 1 ) );
			}
		
			reset(dto);
			return dto.createPk();
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new HistorialMovilDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Updates a single row in the historial_movil table.
	 */
	public void update(HistorialMovilPk pk, HistorialMovil dto) throws HistorialMovilDaoException
	{
		long t1 = System.currentTimeMillis();
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			StringBuffer sql = new StringBuffer();
			sql.append( "UPDATE " + getTableName() + " SET " );
			boolean modified = false;
			if (dto.isIdHistorialMovilModified()) {
				if (modified) {
					sql.append( ", " );
				}
		
				sql.append( "ID_HISTORIAL_MOVIL=?" );
				modified=true;
			}
		
			if (dto.isIdEmpresaModified()) {
				if (modified) {
					sql.append( ", " );
				}
		
				sql.append( "ID_EMPRESA=?" );
				modified=true;
			}
		
			if (dto.isImeiModified()) {
				if (modified) {
					sql.append( ", " );
				}
		
				sql.append( "IMEI=?" );
				modified=true;
			}
		
			if (dto.isLocalizacionModified()) {
				if (modified) {
					sql.append( ", " );
				}
		
				sql.append( "LOCALIZACION=?" );
				modified=true;
			}
		
			if (dto.isMetodoModified()) {
				if (modified) {
					sql.append( ", " );
				}
		
				sql.append( "METODO=?" );
				modified=true;
			}
		
			if (dto.isDescripcionModified()) {
				if (modified) {
					sql.append( ", " );
				}
		
				sql.append( "DESCRIPCION=?" );
				modified=true;
			}
		
			if (dto.isFechaModified()) {
				if (modified) {
					sql.append( ", " );
				}
		
				sql.append( "FECHA=?" );
				modified=true;
			}
		
			if (dto.isTipoModified()) {
				if (modified) {
					sql.append( ", " );
				}
		
				sql.append( "TIPO=?" );
				modified=true;
			}
		
			if (!modified) {
				// nothing to update
				return;
			}
		
			sql.append( " WHERE ID_HISTORIAL_MOVIL=?" );
			System.out.println( "Executing " + sql.toString() + " with values: " + dto );
			stmt = conn.prepareStatement( sql.toString() );
			int index = 1;
			if (dto.isIdHistorialMovilModified()) {
				stmt.setInt( index++, dto.getIdHistorialMovil() );
			}
		
			if (dto.isIdEmpresaModified()) {
				if (dto.isIdEmpresaNull()) {
					stmt.setNull( index++, java.sql.Types.INTEGER );
				} else {
					stmt.setInt( index++, dto.getIdEmpresa() );
				}
		
			}
		
			if (dto.isImeiModified()) {
				stmt.setString( index++, dto.getImei() );
			}
		
			if (dto.isLocalizacionModified()) {
				stmt.setString( index++, dto.getLocalizacion() );
			}
		
			if (dto.isMetodoModified()) {
				stmt.setString( index++, dto.getMetodo() );
			}
		
			if (dto.isDescripcionModified()) {
				stmt.setString( index++, dto.getDescripcion() );
			}
		
			if (dto.isFechaModified()) {
				stmt.setTimestamp(index++, dto.getFecha()==null ? null : new java.sql.Timestamp( dto.getFecha().getTime() ) );
			}
		
			if (dto.isTipoModified()) {
				if (dto.isTipoNull()) {
					stmt.setNull( index++, java.sql.Types.INTEGER );
				} else {
					stmt.setInt( index++, dto.getTipo() );
				}
		
			}
		
			stmt.setInt( index++, pk.getIdHistorialMovil() );
			int rows = stmt.executeUpdate();
			reset(dto);
			long t2 = System.currentTimeMillis();
			System.out.println( rows + " rows affected (" + (t2-t1) + " ms)" );
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new HistorialMovilDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Deletes a single row in the historial_movil table.
	 */
	public void delete(HistorialMovilPk pk) throws HistorialMovilDaoException
	{
		long t1 = System.currentTimeMillis();
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			System.out.println( "Executing " + SQL_DELETE + " with PK: " + pk );
			stmt = conn.prepareStatement( SQL_DELETE );
			stmt.setInt( 1, pk.getIdHistorialMovil() );
			int rows = stmt.executeUpdate();
			long t2 = System.currentTimeMillis();
			System.out.println( rows + " rows affected (" + (t2-t1) + " ms)" );
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new HistorialMovilDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Returns the rows from the historial_movil table that matches the specified primary-key value.
	 */
	public HistorialMovil findByPrimaryKey(HistorialMovilPk pk) throws HistorialMovilDaoException
	{
		return findByPrimaryKey( pk.getIdHistorialMovil() );
	}

	/** 
	 * Returns all rows from the historial_movil table that match the criteria 'ID_HISTORIAL_MOVIL = :idHistorialMovil'.
	 */
	public HistorialMovil findByPrimaryKey(int idHistorialMovil) throws HistorialMovilDaoException
	{
		HistorialMovil ret[] = findByDynamicSelect( SQL_SELECT + " WHERE ID_HISTORIAL_MOVIL = ?", new Object[] {  new Integer(idHistorialMovil) } );
		return ret.length==0 ? null : ret[0];
	}

	/** 
	 * Returns all rows from the historial_movil table that match the criteria ''.
	 */
	public HistorialMovil[] findAll() throws HistorialMovilDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " ORDER BY ID_HISTORIAL_MOVIL", null );
	}

	/** 
	 * Returns all rows from the historial_movil table that match the criteria 'ID_HISTORIAL_MOVIL = :idHistorialMovil'.
	 */
	public HistorialMovil[] findWhereIdHistorialMovilEquals(int idHistorialMovil) throws HistorialMovilDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE ID_HISTORIAL_MOVIL = ? ORDER BY ID_HISTORIAL_MOVIL", new Object[] {  new Integer(idHistorialMovil) } );
	}

	/** 
	 * Returns all rows from the historial_movil table that match the criteria 'ID_EMPRESA = :idEmpresa'.
	 */
	public HistorialMovil[] findWhereIdEmpresaEquals(int idEmpresa) throws HistorialMovilDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE ID_EMPRESA = ? ORDER BY ID_EMPRESA", new Object[] {  new Integer(idEmpresa) } );
	}

	/** 
	 * Returns all rows from the historial_movil table that match the criteria 'IMEI = :imei'.
	 */
	public HistorialMovil[] findWhereImeiEquals(String imei) throws HistorialMovilDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE IMEI = ? ORDER BY IMEI", new Object[] { imei } );
	}

	/** 
	 * Returns all rows from the historial_movil table that match the criteria 'LOCALIZACION = :localizacion'.
	 */
	public HistorialMovil[] findWhereLocalizacionEquals(String localizacion) throws HistorialMovilDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE LOCALIZACION = ? ORDER BY LOCALIZACION", new Object[] { localizacion } );
	}

	/** 
	 * Returns all rows from the historial_movil table that match the criteria 'METODO = :metodo'.
	 */
	public HistorialMovil[] findWhereMetodoEquals(String metodo) throws HistorialMovilDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE METODO = ? ORDER BY METODO", new Object[] { metodo } );
	}

	/** 
	 * Returns all rows from the historial_movil table that match the criteria 'DESCRIPCION = :descripcion'.
	 */
	public HistorialMovil[] findWhereDescripcionEquals(String descripcion) throws HistorialMovilDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE DESCRIPCION = ? ORDER BY DESCRIPCION", new Object[] { descripcion } );
	}

	/** 
	 * Returns all rows from the historial_movil table that match the criteria 'FECHA = :fecha'.
	 */
	public HistorialMovil[] findWhereFechaEquals(Date fecha) throws HistorialMovilDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE FECHA = ? ORDER BY FECHA", new Object[] { fecha==null ? null : new java.sql.Timestamp( fecha.getTime() ) } );
	}

	/** 
	 * Returns all rows from the historial_movil table that match the criteria 'TIPO = :tipo'.
	 */
	public HistorialMovil[] findWhereTipoEquals(int tipo) throws HistorialMovilDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE TIPO = ? ORDER BY TIPO", new Object[] {  new Integer(tipo) } );
	}

	/**
	 * Method 'HistorialMovilDaoImpl'
	 * 
	 */
	public HistorialMovilDaoImpl()
	{
	}

	/**
	 * Method 'HistorialMovilDaoImpl'
	 * 
	 * @param userConn
	 */
	public HistorialMovilDaoImpl(final java.sql.Connection userConn)
	{
		this.userConn = userConn;
	}

	/** 
	 * Sets the value of maxRows
	 */
	public void setMaxRows(int maxRows)
	{
		this.maxRows = maxRows;
	}

	/** 
	 * Gets the value of maxRows
	 */
	public int getMaxRows()
	{
		return maxRows;
	}

	/**
	 * Method 'getTableName'
	 * 
	 * @return String
	 */
	public String getTableName()
	{
		return "historial_movil";
	}

	/** 
	 * Fetches a single row from the result set
	 */
	protected HistorialMovil fetchSingleResult(ResultSet rs) throws SQLException
	{
		if (rs.next()) {
			HistorialMovil dto = new HistorialMovil();
			populateDto( dto, rs);
			return dto;
		} else {
			return null;
		}
		
	}

	/** 
	 * Fetches multiple rows from the result set
	 */
	protected HistorialMovil[] fetchMultiResults(ResultSet rs) throws SQLException
	{
		Collection resultList = new ArrayList();
		while (rs.next()) {
			HistorialMovil dto = new HistorialMovil();
			populateDto( dto, rs);
			resultList.add( dto );
		}
		
		HistorialMovil ret[] = new HistorialMovil[ resultList.size() ];
		resultList.toArray( ret );
		return ret;
	}

	/** 
	 * Populates a DTO with data from a ResultSet
	 */
	protected void populateDto(HistorialMovil dto, ResultSet rs) throws SQLException
	{
		dto.setIdHistorialMovil( rs.getInt( COLUMN_ID_HISTORIAL_MOVIL ) );
		dto.setIdEmpresa( rs.getInt( COLUMN_ID_EMPRESA ) );
		if (rs.wasNull()) {
			dto.setIdEmpresaNull( true );
		}
		
		dto.setImei( rs.getString( COLUMN_IMEI ) );
		dto.setLocalizacion( rs.getString( COLUMN_LOCALIZACION ) );
		dto.setMetodo( rs.getString( COLUMN_METODO ) );
		dto.setDescripcion( rs.getString( COLUMN_DESCRIPCION ) );
		dto.setFecha( rs.getTimestamp(COLUMN_FECHA ) );
		dto.setTipo( rs.getInt( COLUMN_TIPO ) );
		if (rs.wasNull()) {
			dto.setTipoNull( true );
		}
		
		reset(dto);
	}

	/** 
	 * Resets the modified attributes in the DTO
	 */
	protected void reset(HistorialMovil dto)
	{
		dto.setIdHistorialMovilModified( false );
		dto.setIdEmpresaModified( false );
		dto.setImeiModified( false );
		dto.setLocalizacionModified( false );
		dto.setMetodoModified( false );
		dto.setDescripcionModified( false );
		dto.setFechaModified( false );
		dto.setTipoModified( false );
	}

	/** 
	 * Returns all rows from the historial_movil table that match the specified arbitrary SQL statement
	 */
	public HistorialMovil[] findByDynamicSelect(String sql, Object[] sqlParams) throws HistorialMovilDaoException
	{
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		ResultSet rs = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			// construct the SQL statement
			final String SQL = sql;
		
		
			System.out.println( "Executing " + SQL );
			// prepare statement
			stmt = conn.prepareStatement( SQL );
			stmt.setMaxRows( maxRows );
		
			// bind parameters
			for (int i=0; sqlParams!=null && i<sqlParams.length; i++ ) {
				stmt.setObject( i+1, sqlParams[i] );
			}
		
		
			rs = stmt.executeQuery();
		
			// fetch the results
			return fetchMultiResults(rs);
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new HistorialMovilDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(rs);
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Returns all rows from the historial_movil table that match the specified arbitrary SQL statement
	 */
	public HistorialMovil[] findByDynamicWhere(String sql, Object[] sqlParams) throws HistorialMovilDaoException
	{
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		ResultSet rs = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			// construct the SQL statement
			final String SQL = SQL_SELECT + " WHERE " + sql;
		
		
			System.out.println( "Executing " + SQL );
			// prepare statement
			stmt = conn.prepareStatement( SQL );
			stmt.setMaxRows( maxRows );
		
			// bind parameters
			for (int i=0; sqlParams!=null && i<sqlParams.length; i++ ) {
				stmt.setObject( i+1, sqlParams[i] );
			}
		
		
			rs = stmt.executeQuery();
		
			// fetch the results
			return fetchMultiResults(rs);
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new HistorialMovilDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(rs);
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

}
