/*
 * This source file was generated by FireStorm/DAO.
 * 
 * If you purchase a full license for FireStorm/DAO you can customize this header file.
 * 
 * For more information please visit http://www.codefutures.com/products/firestorm
 */

package com.tsp.sct.dao.jdbc;

import com.tsp.sct.dao.dao.*;
import com.tsp.sct.dao.factory.*;
import com.tsp.sct.dao.dto.*;
import com.tsp.sct.dao.exceptions.*;
import java.sql.Connection;
import java.util.Collection;
import java.sql.PreparedStatement;
import java.sql.Statement;
import java.sql.ResultSet;
import java.sql.SQLException;
import java.sql.Time;
import java.util.List;
import java.util.Iterator;
import java.util.ArrayList;

public class GeocercasNotificacionesDaoImpl extends AbstractDAO implements GeocercasNotificacionesDao
{
	/** 
	 * The factory class for this DAO has two versions of the create() method - one that
takes no arguments and one that takes a Connection argument. If the Connection version
is chosen then the connection will be stored in this attribute and will be used by all
calls to this DAO, otherwise a new Connection will be allocated for each operation.
	 */
	protected java.sql.Connection userConn;

	/** 
	 * All finder methods in this class use this SELECT constant to build their queries
	 */
	protected final String SQL_SELECT = "SELECT ID_GEO_NOTIFICACION, ID_EMPRESA, CORREOS, ID_ESTATUS FROM " + getTableName() + "";

	/** 
	 * Finder methods will pass this value to the JDBC setMaxRows method
	 */
	protected int maxRows;

	/** 
	 * SQL INSERT statement for this table
	 */
	protected final String SQL_INSERT = "INSERT INTO " + getTableName() + " ( ID_GEO_NOTIFICACION, ID_EMPRESA, CORREOS, ID_ESTATUS ) VALUES ( ?, ?, ?, ? )";

	/** 
	 * SQL UPDATE statement for this table
	 */
	protected final String SQL_UPDATE = "UPDATE " + getTableName() + " SET ID_GEO_NOTIFICACION = ?, ID_EMPRESA = ?, CORREOS = ?, ID_ESTATUS = ? WHERE ID_GEO_NOTIFICACION = ?";

	/** 
	 * SQL DELETE statement for this table
	 */
	protected final String SQL_DELETE = "DELETE FROM " + getTableName() + " WHERE ID_GEO_NOTIFICACION = ?";

	/** 
	 * Index of column ID_GEO_NOTIFICACION
	 */
	protected static final int COLUMN_ID_GEO_NOTIFICACION = 1;

	/** 
	 * Index of column ID_EMPRESA
	 */
	protected static final int COLUMN_ID_EMPRESA = 2;

	/** 
	 * Index of column CORREOS
	 */
	protected static final int COLUMN_CORREOS = 3;

	/** 
	 * Index of column ID_ESTATUS
	 */
	protected static final int COLUMN_ID_ESTATUS = 4;

	/** 
	 * Number of columns
	 */
	protected static final int NUMBER_OF_COLUMNS = 4;

	/** 
	 * Index of primary-key column ID_GEO_NOTIFICACION
	 */
	protected static final int PK_COLUMN_ID_GEO_NOTIFICACION = 1;

	/** 
	 * Inserts a new row in the geocercas_notificaciones table.
	 */
	public GeocercasNotificacionesPk insert(GeocercasNotificaciones dto) throws GeocercasNotificacionesDaoException
	{
		long t1 = System.currentTimeMillis();
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		ResultSet rs = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			StringBuffer sql = new StringBuffer();
			StringBuffer values = new StringBuffer();
			sql.append( "INSERT INTO " + getTableName() + " (" );
			int modifiedCount = 0;
			if (dto.isIdGeoNotificacionModified()) {
				if (modifiedCount>0) {
					sql.append( ", " );
					values.append( ", " );
				}
		
				sql.append( "ID_GEO_NOTIFICACION" );
				values.append( "?" );
				modifiedCount++;
			}
		
			if (dto.isIdEmpresaModified()) {
				if (modifiedCount>0) {
					sql.append( ", " );
					values.append( ", " );
				}
		
				sql.append( "ID_EMPRESA" );
				values.append( "?" );
				modifiedCount++;
			}
		
			if (dto.isCorreosModified()) {
				if (modifiedCount>0) {
					sql.append( ", " );
					values.append( ", " );
				}
		
				sql.append( "CORREOS" );
				values.append( "?" );
				modifiedCount++;
			}
		
			if (dto.isIdEstatusModified()) {
				if (modifiedCount>0) {
					sql.append( ", " );
					values.append( ", " );
				}
		
				sql.append( "ID_ESTATUS" );
				values.append( "?" );
				modifiedCount++;
			}
		
			if (modifiedCount==0) {
				// nothing to insert
				throw new IllegalStateException( "Nothing to insert" );
			}
		
			sql.append( ") VALUES (" );
			sql.append( values );
			sql.append( ")" );
			stmt = conn.prepareStatement( sql.toString(), Statement.RETURN_GENERATED_KEYS );
			int index = 1;
			if (dto.isIdGeoNotificacionModified()) {
				stmt.setInt( index++, dto.getIdGeoNotificacion() );
			}
		
			if (dto.isIdEmpresaModified()) {
				if (dto.isIdEmpresaNull()) {
					stmt.setNull( index++, java.sql.Types.INTEGER );
				} else {
					stmt.setInt( index++, dto.getIdEmpresa() );
				}
		
			}
		
			if (dto.isCorreosModified()) {
				stmt.setString( index++, dto.getCorreos() );
			}
		
			if (dto.isIdEstatusModified()) {
				if (dto.isIdEstatusNull()) {
					stmt.setNull( index++, java.sql.Types.INTEGER );
				} else {
					stmt.setInt( index++, dto.getIdEstatus() );
				}
		
			}
		
			System.out.println( "Executing " + sql.toString() + " with values: " + dto );
			int rows = stmt.executeUpdate();
			long t2 = System.currentTimeMillis();
			System.out.println( rows + " rows affected (" + (t2-t1) + " ms)" );
		
			// retrieve values from auto-increment columns
			rs = stmt.getGeneratedKeys();
			if (rs != null && rs.next()) {
				dto.setIdGeoNotificacion( rs.getInt( 1 ) );
			}
		
			reset(dto);
			return dto.createPk();
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new GeocercasNotificacionesDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Updates a single row in the geocercas_notificaciones table.
	 */
	public void update(GeocercasNotificacionesPk pk, GeocercasNotificaciones dto) throws GeocercasNotificacionesDaoException
	{
		long t1 = System.currentTimeMillis();
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			StringBuffer sql = new StringBuffer();
			sql.append( "UPDATE " + getTableName() + " SET " );
			boolean modified = false;
			if (dto.isIdGeoNotificacionModified()) {
				if (modified) {
					sql.append( ", " );
				}
		
				sql.append( "ID_GEO_NOTIFICACION=?" );
				modified=true;
			}
		
			if (dto.isIdEmpresaModified()) {
				if (modified) {
					sql.append( ", " );
				}
		
				sql.append( "ID_EMPRESA=?" );
				modified=true;
			}
		
			if (dto.isCorreosModified()) {
				if (modified) {
					sql.append( ", " );
				}
		
				sql.append( "CORREOS=?" );
				modified=true;
			}
		
			if (dto.isIdEstatusModified()) {
				if (modified) {
					sql.append( ", " );
				}
		
				sql.append( "ID_ESTATUS=?" );
				modified=true;
			}
		
			if (!modified) {
				// nothing to update
				return;
			}
		
			sql.append( " WHERE ID_GEO_NOTIFICACION=?" );
			System.out.println( "Executing " + sql.toString() + " with values: " + dto );
			stmt = conn.prepareStatement( sql.toString() );
			int index = 1;
			if (dto.isIdGeoNotificacionModified()) {
				stmt.setInt( index++, dto.getIdGeoNotificacion() );
			}
		
			if (dto.isIdEmpresaModified()) {
				if (dto.isIdEmpresaNull()) {
					stmt.setNull( index++, java.sql.Types.INTEGER );
				} else {
					stmt.setInt( index++, dto.getIdEmpresa() );
				}
		
			}
		
			if (dto.isCorreosModified()) {
				stmt.setString( index++, dto.getCorreos() );
			}
		
			if (dto.isIdEstatusModified()) {
				if (dto.isIdEstatusNull()) {
					stmt.setNull( index++, java.sql.Types.INTEGER );
				} else {
					stmt.setInt( index++, dto.getIdEstatus() );
				}
		
			}
		
			stmt.setInt( index++, pk.getIdGeoNotificacion() );
			int rows = stmt.executeUpdate();
			reset(dto);
			long t2 = System.currentTimeMillis();
			System.out.println( rows + " rows affected (" + (t2-t1) + " ms)" );
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new GeocercasNotificacionesDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Deletes a single row in the geocercas_notificaciones table.
	 */
	public void delete(GeocercasNotificacionesPk pk) throws GeocercasNotificacionesDaoException
	{
		long t1 = System.currentTimeMillis();
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			System.out.println( "Executing " + SQL_DELETE + " with PK: " + pk );
			stmt = conn.prepareStatement( SQL_DELETE );
			stmt.setInt( 1, pk.getIdGeoNotificacion() );
			int rows = stmt.executeUpdate();
			long t2 = System.currentTimeMillis();
			System.out.println( rows + " rows affected (" + (t2-t1) + " ms)" );
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new GeocercasNotificacionesDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Returns the rows from the geocercas_notificaciones table that matches the specified primary-key value.
	 */
	public GeocercasNotificaciones findByPrimaryKey(GeocercasNotificacionesPk pk) throws GeocercasNotificacionesDaoException
	{
		return findByPrimaryKey( pk.getIdGeoNotificacion() );
	}

	/** 
	 * Returns all rows from the geocercas_notificaciones table that match the criteria 'ID_GEO_NOTIFICACION = :idGeoNotificacion'.
	 */
	public GeocercasNotificaciones findByPrimaryKey(int idGeoNotificacion) throws GeocercasNotificacionesDaoException
	{
		GeocercasNotificaciones ret[] = findByDynamicSelect( SQL_SELECT + " WHERE ID_GEO_NOTIFICACION = ?", new Object[] {  new Integer(idGeoNotificacion) } );
		return ret.length==0 ? null : ret[0];
	}

	/** 
	 * Returns all rows from the geocercas_notificaciones table that match the criteria ''.
	 */
	public GeocercasNotificaciones[] findAll() throws GeocercasNotificacionesDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " ORDER BY ID_GEO_NOTIFICACION", null );
	}

	/** 
	 * Returns all rows from the geocercas_notificaciones table that match the criteria 'ID_GEO_NOTIFICACION = :idGeoNotificacion'.
	 */
	public GeocercasNotificaciones[] findWhereIdGeoNotificacionEquals(int idGeoNotificacion) throws GeocercasNotificacionesDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE ID_GEO_NOTIFICACION = ? ORDER BY ID_GEO_NOTIFICACION", new Object[] {  new Integer(idGeoNotificacion) } );
	}

	/** 
	 * Returns all rows from the geocercas_notificaciones table that match the criteria 'ID_EMPRESA = :idEmpresa'.
	 */
	public GeocercasNotificaciones[] findWhereIdEmpresaEquals(int idEmpresa) throws GeocercasNotificacionesDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE ID_EMPRESA = ? ORDER BY ID_EMPRESA", new Object[] {  new Integer(idEmpresa) } );
	}

	/** 
	 * Returns all rows from the geocercas_notificaciones table that match the criteria 'CORREOS = :correos'.
	 */
	public GeocercasNotificaciones[] findWhereCorreosEquals(String correos) throws GeocercasNotificacionesDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE CORREOS = ? ORDER BY CORREOS", new Object[] { correos } );
	}

	/** 
	 * Returns all rows from the geocercas_notificaciones table that match the criteria 'ID_ESTATUS = :idEstatus'.
	 */
	public GeocercasNotificaciones[] findWhereIdEstatusEquals(int idEstatus) throws GeocercasNotificacionesDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE ID_ESTATUS = ? ORDER BY ID_ESTATUS", new Object[] {  new Integer(idEstatus) } );
	}

	/**
	 * Method 'GeocercasNotificacionesDaoImpl'
	 * 
	 */
	public GeocercasNotificacionesDaoImpl()
	{
	}

	/**
	 * Method 'GeocercasNotificacionesDaoImpl'
	 * 
	 * @param userConn
	 */
	public GeocercasNotificacionesDaoImpl(final java.sql.Connection userConn)
	{
		this.userConn = userConn;
	}

	/** 
	 * Sets the value of maxRows
	 */
	public void setMaxRows(int maxRows)
	{
		this.maxRows = maxRows;
	}

	/** 
	 * Gets the value of maxRows
	 */
	public int getMaxRows()
	{
		return maxRows;
	}

	/**
	 * Method 'getTableName'
	 * 
	 * @return String
	 */
	public String getTableName()
	{
		return "geocercas_notificaciones";
	}

	/** 
	 * Fetches a single row from the result set
	 */
	protected GeocercasNotificaciones fetchSingleResult(ResultSet rs) throws SQLException
	{
		if (rs.next()) {
			GeocercasNotificaciones dto = new GeocercasNotificaciones();
			populateDto( dto, rs);
			return dto;
		} else {
			return null;
		}
		
	}

	/** 
	 * Fetches multiple rows from the result set
	 */
	protected GeocercasNotificaciones[] fetchMultiResults(ResultSet rs) throws SQLException
	{
		Collection resultList = new ArrayList();
		while (rs.next()) {
			GeocercasNotificaciones dto = new GeocercasNotificaciones();
			populateDto( dto, rs);
			resultList.add( dto );
		}
		
		GeocercasNotificaciones ret[] = new GeocercasNotificaciones[ resultList.size() ];
		resultList.toArray( ret );
		return ret;
	}

	/** 
	 * Populates a DTO with data from a ResultSet
	 */
	protected void populateDto(GeocercasNotificaciones dto, ResultSet rs) throws SQLException
	{
		dto.setIdGeoNotificacion( rs.getInt( COLUMN_ID_GEO_NOTIFICACION ) );
		dto.setIdEmpresa( rs.getInt( COLUMN_ID_EMPRESA ) );
		if (rs.wasNull()) {
			dto.setIdEmpresaNull( true );
		}
		
		dto.setCorreos( rs.getString( COLUMN_CORREOS ) );
		dto.setIdEstatus( rs.getInt( COLUMN_ID_ESTATUS ) );
		if (rs.wasNull()) {
			dto.setIdEstatusNull( true );
		}
		
		reset(dto);
	}

	/** 
	 * Resets the modified attributes in the DTO
	 */
	protected void reset(GeocercasNotificaciones dto)
	{
		dto.setIdGeoNotificacionModified( false );
		dto.setIdEmpresaModified( false );
		dto.setCorreosModified( false );
		dto.setIdEstatusModified( false );
	}

	/** 
	 * Returns all rows from the geocercas_notificaciones table that match the specified arbitrary SQL statement
	 */
	public GeocercasNotificaciones[] findByDynamicSelect(String sql, Object[] sqlParams) throws GeocercasNotificacionesDaoException
	{
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		ResultSet rs = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			// construct the SQL statement
			final String SQL = sql;
		
		
			System.out.println( "Executing " + SQL );
			// prepare statement
			stmt = conn.prepareStatement( SQL );
			stmt.setMaxRows( maxRows );
		
			// bind parameters
			for (int i=0; sqlParams!=null && i<sqlParams.length; i++ ) {
				stmt.setObject( i+1, sqlParams[i] );
			}
		
		
			rs = stmt.executeQuery();
		
			// fetch the results
			return fetchMultiResults(rs);
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new GeocercasNotificacionesDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(rs);
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Returns all rows from the geocercas_notificaciones table that match the specified arbitrary SQL statement
	 */
	public GeocercasNotificaciones[] findByDynamicWhere(String sql, Object[] sqlParams) throws GeocercasNotificacionesDaoException
	{
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		ResultSet rs = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			// construct the SQL statement
			final String SQL = SQL_SELECT + " WHERE " + sql;
		
		
			System.out.println( "Executing " + SQL );
			// prepare statement
			stmt = conn.prepareStatement( SQL );
			stmt.setMaxRows( maxRows );
		
			// bind parameters
			for (int i=0; sqlParams!=null && i<sqlParams.length; i++ ) {
				stmt.setObject( i+1, sqlParams[i] );
			}
		
		
			rs = stmt.executeQuery();
		
			// fetch the results
			return fetchMultiResults(rs);
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new GeocercasNotificacionesDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(rs);
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

}
