/*
 * This source file was generated by FireStorm/DAO.
 * 
 * If you purchase a full license for FireStorm/DAO you can customize this header file.
 * 
 * For more information please visit http://www.codefutures.com/products/firestorm
 */

package com.tsp.sct.dao.jdbc;

import com.tsp.sct.dao.dao.*;
import com.tsp.sct.dao.factory.*;
import java.util.Date;
import com.tsp.sct.dao.dto.*;
import com.tsp.sct.dao.exceptions.*;
import java.sql.Connection;
import java.util.Collection;
import java.sql.PreparedStatement;
import java.sql.Statement;
import java.sql.ResultSet;
import java.sql.SQLException;
import java.sql.Time;
import java.util.List;
import java.util.Iterator;
import java.util.ArrayList;

public class DonatariasDaoImpl extends AbstractDAO implements DonatariasDao
{
	/** 
	 * The factory class for this DAO has two versions of the create() method - one that
takes no arguments and one that takes a Connection argument. If the Connection version
is chosen then the connection will be stored in this attribute and will be used by all
calls to this DAO, otherwise a new Connection will be allocated for each operation.
	 */
	protected java.sql.Connection userConn;

	/** 
	 * All finder methods in this class use this SELECT constant to build their queries
	 */
	protected final String SQL_SELECT = "SELECT ID_DONATARIA, NO_AUTORIZACION, FECHA_AUTORIZACION, ID_COMPROBANTE_FISCAL FROM " + getTableName() + "";

	/** 
	 * Finder methods will pass this value to the JDBC setMaxRows method
	 */
	protected int maxRows;

	/** 
	 * SQL INSERT statement for this table
	 */
	protected final String SQL_INSERT = "INSERT INTO " + getTableName() + " ( ID_DONATARIA, NO_AUTORIZACION, FECHA_AUTORIZACION, ID_COMPROBANTE_FISCAL ) VALUES ( ?, ?, ?, ? )";

	/** 
	 * SQL UPDATE statement for this table
	 */
	protected final String SQL_UPDATE = "UPDATE " + getTableName() + " SET ID_DONATARIA = ?, NO_AUTORIZACION = ?, FECHA_AUTORIZACION = ?, ID_COMPROBANTE_FISCAL = ? WHERE ID_DONATARIA = ?";

	/** 
	 * SQL DELETE statement for this table
	 */
	protected final String SQL_DELETE = "DELETE FROM " + getTableName() + " WHERE ID_DONATARIA = ?";

	/** 
	 * Index of column ID_DONATARIA
	 */
	protected static final int COLUMN_ID_DONATARIA = 1;

	/** 
	 * Index of column NO_AUTORIZACION
	 */
	protected static final int COLUMN_NO_AUTORIZACION = 2;

	/** 
	 * Index of column FECHA_AUTORIZACION
	 */
	protected static final int COLUMN_FECHA_AUTORIZACION = 3;

	/** 
	 * Index of column ID_COMPROBANTE_FISCAL
	 */
	protected static final int COLUMN_ID_COMPROBANTE_FISCAL = 4;

	/** 
	 * Number of columns
	 */
	protected static final int NUMBER_OF_COLUMNS = 4;

	/** 
	 * Index of primary-key column ID_DONATARIA
	 */
	protected static final int PK_COLUMN_ID_DONATARIA = 1;

	/** 
	 * Inserts a new row in the donatarias table.
	 */
	public DonatariasPk insert(Donatarias dto) throws DonatariasDaoException
	{
		long t1 = System.currentTimeMillis();
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		ResultSet rs = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			stmt = conn.prepareStatement( SQL_INSERT );
			int index = 1;
			stmt.setInt( index++, dto.getIdDonataria() );
			stmt.setString( index++, dto.getNoAutorizacion() );
			stmt.setDate(index++, dto.getFechaAutorizacion()==null ? null : new java.sql.Date( dto.getFechaAutorizacion().getTime() ) );
			stmt.setInt( index++, dto.getIdComprobanteFiscal() );
			System.out.println( "Executing " + SQL_INSERT + " with DTO: " + dto );
			int rows = stmt.executeUpdate();
			long t2 = System.currentTimeMillis();
			System.out.println( rows + " rows affected (" + (t2-t1) + " ms)" );
			reset(dto);
			return dto.createPk();
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new DonatariasDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Updates a single row in the donatarias table.
	 */
	public void update(DonatariasPk pk, Donatarias dto) throws DonatariasDaoException
	{
		long t1 = System.currentTimeMillis();
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			System.out.println( "Executing " + SQL_UPDATE + " with DTO: " + dto );
			stmt = conn.prepareStatement( SQL_UPDATE );
			int index=1;
			stmt.setInt( index++, dto.getIdDonataria() );
			stmt.setString( index++, dto.getNoAutorizacion() );
			stmt.setDate(index++, dto.getFechaAutorizacion()==null ? null : new java.sql.Date( dto.getFechaAutorizacion().getTime() ) );
			stmt.setInt( index++, dto.getIdComprobanteFiscal() );
			stmt.setInt( 5, pk.getIdDonataria() );
			int rows = stmt.executeUpdate();
			reset(dto);
			long t2 = System.currentTimeMillis();
			System.out.println( rows + " rows affected (" + (t2-t1) + " ms)" );
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new DonatariasDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Deletes a single row in the donatarias table.
	 */
	public void delete(DonatariasPk pk) throws DonatariasDaoException
	{
		long t1 = System.currentTimeMillis();
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			System.out.println( "Executing " + SQL_DELETE + " with PK: " + pk );
			stmt = conn.prepareStatement( SQL_DELETE );
			stmt.setInt( 1, pk.getIdDonataria() );
			int rows = stmt.executeUpdate();
			long t2 = System.currentTimeMillis();
			System.out.println( rows + " rows affected (" + (t2-t1) + " ms)" );
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new DonatariasDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Returns the rows from the donatarias table that matches the specified primary-key value.
	 */
	public Donatarias findByPrimaryKey(DonatariasPk pk) throws DonatariasDaoException
	{
		return findByPrimaryKey( pk.getIdDonataria() );
	}

	/** 
	 * Returns all rows from the donatarias table that match the criteria 'ID_DONATARIA = :idDonataria'.
	 */
	public Donatarias findByPrimaryKey(int idDonataria) throws DonatariasDaoException
	{
		Donatarias ret[] = findByDynamicSelect( SQL_SELECT + " WHERE ID_DONATARIA = ?", new Object[] {  new Integer(idDonataria) } );
		return ret.length==0 ? null : ret[0];
	}

	/** 
	 * Returns all rows from the donatarias table that match the criteria ''.
	 */
	public Donatarias[] findAll() throws DonatariasDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " ORDER BY ID_DONATARIA", null );
	}

	/** 
	 * Returns all rows from the donatarias table that match the criteria 'ID_DONATARIA = :idDonataria'.
	 */
	public Donatarias[] findWhereIdDonatariaEquals(int idDonataria) throws DonatariasDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE ID_DONATARIA = ? ORDER BY ID_DONATARIA", new Object[] {  new Integer(idDonataria) } );
	}

	/** 
	 * Returns all rows from the donatarias table that match the criteria 'NO_AUTORIZACION = :noAutorizacion'.
	 */
	public Donatarias[] findWhereNoAutorizacionEquals(String noAutorizacion) throws DonatariasDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE NO_AUTORIZACION = ? ORDER BY NO_AUTORIZACION", new Object[] { noAutorizacion } );
	}

	/** 
	 * Returns all rows from the donatarias table that match the criteria 'FECHA_AUTORIZACION = :fechaAutorizacion'.
	 */
	public Donatarias[] findWhereFechaAutorizacionEquals(Date fechaAutorizacion) throws DonatariasDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE FECHA_AUTORIZACION = ? ORDER BY FECHA_AUTORIZACION", new Object[] { fechaAutorizacion==null ? null : new java.sql.Date( fechaAutorizacion.getTime() ) } );
	}

	/** 
	 * Returns all rows from the donatarias table that match the criteria 'ID_COMPROBANTE_FISCAL = :idComprobanteFiscal'.
	 */
	public Donatarias[] findWhereIdComprobanteFiscalEquals(int idComprobanteFiscal) throws DonatariasDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE ID_COMPROBANTE_FISCAL = ? ORDER BY ID_COMPROBANTE_FISCAL", new Object[] {  new Integer(idComprobanteFiscal) } );
	}

	/**
	 * Method 'DonatariasDaoImpl'
	 * 
	 */
	public DonatariasDaoImpl()
	{
	}

	/**
	 * Method 'DonatariasDaoImpl'
	 * 
	 * @param userConn
	 */
	public DonatariasDaoImpl(final java.sql.Connection userConn)
	{
		this.userConn = userConn;
	}

	/** 
	 * Sets the value of maxRows
	 */
	public void setMaxRows(int maxRows)
	{
		this.maxRows = maxRows;
	}

	/** 
	 * Gets the value of maxRows
	 */
	public int getMaxRows()
	{
		return maxRows;
	}

	/**
	 * Method 'getTableName'
	 * 
	 * @return String
	 */
	public String getTableName()
	{
		return "DONATARIAS";
	}

	/** 
	 * Fetches a single row from the result set
	 */
	protected Donatarias fetchSingleResult(ResultSet rs) throws SQLException
	{
		if (rs.next()) {
			Donatarias dto = new Donatarias();
			populateDto( dto, rs);
			return dto;
		} else {
			return null;
		}
		
	}

	/** 
	 * Fetches multiple rows from the result set
	 */
	protected Donatarias[] fetchMultiResults(ResultSet rs) throws SQLException
	{
		Collection resultList = new ArrayList();
		while (rs.next()) {
			Donatarias dto = new Donatarias();
			populateDto( dto, rs);
			resultList.add( dto );
		}
		
		Donatarias ret[] = new Donatarias[ resultList.size() ];
		resultList.toArray( ret );
		return ret;
	}

	/** 
	 * Populates a DTO with data from a ResultSet
	 */
	protected void populateDto(Donatarias dto, ResultSet rs) throws SQLException
	{
		dto.setIdDonataria( rs.getInt( COLUMN_ID_DONATARIA ) );
		dto.setNoAutorizacion( rs.getString( COLUMN_NO_AUTORIZACION ) );
		dto.setFechaAutorizacion( rs.getDate(COLUMN_FECHA_AUTORIZACION ) );
		dto.setIdComprobanteFiscal( rs.getInt( COLUMN_ID_COMPROBANTE_FISCAL ) );
	}

	/** 
	 * Resets the modified attributes in the DTO
	 */
	protected void reset(Donatarias dto)
	{
	}

	/** 
	 * Returns all rows from the donatarias table that match the specified arbitrary SQL statement
	 */
	public Donatarias[] findByDynamicSelect(String sql, Object[] sqlParams) throws DonatariasDaoException
	{
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		ResultSet rs = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			// construct the SQL statement
			final String SQL = sql;
		
		
			System.out.println( "Executing " + SQL );
			// prepare statement
			stmt = conn.prepareStatement( SQL );
			stmt.setMaxRows( maxRows );
		
			// bind parameters
			for (int i=0; sqlParams!=null && i<sqlParams.length; i++ ) {
				stmt.setObject( i+1, sqlParams[i] );
			}
		
		
			rs = stmt.executeQuery();
		
			// fetch the results
			return fetchMultiResults(rs);
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new DonatariasDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(rs);
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Returns all rows from the donatarias table that match the specified arbitrary SQL statement
	 */
	public Donatarias[] findByDynamicWhere(String sql, Object[] sqlParams) throws DonatariasDaoException
	{
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		ResultSet rs = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			// construct the SQL statement
			final String SQL = SQL_SELECT + " WHERE " + sql;
		
		
			System.out.println( "Executing " + SQL );
			// prepare statement
			stmt = conn.prepareStatement( SQL );
			stmt.setMaxRows( maxRows );
		
			// bind parameters
			for (int i=0; sqlParams!=null && i<sqlParams.length; i++ ) {
				stmt.setObject( i+1, sqlParams[i] );
			}
		
		
			rs = stmt.executeQuery();
		
			// fetch the results
			return fetchMultiResults(rs);
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new DonatariasDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(rs);
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

}
