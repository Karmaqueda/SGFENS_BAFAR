/*
 * This source file was generated by FireStorm/DAO.
 * 
 * If you purchase a full license for FireStorm/DAO you can customize this header file.
 * 
 * For more information please visit http://www.codefutures.com/products/firestorm
 */

package com.tsp.sct.dao.jdbc;

import com.tsp.sct.dao.dao.*;
import com.tsp.sct.dao.factory.*;
import com.tsp.sct.dao.dto.*;
import com.tsp.sct.dao.exceptions.*;
import java.sql.Connection;
import java.util.Collection;
import java.sql.PreparedStatement;
import java.sql.Statement;
import java.sql.ResultSet;
import java.sql.SQLException;
import java.sql.Time;
import java.util.List;
import java.util.Iterator;
import java.util.ArrayList;

public class VentaDetalleDaoImpl extends AbstractDAO implements VentaDetalleDao
{
	/** 
	 * The factory class for this DAO has two versions of the create() method - one that
takes no arguments and one that takes a Connection argument. If the Connection version
is chosen then the connection will be stored in this attribute and will be used by all
calls to this DAO, otherwise a new Connection will be allocated for each operation.
	 */
	protected java.sql.Connection userConn;

	/** 
	 * All finder methods in this class use this SELECT constant to build their queries
	 */
	protected final String SQL_SELECT = "SELECT ID_VENTA, ID_CONCEPTO, CANTIDAD, DESCRIPCION, PRECIO_UNITARIO, UNIDAD, IDENTIFICACION FROM " + getTableName() + "";

	/** 
	 * Finder methods will pass this value to the JDBC setMaxRows method
	 */
	protected int maxRows;

	/** 
	 * SQL INSERT statement for this table
	 */
	protected final String SQL_INSERT = "INSERT INTO " + getTableName() + " ( ID_VENTA, ID_CONCEPTO, CANTIDAD, DESCRIPCION, PRECIO_UNITARIO, UNIDAD, IDENTIFICACION ) VALUES ( ?, ?, ?, ?, ?, ?, ? )";

	/** 
	 * Index of column ID_VENTA
	 */
	protected static final int COLUMN_ID_VENTA = 1;

	/** 
	 * Index of column ID_CONCEPTO
	 */
	protected static final int COLUMN_ID_CONCEPTO = 2;

	/** 
	 * Index of column CANTIDAD
	 */
	protected static final int COLUMN_CANTIDAD = 3;

	/** 
	 * Index of column DESCRIPCION
	 */
	protected static final int COLUMN_DESCRIPCION = 4;

	/** 
	 * Index of column PRECIO_UNITARIO
	 */
	protected static final int COLUMN_PRECIO_UNITARIO = 5;

	/** 
	 * Index of column UNIDAD
	 */
	protected static final int COLUMN_UNIDAD = 6;

	/** 
	 * Index of column IDENTIFICACION
	 */
	protected static final int COLUMN_IDENTIFICACION = 7;

	/** 
	 * Number of columns
	 */
	protected static final int NUMBER_OF_COLUMNS = 7;

	/** 
	 * Inserts a new row in the venta_detalle table.
	 */
	public void insert(VentaDetalle dto) throws VentaDetalleDaoException
	{
		long t1 = System.currentTimeMillis();
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		ResultSet rs = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			stmt = conn.prepareStatement( SQL_INSERT );
			int index = 1;
			stmt.setInt( index++, dto.getIdVenta() );
			stmt.setInt( index++, dto.getIdConcepto() );
			if (dto.isCantidadNull()) {
				stmt.setNull( index++, java.sql.Types.DOUBLE );
			} else {
				stmt.setDouble( index++, dto.getCantidad() );
			}
		
			stmt.setString( index++, dto.getDescripcion() );
			if (dto.isPrecioUnitarioNull()) {
				stmt.setNull( index++, java.sql.Types.DOUBLE );
			} else {
				stmt.setDouble( index++, dto.getPrecioUnitario() );
			}
		
			stmt.setString( index++, dto.getUnidad() );
			stmt.setString( index++, dto.getIdentificacion() );
			System.out.println( "Executing " + SQL_INSERT + " with DTO: " + dto );
			int rows = stmt.executeUpdate();
			long t2 = System.currentTimeMillis();
			System.out.println( rows + " rows affected (" + (t2-t1) + " ms)" );
			reset(dto);
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new VentaDetalleDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Returns all rows from the venta_detalle table that match the criteria ''.
	 */
	public VentaDetalle[] findAll() throws VentaDetalleDaoException
	{
		return findByDynamicSelect( SQL_SELECT, null );
	}

	/** 
	 * Returns all rows from the venta_detalle table that match the criteria 'ID_VENTA = :idVenta'.
	 */
	public VentaDetalle[] findWhereIdVentaEquals(int idVenta) throws VentaDetalleDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE ID_VENTA = ? ORDER BY ID_VENTA", new Object[] {  new Integer(idVenta) } );
	}

	/** 
	 * Returns all rows from the venta_detalle table that match the criteria 'ID_CONCEPTO = :idConcepto'.
	 */
	public VentaDetalle[] findWhereIdConceptoEquals(int idConcepto) throws VentaDetalleDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE ID_CONCEPTO = ? ORDER BY ID_CONCEPTO", new Object[] {  new Integer(idConcepto) } );
	}

	/** 
	 * Returns all rows from the venta_detalle table that match the criteria 'CANTIDAD = :cantidad'.
	 */
	public VentaDetalle[] findWhereCantidadEquals(double cantidad) throws VentaDetalleDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE CANTIDAD = ? ORDER BY CANTIDAD", new Object[] {  new Double(cantidad) } );
	}

	/** 
	 * Returns all rows from the venta_detalle table that match the criteria 'DESCRIPCION = :descripcion'.
	 */
	public VentaDetalle[] findWhereDescripcionEquals(String descripcion) throws VentaDetalleDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE DESCRIPCION = ? ORDER BY DESCRIPCION", new Object[] { descripcion } );
	}

	/** 
	 * Returns all rows from the venta_detalle table that match the criteria 'PRECIO_UNITARIO = :precioUnitario'.
	 */
	public VentaDetalle[] findWherePrecioUnitarioEquals(double precioUnitario) throws VentaDetalleDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE PRECIO_UNITARIO = ? ORDER BY PRECIO_UNITARIO", new Object[] {  new Double(precioUnitario) } );
	}

	/** 
	 * Returns all rows from the venta_detalle table that match the criteria 'UNIDAD = :unidad'.
	 */
	public VentaDetalle[] findWhereUnidadEquals(String unidad) throws VentaDetalleDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE UNIDAD = ? ORDER BY UNIDAD", new Object[] { unidad } );
	}

	/** 
	 * Returns all rows from the venta_detalle table that match the criteria 'IDENTIFICACION = :identificacion'.
	 */
	public VentaDetalle[] findWhereIdentificacionEquals(String identificacion) throws VentaDetalleDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE IDENTIFICACION = ? ORDER BY IDENTIFICACION", new Object[] { identificacion } );
	}

	/**
	 * Method 'VentaDetalleDaoImpl'
	 * 
	 */
	public VentaDetalleDaoImpl()
	{
	}

	/**
	 * Method 'VentaDetalleDaoImpl'
	 * 
	 * @param userConn
	 */
	public VentaDetalleDaoImpl(final java.sql.Connection userConn)
	{
		this.userConn = userConn;
	}

	/** 
	 * Sets the value of maxRows
	 */
	public void setMaxRows(int maxRows)
	{
		this.maxRows = maxRows;
	}

	/** 
	 * Gets the value of maxRows
	 */
	public int getMaxRows()
	{
		return maxRows;
	}

	/**
	 * Method 'getTableName'
	 * 
	 * @return String
	 */
	public String getTableName()
	{
		return "VENTA_DETALLE";
	}

	/** 
	 * Fetches a single row from the result set
	 */
	protected VentaDetalle fetchSingleResult(ResultSet rs) throws SQLException
	{
		if (rs.next()) {
			VentaDetalle dto = new VentaDetalle();
			populateDto( dto, rs);
			return dto;
		} else {
			return null;
		}
		
	}

	/** 
	 * Fetches multiple rows from the result set
	 */
	protected VentaDetalle[] fetchMultiResults(ResultSet rs) throws SQLException
	{
		Collection resultList = new ArrayList();
		while (rs.next()) {
			VentaDetalle dto = new VentaDetalle();
			populateDto( dto, rs);
			resultList.add( dto );
		}
		
		VentaDetalle ret[] = new VentaDetalle[ resultList.size() ];
		resultList.toArray( ret );
		return ret;
	}

	/** 
	 * Populates a DTO with data from a ResultSet
	 */
	protected void populateDto(VentaDetalle dto, ResultSet rs) throws SQLException
	{
		dto.setIdVenta( rs.getInt( COLUMN_ID_VENTA ) );
		dto.setIdConcepto( rs.getInt( COLUMN_ID_CONCEPTO ) );
		dto.setCantidad( rs.getDouble( COLUMN_CANTIDAD ) );
		if (rs.wasNull()) {
			dto.setCantidadNull( true );
		}
		
		dto.setDescripcion( rs.getString( COLUMN_DESCRIPCION ) );
		dto.setPrecioUnitario( rs.getDouble( COLUMN_PRECIO_UNITARIO ) );
		if (rs.wasNull()) {
			dto.setPrecioUnitarioNull( true );
		}
		
		dto.setUnidad( rs.getString( COLUMN_UNIDAD ) );
		dto.setIdentificacion( rs.getString( COLUMN_IDENTIFICACION ) );
	}

	/** 
	 * Resets the modified attributes in the DTO
	 */
	protected void reset(VentaDetalle dto)
	{
	}

	/** 
	 * Returns all rows from the venta_detalle table that match the specified arbitrary SQL statement
	 */
	public VentaDetalle[] findByDynamicSelect(String sql, Object[] sqlParams) throws VentaDetalleDaoException
	{
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		ResultSet rs = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			// construct the SQL statement
			final String SQL = sql;
		
		
			System.out.println( "Executing " + SQL );
			// prepare statement
			stmt = conn.prepareStatement( SQL );
			stmt.setMaxRows( maxRows );
		
			// bind parameters
			for (int i=0; sqlParams!=null && i<sqlParams.length; i++ ) {
				stmt.setObject( i+1, sqlParams[i] );
			}
		
		
			rs = stmt.executeQuery();
		
			// fetch the results
			return fetchMultiResults(rs);
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new VentaDetalleDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(rs);
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Returns all rows from the venta_detalle table that match the specified arbitrary SQL statement
	 */
	public VentaDetalle[] findByDynamicWhere(String sql, Object[] sqlParams) throws VentaDetalleDaoException
	{
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		ResultSet rs = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			// construct the SQL statement
			final String SQL = SQL_SELECT + " WHERE " + sql;
		
		
			System.out.println( "Executing " + SQL );
			// prepare statement
			stmt = conn.prepareStatement( SQL );
			stmt.setMaxRows( maxRows );
		
			// bind parameters
			for (int i=0; sqlParams!=null && i<sqlParams.length; i++ ) {
				stmt.setObject( i+1, sqlParams[i] );
			}
		
		
			rs = stmt.executeQuery();
		
			// fetch the results
			return fetchMultiResults(rs);
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new VentaDetalleDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(rs);
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

}
