/*
 * This source file was generated by FireStorm/DAO.
 * 
 * If you purchase a full license for FireStorm/DAO you can customize this header file.
 * 
 * For more information please visit http://www.codefutures.com/products/firestorm
 */

package com.tsp.sct.dao.jdbc;

import com.tsp.sct.dao.dao.*;
import com.tsp.sct.dao.factory.*;
import com.tsp.sct.dao.dto.*;
import com.tsp.sct.dao.exceptions.*;
import java.sql.Connection;
import java.util.Collection;
import java.sql.PreparedStatement;
import java.sql.Statement;
import java.sql.ResultSet;
import java.sql.SQLException;
import java.sql.Time;
import java.util.List;
import java.util.Iterator;
import java.util.ArrayList;

public class DatosAccesoPagoLineaDaoImpl extends AbstractDAO implements DatosAccesoPagoLineaDao
{
	/** 
	 * The factory class for this DAO has two versions of the create() method - one that
takes no arguments and one that takes a Connection argument. If the Connection version
is chosen then the connection will be stored in this attribute and will be used by all
calls to this DAO, otherwise a new Connection will be allocated for each operation.
	 */
	protected java.sql.Connection userConn;

	/** 
	 * All finder methods in this class use this SELECT constant to build their queries
	 */
	protected final String SQL_SELECT = "SELECT ID_DATOS_ACCESO, ID_EMPRESA, USUARIO, PASS, PIN, ID_USUARIOS FROM " + getTableName() + "";

	/** 
	 * Finder methods will pass this value to the JDBC setMaxRows method
	 */
	protected int maxRows;

	/** 
	 * SQL INSERT statement for this table
	 */
	protected final String SQL_INSERT = "INSERT INTO " + getTableName() + " ( ID_DATOS_ACCESO, ID_EMPRESA, USUARIO, PASS, PIN, ID_USUARIOS ) VALUES ( ?, ?, ?, ?, ?, ? )";

	/** 
	 * SQL UPDATE statement for this table
	 */
	protected final String SQL_UPDATE = "UPDATE " + getTableName() + " SET ID_DATOS_ACCESO = ?, ID_EMPRESA = ?, USUARIO = ?, PASS = ?, PIN = ?, ID_USUARIOS = ? WHERE ID_DATOS_ACCESO = ?";

	/** 
	 * SQL DELETE statement for this table
	 */
	protected final String SQL_DELETE = "DELETE FROM " + getTableName() + " WHERE ID_DATOS_ACCESO = ?";

	/** 
	 * Index of column ID_DATOS_ACCESO
	 */
	protected static final int COLUMN_ID_DATOS_ACCESO = 1;

	/** 
	 * Index of column ID_EMPRESA
	 */
	protected static final int COLUMN_ID_EMPRESA = 2;

	/** 
	 * Index of column USUARIO
	 */
	protected static final int COLUMN_USUARIO = 3;

	/** 
	 * Index of column PASS
	 */
	protected static final int COLUMN_PASS = 4;

	/** 
	 * Index of column PIN
	 */
	protected static final int COLUMN_PIN = 5;

	/** 
	 * Index of column ID_USUARIOS
	 */
	protected static final int COLUMN_ID_USUARIOS = 6;

	/** 
	 * Number of columns
	 */
	protected static final int NUMBER_OF_COLUMNS = 6;

	/** 
	 * Index of primary-key column ID_DATOS_ACCESO
	 */
	protected static final int PK_COLUMN_ID_DATOS_ACCESO = 1;

	/** 
	 * Inserts a new row in the datos_acceso_pago_linea table.
	 */
	public DatosAccesoPagoLineaPk insert(DatosAccesoPagoLinea dto) throws DatosAccesoPagoLineaDaoException
	{
		long t1 = System.currentTimeMillis();
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		ResultSet rs = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			StringBuffer sql = new StringBuffer();
			StringBuffer values = new StringBuffer();
			sql.append( "INSERT INTO " + getTableName() + " (" );
			int modifiedCount = 0;
			if (dto.isIdDatosAccesoModified()) {
				if (modifiedCount>0) {
					sql.append( ", " );
					values.append( ", " );
				}
		
				sql.append( "ID_DATOS_ACCESO" );
				values.append( "?" );
				modifiedCount++;
			}
		
			if (dto.isIdEmpresaModified()) {
				if (modifiedCount>0) {
					sql.append( ", " );
					values.append( ", " );
				}
		
				sql.append( "ID_EMPRESA" );
				values.append( "?" );
				modifiedCount++;
			}
		
			if (dto.isUsuarioModified()) {
				if (modifiedCount>0) {
					sql.append( ", " );
					values.append( ", " );
				}
		
				sql.append( "USUARIO" );
				values.append( "?" );
				modifiedCount++;
			}
		
			if (dto.isPassModified()) {
				if (modifiedCount>0) {
					sql.append( ", " );
					values.append( ", " );
				}
		
				sql.append( "PASS" );
				values.append( "?" );
				modifiedCount++;
			}
		
			if (dto.isPinModified()) {
				if (modifiedCount>0) {
					sql.append( ", " );
					values.append( ", " );
				}
		
				sql.append( "PIN" );
				values.append( "?" );
				modifiedCount++;
			}
		
			if (dto.isIdUsuariosModified()) {
				if (modifiedCount>0) {
					sql.append( ", " );
					values.append( ", " );
				}
		
				sql.append( "ID_USUARIOS" );
				values.append( "?" );
				modifiedCount++;
			}
		
			if (modifiedCount==0) {
				// nothing to insert
				throw new IllegalStateException( "Nothing to insert" );
			}
		
			sql.append( ") VALUES (" );
			sql.append( values );
			sql.append( ")" );
			stmt = conn.prepareStatement( sql.toString(), Statement.RETURN_GENERATED_KEYS );
			int index = 1;
			if (dto.isIdDatosAccesoModified()) {
				stmt.setInt( index++, dto.getIdDatosAcceso() );
			}
		
			if (dto.isIdEmpresaModified()) {
				stmt.setInt( index++, dto.getIdEmpresa() );
			}
		
			if (dto.isUsuarioModified()) {
				stmt.setString( index++, dto.getUsuario() );
			}
		
			if (dto.isPassModified()) {
				stmt.setString( index++, dto.getPass() );
			}
		
			if (dto.isPinModified()) {
				stmt.setString( index++, dto.getPin() );
			}
		
			if (dto.isIdUsuariosModified()) {
				stmt.setInt( index++, dto.getIdUsuarios() );
			}
		
			System.out.println( "Executing " + sql.toString() + " with values: " + dto );
			int rows = stmt.executeUpdate();
			long t2 = System.currentTimeMillis();
			System.out.println( rows + " rows affected (" + (t2-t1) + " ms)" );
		
			// retrieve values from auto-increment columns
			rs = stmt.getGeneratedKeys();
			if (rs != null && rs.next()) {
				dto.setIdDatosAcceso( rs.getInt( 1 ) );
			}
		
			reset(dto);
			return dto.createPk();
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new DatosAccesoPagoLineaDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Updates a single row in the datos_acceso_pago_linea table.
	 */
	public void update(DatosAccesoPagoLineaPk pk, DatosAccesoPagoLinea dto) throws DatosAccesoPagoLineaDaoException
	{
		long t1 = System.currentTimeMillis();
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			StringBuffer sql = new StringBuffer();
			sql.append( "UPDATE " + getTableName() + " SET " );
			boolean modified = false;
			if (dto.isIdDatosAccesoModified()) {
				if (modified) {
					sql.append( ", " );
				}
		
				sql.append( "ID_DATOS_ACCESO=?" );
				modified=true;
			}
		
			if (dto.isIdEmpresaModified()) {
				if (modified) {
					sql.append( ", " );
				}
		
				sql.append( "ID_EMPRESA=?" );
				modified=true;
			}
		
			if (dto.isUsuarioModified()) {
				if (modified) {
					sql.append( ", " );
				}
		
				sql.append( "USUARIO=?" );
				modified=true;
			}
		
			if (dto.isPassModified()) {
				if (modified) {
					sql.append( ", " );
				}
		
				sql.append( "PASS=?" );
				modified=true;
			}
		
			if (dto.isPinModified()) {
				if (modified) {
					sql.append( ", " );
				}
		
				sql.append( "PIN=?" );
				modified=true;
			}
		
			if (dto.isIdUsuariosModified()) {
				if (modified) {
					sql.append( ", " );
				}
		
				sql.append( "ID_USUARIOS=?" );
				modified=true;
			}
		
			if (!modified) {
				// nothing to update
				return;
			}
		
			sql.append( " WHERE ID_DATOS_ACCESO=?" );
			System.out.println( "Executing " + sql.toString() + " with values: " + dto );
			stmt = conn.prepareStatement( sql.toString() );
			int index = 1;
			if (dto.isIdDatosAccesoModified()) {
				stmt.setInt( index++, dto.getIdDatosAcceso() );
			}
		
			if (dto.isIdEmpresaModified()) {
				stmt.setInt( index++, dto.getIdEmpresa() );
			}
		
			if (dto.isUsuarioModified()) {
				stmt.setString( index++, dto.getUsuario() );
			}
		
			if (dto.isPassModified()) {
				stmt.setString( index++, dto.getPass() );
			}
		
			if (dto.isPinModified()) {
				stmt.setString( index++, dto.getPin() );
			}
		
			if (dto.isIdUsuariosModified()) {
				stmt.setInt( index++, dto.getIdUsuarios() );
			}
		
			stmt.setInt( index++, pk.getIdDatosAcceso() );
			int rows = stmt.executeUpdate();
			reset(dto);
			long t2 = System.currentTimeMillis();
			System.out.println( rows + " rows affected (" + (t2-t1) + " ms)" );
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new DatosAccesoPagoLineaDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Deletes a single row in the datos_acceso_pago_linea table.
	 */
	public void delete(DatosAccesoPagoLineaPk pk) throws DatosAccesoPagoLineaDaoException
	{
		long t1 = System.currentTimeMillis();
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			System.out.println( "Executing " + SQL_DELETE + " with PK: " + pk );
			stmt = conn.prepareStatement( SQL_DELETE );
			stmt.setInt( 1, pk.getIdDatosAcceso() );
			int rows = stmt.executeUpdate();
			long t2 = System.currentTimeMillis();
			System.out.println( rows + " rows affected (" + (t2-t1) + " ms)" );
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new DatosAccesoPagoLineaDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Returns the rows from the datos_acceso_pago_linea table that matches the specified primary-key value.
	 */
	public DatosAccesoPagoLinea findByPrimaryKey(DatosAccesoPagoLineaPk pk) throws DatosAccesoPagoLineaDaoException
	{
		return findByPrimaryKey( pk.getIdDatosAcceso() );
	}

	/** 
	 * Returns all rows from the datos_acceso_pago_linea table that match the criteria 'ID_DATOS_ACCESO = :idDatosAcceso'.
	 */
	public DatosAccesoPagoLinea findByPrimaryKey(int idDatosAcceso) throws DatosAccesoPagoLineaDaoException
	{
		DatosAccesoPagoLinea ret[] = findByDynamicSelect( SQL_SELECT + " WHERE ID_DATOS_ACCESO = ?", new Object[] {  new Integer(idDatosAcceso) } );
		return ret.length==0 ? null : ret[0];
	}

	/** 
	 * Returns all rows from the datos_acceso_pago_linea table that match the criteria ''.
	 */
	public DatosAccesoPagoLinea[] findAll() throws DatosAccesoPagoLineaDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " ORDER BY ID_DATOS_ACCESO", null );
	}

	/** 
	 * Returns all rows from the datos_acceso_pago_linea table that match the criteria 'ID_DATOS_ACCESO = :idDatosAcceso'.
	 */
	public DatosAccesoPagoLinea[] findWhereIdDatosAccesoEquals(int idDatosAcceso) throws DatosAccesoPagoLineaDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE ID_DATOS_ACCESO = ? ORDER BY ID_DATOS_ACCESO", new Object[] {  new Integer(idDatosAcceso) } );
	}

	/** 
	 * Returns all rows from the datos_acceso_pago_linea table that match the criteria 'ID_EMPRESA = :idEmpresa'.
	 */
	public DatosAccesoPagoLinea[] findWhereIdEmpresaEquals(int idEmpresa) throws DatosAccesoPagoLineaDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE ID_EMPRESA = ? ORDER BY ID_EMPRESA", new Object[] {  new Integer(idEmpresa) } );
	}

	/** 
	 * Returns all rows from the datos_acceso_pago_linea table that match the criteria 'USUARIO = :usuario'.
	 */
	public DatosAccesoPagoLinea[] findWhereUsuarioEquals(String usuario) throws DatosAccesoPagoLineaDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE USUARIO = ? ORDER BY USUARIO", new Object[] { usuario } );
	}

	/** 
	 * Returns all rows from the datos_acceso_pago_linea table that match the criteria 'PASS = :pass'.
	 */
	public DatosAccesoPagoLinea[] findWherePassEquals(String pass) throws DatosAccesoPagoLineaDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE PASS = ? ORDER BY PASS", new Object[] { pass } );
	}

	/** 
	 * Returns all rows from the datos_acceso_pago_linea table that match the criteria 'PIN = :pin'.
	 */
	public DatosAccesoPagoLinea[] findWherePinEquals(String pin) throws DatosAccesoPagoLineaDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE PIN = ? ORDER BY PIN", new Object[] { pin } );
	}

	/** 
	 * Returns all rows from the datos_acceso_pago_linea table that match the criteria 'ID_USUARIOS = :idUsuarios'.
	 */
	public DatosAccesoPagoLinea[] findWhereIdUsuariosEquals(int idUsuarios) throws DatosAccesoPagoLineaDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE ID_USUARIOS = ? ORDER BY ID_USUARIOS", new Object[] {  new Integer(idUsuarios) } );
	}

	/**
	 * Method 'DatosAccesoPagoLineaDaoImpl'
	 * 
	 */
	public DatosAccesoPagoLineaDaoImpl()
	{
	}

	/**
	 * Method 'DatosAccesoPagoLineaDaoImpl'
	 * 
	 * @param userConn
	 */
	public DatosAccesoPagoLineaDaoImpl(final java.sql.Connection userConn)
	{
		this.userConn = userConn;
	}

	/** 
	 * Sets the value of maxRows
	 */
	public void setMaxRows(int maxRows)
	{
		this.maxRows = maxRows;
	}

	/** 
	 * Gets the value of maxRows
	 */
	public int getMaxRows()
	{
		return maxRows;
	}

	/**
	 * Method 'getTableName'
	 * 
	 * @return String
	 */
	public String getTableName()
	{
		return "DATOS_ACCESO_PAGO_LINEA";
	}

	/** 
	 * Fetches a single row from the result set
	 */
	protected DatosAccesoPagoLinea fetchSingleResult(ResultSet rs) throws SQLException
	{
		if (rs.next()) {
			DatosAccesoPagoLinea dto = new DatosAccesoPagoLinea();
			populateDto( dto, rs);
			return dto;
		} else {
			return null;
		}
		
	}

	/** 
	 * Fetches multiple rows from the result set
	 */
	protected DatosAccesoPagoLinea[] fetchMultiResults(ResultSet rs) throws SQLException
	{
		Collection resultList = new ArrayList();
		while (rs.next()) {
			DatosAccesoPagoLinea dto = new DatosAccesoPagoLinea();
			populateDto( dto, rs);
			resultList.add( dto );
		}
		
		DatosAccesoPagoLinea ret[] = new DatosAccesoPagoLinea[ resultList.size() ];
		resultList.toArray( ret );
		return ret;
	}

	/** 
	 * Populates a DTO with data from a ResultSet
	 */
	protected void populateDto(DatosAccesoPagoLinea dto, ResultSet rs) throws SQLException
	{
		dto.setIdDatosAcceso( rs.getInt( COLUMN_ID_DATOS_ACCESO ) );
		dto.setIdEmpresa( rs.getInt( COLUMN_ID_EMPRESA ) );
		dto.setUsuario( rs.getString( COLUMN_USUARIO ) );
		dto.setPass( rs.getString( COLUMN_PASS ) );
		dto.setPin( rs.getString( COLUMN_PIN ) );
		dto.setIdUsuarios( rs.getInt( COLUMN_ID_USUARIOS ) );
		reset(dto);
	}

	/** 
	 * Resets the modified attributes in the DTO
	 */
	protected void reset(DatosAccesoPagoLinea dto)
	{
		dto.setIdDatosAccesoModified( false );
		dto.setIdEmpresaModified( false );
		dto.setUsuarioModified( false );
		dto.setPassModified( false );
		dto.setPinModified( false );
		dto.setIdUsuariosModified( false );
	}

	/** 
	 * Returns all rows from the datos_acceso_pago_linea table that match the specified arbitrary SQL statement
	 */
	public DatosAccesoPagoLinea[] findByDynamicSelect(String sql, Object[] sqlParams) throws DatosAccesoPagoLineaDaoException
	{
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		ResultSet rs = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			// construct the SQL statement
			final String SQL = sql;
		
		
			System.out.println( "Executing " + SQL );
			// prepare statement
			stmt = conn.prepareStatement( SQL );
			stmt.setMaxRows( maxRows );
		
			// bind parameters
			for (int i=0; sqlParams!=null && i<sqlParams.length; i++ ) {
				stmt.setObject( i+1, sqlParams[i] );
			}
		
		
			rs = stmt.executeQuery();
		
			// fetch the results
			return fetchMultiResults(rs);
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new DatosAccesoPagoLineaDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(rs);
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Returns all rows from the datos_acceso_pago_linea table that match the specified arbitrary SQL statement
	 */
	public DatosAccesoPagoLinea[] findByDynamicWhere(String sql, Object[] sqlParams) throws DatosAccesoPagoLineaDaoException
	{
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		ResultSet rs = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			// construct the SQL statement
			final String SQL = SQL_SELECT + " WHERE " + sql;
		
		
			System.out.println( "Executing " + SQL );
			// prepare statement
			stmt = conn.prepareStatement( SQL );
			stmt.setMaxRows( maxRows );
		
			// bind parameters
			for (int i=0; sqlParams!=null && i<sqlParams.length; i++ ) {
				stmt.setObject( i+1, sqlParams[i] );
			}
		
		
			rs = stmt.executeQuery();
		
			// fetch the results
			return fetchMultiResults(rs);
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new DatosAccesoPagoLineaDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(rs);
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

}
