/*
 * This source file was generated by FireStorm/DAO.
 * 
 * If you purchase a full license for FireStorm/DAO you can customize this header file.
 * 
 * For more information please visit http://www.codefutures.com/products/firestorm
 */

package com.tsp.sct.dao.jdbc;

import com.tsp.sct.dao.dao.*;
import com.tsp.sct.dao.factory.*;
import com.tsp.sct.dao.dto.*;
import com.tsp.sct.dao.exceptions.*;
import java.sql.Connection;
import java.util.Collection;
import java.sql.PreparedStatement;
import java.sql.Statement;
import java.sql.ResultSet;
import java.sql.SQLException;
import java.sql.Time;
import java.util.List;
import java.util.Iterator;
import java.util.ArrayList;

public class TipoPagoDaoImpl extends AbstractDAO implements TipoPagoDao
{
	/** 
	 * The factory class for this DAO has two versions of the create() method - one that
takes no arguments and one that takes a Connection argument. If the Connection version
is chosen then the connection will be stored in this attribute and will be used by all
calls to this DAO, otherwise a new Connection will be allocated for each operation.
	 */
	protected java.sql.Connection userConn;

	/** 
	 * All finder methods in this class use this SELECT constant to build their queries
	 */
	protected final String SQL_SELECT = "SELECT ID_TIPO_PAGO, DESC_TIPO_PAGO, NUMERO_CUENTA, CLAVE_METODO_SAT FROM " + getTableName() + "";

	/** 
	 * Finder methods will pass this value to the JDBC setMaxRows method
	 */
	protected int maxRows;

	/** 
	 * SQL INSERT statement for this table
	 */
	protected final String SQL_INSERT = "INSERT INTO " + getTableName() + " ( ID_TIPO_PAGO, DESC_TIPO_PAGO, NUMERO_CUENTA, CLAVE_METODO_SAT ) VALUES ( ?, ?, ?, ? )";

	/** 
	 * SQL UPDATE statement for this table
	 */
	protected final String SQL_UPDATE = "UPDATE " + getTableName() + " SET ID_TIPO_PAGO = ?, DESC_TIPO_PAGO = ?, NUMERO_CUENTA = ?, CLAVE_METODO_SAT = ? WHERE ID_TIPO_PAGO = ?";

	/** 
	 * SQL DELETE statement for this table
	 */
	protected final String SQL_DELETE = "DELETE FROM " + getTableName() + " WHERE ID_TIPO_PAGO = ?";

	/** 
	 * Index of column ID_TIPO_PAGO
	 */
	protected static final int COLUMN_ID_TIPO_PAGO = 1;

	/** 
	 * Index of column DESC_TIPO_PAGO
	 */
	protected static final int COLUMN_DESC_TIPO_PAGO = 2;

	/** 
	 * Index of column NUMERO_CUENTA
	 */
	protected static final int COLUMN_NUMERO_CUENTA = 3;

	/** 
	 * Index of column CLAVE_METODO_SAT
	 */
	protected static final int COLUMN_CLAVE_METODO_SAT = 4;

	/** 
	 * Number of columns
	 */
	protected static final int NUMBER_OF_COLUMNS = 4;

	/** 
	 * Index of primary-key column ID_TIPO_PAGO
	 */
	protected static final int PK_COLUMN_ID_TIPO_PAGO = 1;

	/** 
	 * Inserts a new row in the tipo_pago table.
	 */
	public TipoPagoPk insert(TipoPago dto) throws TipoPagoDaoException
	{
		long t1 = System.currentTimeMillis();
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		ResultSet rs = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			StringBuffer sql = new StringBuffer();
			StringBuffer values = new StringBuffer();
			sql.append( "INSERT INTO " + getTableName() + " (" );
			int modifiedCount = 0;
			if (dto.isIdTipoPagoModified()) {
				if (modifiedCount>0) {
					sql.append( ", " );
					values.append( ", " );
				}
		
				sql.append( "ID_TIPO_PAGO" );
				values.append( "?" );
				modifiedCount++;
			}
		
			if (dto.isDescTipoPagoModified()) {
				if (modifiedCount>0) {
					sql.append( ", " );
					values.append( ", " );
				}
		
				sql.append( "DESC_TIPO_PAGO" );
				values.append( "?" );
				modifiedCount++;
			}
		
			if (dto.isNumeroCuentaModified()) {
				if (modifiedCount>0) {
					sql.append( ", " );
					values.append( ", " );
				}
		
				sql.append( "NUMERO_CUENTA" );
				values.append( "?" );
				modifiedCount++;
			}
		
			if (dto.isClaveMetodoSatModified()) {
				if (modifiedCount>0) {
					sql.append( ", " );
					values.append( ", " );
				}
		
				sql.append( "CLAVE_METODO_SAT" );
				values.append( "?" );
				modifiedCount++;
			}
		
			if (modifiedCount==0) {
				// nothing to insert
				throw new IllegalStateException( "Nothing to insert" );
			}
		
			sql.append( ") VALUES (" );
			sql.append( values );
			sql.append( ")" );
			stmt = conn.prepareStatement( sql.toString() );
			int index = 1;
			if (dto.isIdTipoPagoModified()) {
				stmt.setInt( index++, dto.getIdTipoPago() );
			}
		
			if (dto.isDescTipoPagoModified()) {
				stmt.setString( index++, dto.getDescTipoPago() );
			}
		
			if (dto.isNumeroCuentaModified()) {
				stmt.setString( index++, dto.getNumeroCuenta() );
			}
		
			if (dto.isClaveMetodoSatModified()) {
				stmt.setString( index++, dto.getClaveMetodoSat() );
			}
		
			System.out.println( "Executing " + sql.toString() + " with values: " + dto );
			int rows = stmt.executeUpdate();
			long t2 = System.currentTimeMillis();
			System.out.println( rows + " rows affected (" + (t2-t1) + " ms)" );
			reset(dto);
			return dto.createPk();
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new TipoPagoDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Updates a single row in the tipo_pago table.
	 */
	public void update(TipoPagoPk pk, TipoPago dto) throws TipoPagoDaoException
	{
		long t1 = System.currentTimeMillis();
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			StringBuffer sql = new StringBuffer();
			sql.append( "UPDATE " + getTableName() + " SET " );
			boolean modified = false;
			if (dto.isIdTipoPagoModified()) {
				if (modified) {
					sql.append( ", " );
				}
		
				sql.append( "ID_TIPO_PAGO=?" );
				modified=true;
			}
		
			if (dto.isDescTipoPagoModified()) {
				if (modified) {
					sql.append( ", " );
				}
		
				sql.append( "DESC_TIPO_PAGO=?" );
				modified=true;
			}
		
			if (dto.isNumeroCuentaModified()) {
				if (modified) {
					sql.append( ", " );
				}
		
				sql.append( "NUMERO_CUENTA=?" );
				modified=true;
			}
		
			if (dto.isClaveMetodoSatModified()) {
				if (modified) {
					sql.append( ", " );
				}
		
				sql.append( "CLAVE_METODO_SAT=?" );
				modified=true;
			}
		
			if (!modified) {
				// nothing to update
				return;
			}
		
			sql.append( " WHERE ID_TIPO_PAGO=?" );
			System.out.println( "Executing " + sql.toString() + " with values: " + dto );
			stmt = conn.prepareStatement( sql.toString() );
			int index = 1;
			if (dto.isIdTipoPagoModified()) {
				stmt.setInt( index++, dto.getIdTipoPago() );
			}
		
			if (dto.isDescTipoPagoModified()) {
				stmt.setString( index++, dto.getDescTipoPago() );
			}
		
			if (dto.isNumeroCuentaModified()) {
				stmt.setString( index++, dto.getNumeroCuenta() );
			}
		
			if (dto.isClaveMetodoSatModified()) {
				stmt.setString( index++, dto.getClaveMetodoSat() );
			}
		
			stmt.setInt( index++, pk.getIdTipoPago() );
			int rows = stmt.executeUpdate();
			reset(dto);
			long t2 = System.currentTimeMillis();
			System.out.println( rows + " rows affected (" + (t2-t1) + " ms)" );
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new TipoPagoDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Deletes a single row in the tipo_pago table.
	 */
	public void delete(TipoPagoPk pk) throws TipoPagoDaoException
	{
		long t1 = System.currentTimeMillis();
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			System.out.println( "Executing " + SQL_DELETE + " with PK: " + pk );
			stmt = conn.prepareStatement( SQL_DELETE );
			stmt.setInt( 1, pk.getIdTipoPago() );
			int rows = stmt.executeUpdate();
			long t2 = System.currentTimeMillis();
			System.out.println( rows + " rows affected (" + (t2-t1) + " ms)" );
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new TipoPagoDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Returns the rows from the tipo_pago table that matches the specified primary-key value.
	 */
	public TipoPago findByPrimaryKey(TipoPagoPk pk) throws TipoPagoDaoException
	{
		return findByPrimaryKey( pk.getIdTipoPago() );
	}

	/** 
	 * Returns all rows from the tipo_pago table that match the criteria 'ID_TIPO_PAGO = :idTipoPago'.
	 */
	public TipoPago findByPrimaryKey(int idTipoPago) throws TipoPagoDaoException
	{
		TipoPago ret[] = findByDynamicSelect( SQL_SELECT + " WHERE ID_TIPO_PAGO = ?", new Object[] {  new Integer(idTipoPago) } );
		return ret.length==0 ? null : ret[0];
	}

	/** 
	 * Returns all rows from the tipo_pago table that match the criteria ''.
	 */
	public TipoPago[] findAll() throws TipoPagoDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " ORDER BY ID_TIPO_PAGO", null );
	}

	/** 
	 * Returns all rows from the tipo_pago table that match the criteria 'ID_TIPO_PAGO = :idTipoPago'.
	 */
	public TipoPago[] findWhereIdTipoPagoEquals(int idTipoPago) throws TipoPagoDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE ID_TIPO_PAGO = ? ORDER BY ID_TIPO_PAGO", new Object[] {  new Integer(idTipoPago) } );
	}

	/** 
	 * Returns all rows from the tipo_pago table that match the criteria 'DESC_TIPO_PAGO = :descTipoPago'.
	 */
	public TipoPago[] findWhereDescTipoPagoEquals(String descTipoPago) throws TipoPagoDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE DESC_TIPO_PAGO = ? ORDER BY DESC_TIPO_PAGO", new Object[] { descTipoPago } );
	}

	/** 
	 * Returns all rows from the tipo_pago table that match the criteria 'NUMERO_CUENTA = :numeroCuenta'.
	 */
	public TipoPago[] findWhereNumeroCuentaEquals(String numeroCuenta) throws TipoPagoDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE NUMERO_CUENTA = ? ORDER BY NUMERO_CUENTA", new Object[] { numeroCuenta } );
	}

	/** 
	 * Returns all rows from the tipo_pago table that match the criteria 'CLAVE_METODO_SAT = :claveMetodoSat'.
	 */
	public TipoPago[] findWhereClaveMetodoSatEquals(String claveMetodoSat) throws TipoPagoDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE CLAVE_METODO_SAT = ? ORDER BY CLAVE_METODO_SAT", new Object[] { claveMetodoSat } );
	}

	/**
	 * Method 'TipoPagoDaoImpl'
	 * 
	 */
	public TipoPagoDaoImpl()
	{
	}

	/**
	 * Method 'TipoPagoDaoImpl'
	 * 
	 * @param userConn
	 */
	public TipoPagoDaoImpl(final java.sql.Connection userConn)
	{
		this.userConn = userConn;
	}

	/** 
	 * Sets the value of maxRows
	 */
	public void setMaxRows(int maxRows)
	{
		this.maxRows = maxRows;
	}

	/** 
	 * Gets the value of maxRows
	 */
	public int getMaxRows()
	{
		return maxRows;
	}

	/**
	 * Method 'getTableName'
	 * 
	 * @return String
	 */
	public String getTableName()
	{
		return "tipo_pago";
	}

	/** 
	 * Fetches a single row from the result set
	 */
	protected TipoPago fetchSingleResult(ResultSet rs) throws SQLException
	{
		if (rs.next()) {
			TipoPago dto = new TipoPago();
			populateDto( dto, rs);
			return dto;
		} else {
			return null;
		}
		
	}

	/** 
	 * Fetches multiple rows from the result set
	 */
	protected TipoPago[] fetchMultiResults(ResultSet rs) throws SQLException
	{
		Collection resultList = new ArrayList();
		while (rs.next()) {
			TipoPago dto = new TipoPago();
			populateDto( dto, rs);
			resultList.add( dto );
		}
		
		TipoPago ret[] = new TipoPago[ resultList.size() ];
		resultList.toArray( ret );
		return ret;
	}

	/** 
	 * Populates a DTO with data from a ResultSet
	 */
	protected void populateDto(TipoPago dto, ResultSet rs) throws SQLException
	{
		dto.setIdTipoPago( rs.getInt( COLUMN_ID_TIPO_PAGO ) );
		dto.setDescTipoPago( rs.getString( COLUMN_DESC_TIPO_PAGO ) );
		dto.setNumeroCuenta( rs.getString( COLUMN_NUMERO_CUENTA ) );
		dto.setClaveMetodoSat( rs.getString( COLUMN_CLAVE_METODO_SAT ) );
		reset(dto);
	}

	/** 
	 * Resets the modified attributes in the DTO
	 */
	protected void reset(TipoPago dto)
	{
		dto.setIdTipoPagoModified( false );
		dto.setDescTipoPagoModified( false );
		dto.setNumeroCuentaModified( false );
		dto.setClaveMetodoSatModified( false );
	}

	/** 
	 * Returns all rows from the tipo_pago table that match the specified arbitrary SQL statement
	 */
	public TipoPago[] findByDynamicSelect(String sql, Object[] sqlParams) throws TipoPagoDaoException
	{
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		ResultSet rs = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			// construct the SQL statement
			final String SQL = sql;
		
		
			System.out.println( "Executing " + SQL );
			// prepare statement
			stmt = conn.prepareStatement( SQL );
			stmt.setMaxRows( maxRows );
		
			// bind parameters
			for (int i=0; sqlParams!=null && i<sqlParams.length; i++ ) {
				stmt.setObject( i+1, sqlParams[i] );
			}
		
		
			rs = stmt.executeQuery();
		
			// fetch the results
			return fetchMultiResults(rs);
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new TipoPagoDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(rs);
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Returns all rows from the tipo_pago table that match the specified arbitrary SQL statement
	 */
	public TipoPago[] findByDynamicWhere(String sql, Object[] sqlParams) throws TipoPagoDaoException
	{
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		ResultSet rs = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			// construct the SQL statement
			final String SQL = SQL_SELECT + " WHERE " + sql;
		
		
			System.out.println( "Executing " + SQL );
			// prepare statement
			stmt = conn.prepareStatement( SQL );
			stmt.setMaxRows( maxRows );
		
			// bind parameters
			for (int i=0; sqlParams!=null && i<sqlParams.length; i++ ) {
				stmt.setObject( i+1, sqlParams[i] );
			}
		
		
			rs = stmt.executeQuery();
		
			// fetch the results
			return fetchMultiResults(rs);
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new TipoPagoDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(rs);
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

        protected final String SQL_SELECT_MAX = "SELECT ID_TIPO_PAGO, DESC_TIPO_PAGO, NUMERO_CUENTA, CLAVE_METODO_SAT FROM " + getTableName() + " ORDER BY ID_TIPO_PAGO DESC LIMIT 1 ";
                
        public TipoPago findLast() throws TipoPagoDaoException	{
		TipoPago ret[] = findByDynamicSelect( SQL_SELECT_MAX , new Object[0] );
		return ret.length==0 ? null : ret[0];
        }

}
